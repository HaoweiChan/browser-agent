# Retired M44 campaign harness

Archived by M48. The runner and its four synthetic cases bind the retired
three-mode campaign to its old deployment/model route and omit the now-required
`X-LLM-Access-Key`; they are retained as historical evidence, not discovered by
`evals.run`. ADR-044 and the original journal/report history remain unchanged.

The reusable completed-run aggregate stop is active as
`canonical-budget-stops-before-next-run`; M52 will define a new authorized
campaign rather than reviving this harness or journal.
