{
  "id": "url-guard-literal-ips",
  "task": "browser",
  "suites": ["fast", "invariant"],
  "input": {
    "kind": "url-guard",
    "checks": [
      ["http://2130706433/", false],
      ["http://127.1/", false],
      ["http://0x7f000001/", false],
      ["http://[::ffff:127.0.0.1]/", false],
      ["http://localhost/x", false],
      ["http://192.168.1.1/", false],
      ["file:///etc/passwd", false],
      ["https://example.com/", true],
      ["https://en.wikipedia.org/wiki/Cat", true]
    ]
  },
  "expect": { "note": "every check pair is (url, allowed)" },
  "provenance": "cold-reviewer finding 2 (M1 close-out): ipaddress.ip_address raises on decimal/dotted-short/hex IP forms and the ValueError branch allowed them as 'named hosts'; Chromium normalizes them to loopback. IPv4-mapped IPv6 also slipped through.",
  "triage": { "class": "invariant-gap", "note": "guard-side silent wrong output; tagged invariant — the guard must always hold" }
}
