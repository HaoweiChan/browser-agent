# Decisions index

This is the current-rules digest: one line per ADR, the ruling only, not the
incident narrative behind it. Every ADR carries a matching `**Ruling**` /
`**Because**` / `**Enforced by**` header directly under its title — read the
full file when you need the "why" or the evidence, read this index when you
just need "what is now true".

**Allocating a number (T-ADR-NUM, 2026-08-28).** Claim the number when the PR
OPENS, not when the ADR is written, and claim it by adding the INDEX row in the
same push — the row is the reservation. "Next free at writing time" is what
produced three forced renames inside one PR (this repo's wall-clock ADR was
written as ADR-010 and shipped as ADR-013) and one more on 2026-08-28, because
concurrent branches all see the same next free number and whichever merges last
renumbers. A rename rewrites one string across ~12 files, and once several have
layered a sweep stops being verifiable by grep: every hit for the old number
looks plausible, since the text around it was written by an earlier sweep.
Reserving early does not prevent a collision — two branches can still open on
the same day — so it is paired with a guard rather than trusted:
`adr-header-and-index` refuses two files sharing a number, a gap in the
sequence, and an INDEX row naming no file. Each was watched red, the first
against the collision that actually happened here.

- ADR-000 — specs/ holds only invariants, output contracts and ADRs; enforcement lives in hooks, never in prose — enforced by `.githooks/pre-commit`, `.claude/hooks/post-edit-invariant.sh` · amended by ADR-001
- ADR-001 — a bounded docs/ planning layer and a milestone-level tasks/TODO.md are allowed; specs/'s three-kind charter is unchanged — enforced by advisory (cold-reviewer subagent, CLAUDE.md rule 3)
- ADR-002 — sets the pre-commit gate: fast ≥ baseline, invariant = 100%, trap-catch ≥ 90%, fast cost = $0.00, and each suite's wall clock ≤ its own environment's measured ceiling (the four numbers are ADR-019 §2/§3/§5 and the one override variable per suite is its §4 — that ADR amends this one; the local pair is graded against `evals/run.py` and README by `published-band-matches-the-ledger`, CI's against the workflow by `fast-wall-clock-budget`) — enforced by `.eval-baseline.json` + `.githooks/pre-commit`, wall clock by `evals/run.py` and graded by `fast-wall-clock-budget` and `published-band-matches-the-ledger` · amended by ADR-009, ADR-013, ADR-019
- ADR-003 — recovery runs on two dispatch-classified ladders (locate→relocate, act→replan), budget exhaustion is classified by the failure it died of — enforced by `diagnosis-classifier-classes`, `relocation-distinct-tier`, `budget-replans-exhausted`
- ADR-004 — the trace stream emits every attempted step including superseded ones, the support matrix is parsed live never duplicated, every gateway failure returns the RunResult shape — enforced by `stream-shows-every-step`, `support-matrix-cites-real-cases`, `gateway-error-contract-shape`
- ADR-005 — answer matching separates value/unit/currency, a replan can't launder a no-op action, recovery requires a real strategy change, the URL guard re-checks after every action, supersede pointers are never dangling — enforced by `verifier-sign-currency-percent`, `replan-cannot-launder-noop-action`, `supersede-never-dangles`
- ADR-006 — implements `near:` as the `structural` tier (document-order nearest anchor, excludes its own subtree, exact-before-substring, refuses on ambiguity), closes 3 quiet-wrong-answer defects — enforced by `near-prefers-the-container`, `near-anchor-substring`, `relocate-fill-non-editable` · amended by ADR-007
- ADR-007 — navigation waits `domcontentloaded` then best-effort-bounded `load`, not strict `load`/`networkidle`; screenshot font-wait gets its own 2s bound; corrects ADR-006's live-coverage table — enforced by `nav-load-event-never-fires`, `nav-action-load-event-never-fires`
- ADR-008 — `not_a_dump` divides value length by real page size against DUMP_RATIO=0.35, gated by MIN_PAGE_CHARS=100, calibrated on a 25-record pinned confusion matrix (tp=10,fp=11,fn=1,tn=3) — enforced by `verifier-precision-recall`, `verifier-sparse-page-not-a-dump`, `verifier-dump-ratio-anchor-flip`
- ADR-009 — mutation catalogue admits 5 capability-breaking mutations (not 6), unrescuable losses are pinned honestly not smoothed over, survival/recovery counters are pure functions, quotes.toscrape.com/js is published unsupported (M38: the DOMAIN row is now `unreliable`; the /js page this ADR is about is unchanged and its case still fails — ADR-009 Decision 4's M38 amendment. None of the three cases below grades the status word, so this drifted silently for as long as it took a spec-drift audit to read both files) — enforced by `mutation-metrics-honesty`, `mutation-catalog-integrity`, `opt-in-expect-keys-declared` · Decision 6 closed by ADR-013
- ADR-010 — the model ablation ships as a graded-empty table (no number without a committed report behind it), the price ceiling is a model not a literal, and only statuses that measure the model are published — enforced by `analysis-ablation-table-not-estimated`, `ablation-env-failure-is-a-result`, `gateway-model-reaches-planner` · amended by ADR-027 (loop-mode model additions are exempt from the price ceiling; the ceiling stays for the ablation arms and mode B's default)
- ADR-011 — `/readyz` reports the run slot as structured JSON and always answers 200, while `/healthz` stays liveness only; graded as an idle->busy->idle transition, not a status code — enforced by `readyz-tracks-the-run-slot`
- ADR-012 — every eval run appends an unconditional history.jsonl line; a full per-case report is written only on request, `--suite all`, or red; 124 uncited routine reports pruned (158 -> 34 files, 4.85MB -> 0.63MB) after a lossless backfill — enforced by `report-citations-resolve`, `evals/run.py`'s write policy
- ADR-013 — ADR-002's `fast` wall-clock ceiling is per-environment: 60s locally (a straddling band briefly moved it to 70s, withdrawn the same day when round-5 review could not reproduce it), CI's own measured from observed bands (slowest run +15%) via `EVAL_WALL_BUDGET_S`, applied by `evals/run.py` to the run it measured; the suite shares one Chromium (own BrowserContext per run, re-launched if it dies) — enforced by `over_budget()` in the runner, `fast-wall-clock-budget`, `agent-launches-its-own-browser`, `shared-browser-relaunches-when-dead` · amends ADR-002, ADR-009
- ADR-014 — the reviewer UI uses TinBoker terminal styling without changing its trace-first architecture, stable DOM hooks, single-page delivery, or external-dependency footprint — enforced by `ui-tinboker-style`, `ui-rendered-narrow`, `stream-shows-every-step`, `gateway-error-contract-shape`
- ADR-015 — the second held-out probe ran RED on the inviolable property (a wrong answer scored success, reproduced 3x) plus a scope-screen bypass; both fixed and eval-pinned in-PR (round-1 review then found the fix citing undeclared support-matrix rows, D21/D22 now declare them); **amended 2026-08-22 (M29): criterion 5 is RED on the deployed build** — the post-merge confirmation reproduced the same class of violation on a plain single-hop task neither M10 fix covers (`docs/analysis.md` §8a-3, `docs/support-matrix.md` D23), fix tracked as M34 — enforced by `docs-numbers-are-derived`, `report-citations-resolve`, `support-matrix-cites-real-cases`, `adr-header-and-index`, `verifier-aggregate-superlative-fails-loud`, `l5-refuse-delete-determiners`, `verifier-aggregate-ground-truth-untouched`
- ADR-016 — M34: `verify()` fails an extraction whose local context (value + surrounding chars) also matches verbatim on a different page the same run visited (site-invariance, not a task-string regex); amended round 1 (PR #30 R1) to compare context instead of the bare value after the bare-value cut flagged a correct listing->detail title as furniture — fixed and demonstrated OFFLINE only, ADR-015 criterion 5 stays RED pending the post-merge live re-run — enforced by `verifier-responsive-not-page-furniture`, `verifier-listing-detail-title-not-furniture`, `tc2-shop-search-zh`, `trap-near-miss-entity`, `docs-numbers-are-derived`, `support-matrix-cites-real-cases`
- ADR-017 — M36: an LLM judge is the LAST rung of the escalation ladder, called once at the terminal-verdict boundary only when every L1 check already passed; fails CLOSED on reject/error/timeout/missing-key/budget-exhausted, stubbed at $0 in `fast` via the same injection-boundary shape as `stub_planner`, prompt structurally isolates untrusted page evidence from instructions — fixed and demonstrated OFFLINE only (no OPENROUTER_API_KEY in this environment), ADR-015 criterion 5 stays RED pending the post-merge live re-run — enforced by `judge-catches-varying-context-furniture`, `judge-fail-closed-on-error`, `judge-fail-closed-on-any-exception`, `judge-missing-key-fails-closed`, `judge-run-budget-enforced`, `judge-fast-suite-stub-boundary`, `judge-cache-hit-needs-no-key`, `judge-injection-cannot-flip-verdict`
- ADR-018 — a superlative task whose plan does not read the page exactly once with `extract_all` is rejected at every point the executor adopts a plan and replanned once through the existing replan budget; `extract_all` enumerates and `verifier.rank` ranks in code, never the model, both gated on the one `is_aggregate` shape; an LLM debater was considered and rejected — enforced by `verifier-aggregate-superlative-fails-loud`, `probe3-quotes-most-quoted-author`, `rank-reduces-enumeration-in-code`, `plan-gap-truth-table`, `extract-all-refuses-a-selector`, `extract-all-list-task-keeps-every-row`, `extract-all-cheapest-wording-still-reduces`, `plan-lint-holds-across-a-midrun-replan`, `replan-cannot-launder-noop-action-extract-all`, `planner-prompt-carries-the-note`, `verifier-aggregate-ground-truth-untouched` · amends ADR-015
- ADR-019 — four wall-clock ceilings, one per (suite, environment), each derived by ADR-013's own +15%-round-to-five rule — the local pair from a band computed from the committed ledger, the CI pair hand-read off the log of a named workflow run, which §7 explains rather than papers over: local `fast` 60 -> 80 [local], local `invariant` 20 [local], ~~CI `fast` 80 -> 90, CI `invariant` 20~~ — struck [historical], re-derived by ADR-029 from a measurement of the tree M42 ships and published in ADR-019 §5, which grades them against the workflow (PR #57 R33); read through one override variable per suite so a tag cannot be an unbounded relief valve for the `fast` gate — enforced by `fast-wall-clock-budget`, `published-band-matches-the-ledger`, `published-band-slack-is-declared`, `evals/run.py` `over_budget()` · amends ADR-013, ADR-002 · §6 (2026-08-23) declares the band property's slack — a published number may sit up to one ceiling step below the ledger's maximum (the bound itself is published in §6 and derived from the rule, so it is not restated here), the strict `published >= ledger max` refused on frequency (a doc edit per new maximum, against one per band crossing) and not on the append-after-grading lag, which both forms share; §6's numbered list is what the grader enforces, and every reference to it — in that file, in README and in the marked band region of the check's source — must name the item it points at, slug and all, or the gate is red; §7 (2026-08-23) gives every ledger row an environment tag and §6 item 9 (environment) grades a band only against rows from its own, after CI's `invariant` row — appended to the job's copy of the ledger by the step before, clean, and stamped in UTC where the band row was stamped in local time, so it sorted eight hours before a run it followed by 25 minutes — reddened a band measured on a laptop through item 2 (cited-run)'s dirty allowance, which is what made adding a case cost two commits instead of one (T-R44); and the `ts` that clause orders on is stamped UTC in the same change — two properties, not one, since a foreign row is the wrong row to derive a ceiling from whatever its stamp says (T-M32-13, closed); §8 (2026-08-28) grades a band against the slowest CITABLE row — item 2 (cited-run)'s own dirty test applied to every row, so a row no band may legally cite is not a target any band is held to — after a dirty verification run past a rounding step, at an UNCHANGED case count, left a tree with no publishable citation at all and three more such rows were discarded by hand in one session on 2026-08-27; every run still appends to the ledger (ADR-012 unchanged) and only citable rows govern a band — enforced by `band-is-graded-against-the-citable-maximum` (T-M39-13); §9 (2026-08-28) re-derives CI's two ceilings from a cross-commit sample of measured RUNS — four slowest per suite — in place of four attempts of one run, after a run that breached on `invariant` turned out to have no `fast` figure at all (the job exits at the first suite), so the row that sets the ceiling could not enter a two-column table without being dropped; the live values are ADR-019 §5's and the workflow's, graded against each other, and the motivation is on record as a compliance gap rather than a branch's growth — with BOTH breaching runs removed, `main`'s own CI runs already derived higher ceilings than were committed, for both suites · amended by ADR-021 (local `fast` 80 -> 90)
- ADR-020 — M32: the plan vocabulary gains `observe`, a drill-down whose `target` names a container the planner was already shown; the executor re-observes that subtree with the full `MAX_ELEMS` budget and a 1,500-char text head and returns it through the replan's existing observation+note path, spending the existing `MAX_REPLANS` budget and no new one — progressive disclosure of the page, not of the tool set; it is not a recovery, must not wear the label, and an unresolvable `observe` fails loudly rather than relocating to a different subtree; 017/018 skipped to avoid an in-flight collision — enforced by `observe-drilldown-past-max-elems`, `observe-cap-hides-the-answer-element`, `observe-blind-plan-dumps-the-container`, `observe-refused-drilldown-stops-the-run`, `observe-drilldown-no-progress-stops-the-run`, `observe-drill-into-chrome-gets-the-page-budget`, `observe-drill-text-head-reaches-past-300`, `observe-cannot-launder-noop-action`, `observe-cannot-launder-extract-all`, `observe-drilldown-cannot-launder-noop-action`, `observe-drilldown-cannot-launder-unchecked-action`, `recovery-label-lands-on-the-extract`, `observe-document-root-does-not-relocate`, `observe-step-cannot-carry-expected-state`, `planner-note-is-not-always-a-failure`
- ADR-021 — the local `fast` ceiling moves 80 -> 90, taken from `published-band-matches-the-ledger`'s own `required_by_adr013_rule` at 146 cases (ledger slowest 74.8s) rather than derived by hand; `invariant` stays 20; ~~CI's `EVAL_WALL_BUDGET_S_FAST` stays 90 and that is a RULING~~ — OVERTURNED by ADR-029, which re-derived both CI ceilings from a measurement of the tree M42 ships; the live values are in ADR-019 §5 and the workflow, graded against each other — CI run 32639577041 measured the shipped tree `07e3d34` at `fast` 152/152 in **74.25s** (17.5% margin) and `invariant` 58/58 in 14.88s, ADR-013's rule applied to 74.25s derives exactly the committed 90, and the human was asked and answered; the raise had been requested against the PARENT `920218e` (88.39s, 1.8% margin, run 32627229208) and the shipped tree's own measurement removed the premise by coming in FASTER on a LARGER suite; two earlier versions of this line published absolute claims about what CI had not measured and both were falsified within the hour (PR #34 R22, R28), so the ADR now scopes such claims to the commit that makes them; the margin question is NOT closed on the numbers, but its live form is: T-M32-13's second symptom (CI rows entering `ledger max` mid-job, ungreenable locally) was closed by T-R44 giving every row an environment tag, per ADR-021's 2026-08-23 amendment, and ADR-021's CI figures remain hand-read, carrying the run ids that make them reader-checkable (T-R73 keeps the ledger route open); every non-ceiling case passes and the growth is case count, not per-case cost — if a future gap comes from per-case cost the answer is removing waste (T-M32-3), not another raise — enforced by `published-band-matches-the-ledger`, `fast-wall-clock-budget`, `evals/run.py` `over_budget()` · amends ADR-019
- ADR-022 — a real-site support-matrix row may be declared from repeated end-to-end deployment runs with the real planner and NO eval case, provided the row carries every run id, its repeat count and a declared limitation naming what failed, and is marked as the exception it is; the reviewer UI gains a trace-derived page view (step screenshot + the extractions with their page text), maps read-only `observe` steps to a distinct **Read page** phase, has a CSS-only running-stage spinner, and must reach a terminal state on every ending — enforced by `ui-terminal-state-on-every-ending`, `ui-execution-progress-is-trace-derived`, `ui-tinboker-style`, `ui-examples-cover-matrix`, `ui-rendered-narrow` · amends ADR-014 · concedes in writing that no gate re-checks the three rows it declares, that their citation rule admits run ids instead of case ids, and that nothing grades either that rule or D28's arithmetic
- ADR-023 — M39: an unreadable judge completion (empty or non-JSON body) is a failed READ, not a verdict, and buys exactly one more attempt at the same call with the same prompt (`JUDGE_ATTEMPTS = 2`); a refusal, a parsed body with no `certify`, a missing key and a reasoned FAIL are never retried, both attempts are billed, `RUN_JUDGE_BUDGET` stays 1 boundary call per run and the verdict records `checks.judge_attempts` — ADR-017's fail-closed ruling is narrowed in WHEN it fires, never in WHETHER — enforced by `judge-retries-one-malformed-completion`, `judge-two-malformed-completions-fail-closed`, `judge-retry-only-on-unreadable-completion` · amends ADR-017
- ADR-024 — a plan that extracts from the accessibility document root (`WebArea`/`RootWebArea`, stripped and case-folded; NOT ARIA `document`, which is an in-page container that resolves) is refused by the plan lint for every task shape, above `plan_gap`'s `is_aggregate` early return — one replan from the existing budget before execution, and the end of the run at a mid-run adoption point, where the replan is what produced the plan; extraction verbs only, so M32's `observe` drill-down onto a container is untouched, and the ROOT only, so every other container stays with `verify`'s calibrated `not_a_dump` (ADR-008); `relocation_candidates` will not propose a root rung either — the relocation rung it degrades into (`{text: <the page title>}`) is unreachable rather than separately forbidden, because a refused plan never reaches `resolve`; the observation still SHOWS the root as element #1 and the planner prompt is unchanged, both deliberately (T-M40-2-1, T-M40-2-2) so a post-fix live probe can attribute what it measures — enforced by `plan-lint-refuses-a-document-root-extract`, `plan-gap-truth-table`, `relocation-distinct-tier` · second rule of the lint ADR-018 established, whose own ruling is unchanged
- ADR-025 — pre-registers the T-M40-5 post-fix probe before it runs: the six-task set (four regressed groups — x-rates.com, multpl.com, quotes.toscrape.com author page, openlibrary.org — plus two controls, companiesmarketcap.com and bankofcanada.ca), verbatim task text and start URLs, the protocol (3 runs/task via `POST /tasks`, default model, every run_id/answer/status/cost/wall-time published, ground truth re-verified by `curl` at probe time), and fixed thresholds — zero wrong-success is a hard fail, regressed set needs ≥50% correct, controls allow at most one miss each off their prior post-M32 rate, refusals counted separately — enforced procedurally: no code, the push timestamp is the pre-registration evidence and T-M40-5's acceptance clause is the tracking hook
- ADR-026 — M38: a target that resolves to N>1 elements is narrowed by the page before it is failed — three site-agnostic rungs tried only after every tier has had its chance at a unique match: the step's identity `anchor` reused as a proximity anchor, the first match in document order under four conjuncts (no `index`, a singular task, a READING step, and matches interchangeable in role AND rendered text), and `near` anchors matched through typographic variants then by a 40-character prefix; the rung that fired is named in the trace `note` and is deliberately NOT `retry_or_recovery` (nothing failed, no ladder ran — ADR-020's ruling); rung 2's guard is one conjunct wider than tasks/TODO.md M38 asked for, because role alone is vacuous on the role tier and text alone is vacuous on the text tier; failing loudly on every ambiguity was the rejected alternative and stays the behaviour everywhere the page does not settle it — enforced by the rungs — `resolver-narrows-by-anchor-proximity`, `resolver-narrows-identical-matches`, `resolver-near-normalises-typography` — and by one case per guard conjunct, each red when and only when its own conjunct is removed: `resolver-refuses-narrowing-a-click` (acting steps), `resolver-refuses-mixed-roles` (role half), `resolver-refuses-different-readings` (text half), `resolver-refuses-plural-with-anchor` / `resolver-refuses-plural-wording` / `resolver-refuses-plural-on-a-loose-anchor` / `resolver-refuses-a-click-on-a-loose-anchor` (the two refusals across all three rungs) and `resolver-narrows-singular-noun-ending-in-s` (the plural test's over-firing direction); `l4-shop-duplicate-labels` does NOT pin any of them (PR #42 R2) and keeps its own job; `near-equidistant-is-ambiguous`
- ADR-027 — the interviewer mandate of 2026-08-25 ("complete the task by any means, cost is not a constraint") makes the per-step agentic loop a deliverable capability rather than M33's ablation question: a loop mode is built as a peer of mode B (model called after every action with a fresh observation, wider action vocabulary, M43 adds vision), sharing the executor/resolver/trace/verifier/judge; the plan-adoption-anchored guards (ADR-018's aggregate single-read, ADR-024's document-root refusal) are re-homed to tool-call time in loop mode, red-first, before the mode ships; cost ceilings become runaway protection while accounting stays per-run; explicitly NOT reached by the mandate — zero wrong-success, rule 6, eval-first (a scripted tool-call stub keeps `fast` at $0), and the trace as the single evidence pipeline — enforced procedurally: tracking hooks are tasks/TODO.md M42/M43/M44, each landing with its own ADR and red-first cases · amends ADR-010 (loop-mode model additions exempt from the price ceiling) and, scoped to loop mode only, ADR-018/ADR-024 (adoption-point anchors re-homed) and ADR-020 (loop-mode observe spends the step budget)
- ADR-031 — pre-registers the M45 Chinese-language probe before it runs: four **language-paired** shapes (the interviewer's plain zh search and plain zh QA, plus the companiesmarketcap.com and bankofcanada.ca M40 cards), each 3× in Chinese and 3× in English against the same URL on the same build, plus five single-rep screening shapes (密碼學 / 購買力平價 / 刪除-as-subject-matter in the false-positive direction, the destructive and auth zh asks in the true-positive direction); verbatim task text, start URLs, ADR-025's four metric classes reported separately, and — new here — written-down predictions and verdict rules, so "中文都會失敗" is graded against criteria fixed before the results are known; the English arm is what separates "Chinese fails" from "this task fails", and §What this probe deliberately does not settle declares that zh 下載/付款 stay blocked on a mention because the English side is pinned that way (`screening-word-boundary`) and moving it would move the refusal policy — enforced procedurally: this ADR's commit on `task/M45` precedes the report commit, which is weaker evidence than ADR-025's push timestamp and says so
- ADR-028 — M42: loop mode ships as a peer cadence and shares every piece of machinery that grades it — one driver call per step (OpenRouter native tool-calling, twelve tools, `final_answer` carries NO answer text so the answer is still assembled in code from what was extracted), the same `execute`/`attempt`, and one extracted `finalize` both modes reach so there is a single answer-assembly/verify/judge tail; ADR-024's document-root refusal and ADR-018's aggregate single-read rule are re-homed as ONE function with two callers each (`root_target_gap` asked by `plan_gap` and by `execute` as the call is emitted; `plan_gap` asked again at answer assembly over the executed trace) rather than copied for the loop; the vocabulary widens for BOTH modes with `select_option`/`scroll`/`press`/`wait_for`/`go_back`, each with a postcondition, and `verifier.STATE_CHANGING` widens the unverified-state-change guard from `click` to `{click, press, go_back}`; observation and evidence pierce iframes (measured structurally invisible AND unresolvable before) and open shadow roots (already resolvable — what was blind there was `page.inner_text('body')`, so a correct read was failed as ungrounded, now one `observe.page_text` behind all five former call sites); a no-progress harness forces a strategy change then ends the run loudly on the visit after, counting ARRIVALS not turns after its own golden case caught the first version killing a working run; loop budgets `{actions 40, llm_tokens 400k, llm_usd $5}` are injectable so the ceilings are graded without simulating $99 of spend onto a $0.00 suite's headline; the trace gains NO fields; and `ALLOWED_MODELS` gains `anthropic/claude-opus-5` as a third list (`LOOP_MODELS`), deliberately outside `ABLATION_MODELS` so ADR-010's sweep is untouched, at 3.1x/7.8x the ceiling model's price — read from the live endpoint and frozen, never typed — enforced by `loop-drives-a-fetch-then-render-page`, `loop-mode-b-cannot-read-the-un-awaited-result`, `loop-refuses-a-document-root-extract`, `loop-aggregate-single-read-at-answer-assembly`, `loop-aggregate-enumeration-is-accepted`, `loop-no-progress-revisit-ends-the-run-loudly`, `loop-token-ceiling-stops-the-run-loudly`, `loop-usd-ceiling-stops-the-run-loudly`, `contract-trace-schema-loop-mode`, `gateway-mode-selects-the-driver`, `gateway-model-reaches-planner`, `gateway-model-not-allowlisted`, `action-select-option-verifies-by-readback`, `action-select-option-refuses-an-absent-option`, `action-scroll-moves-the-viewport`, `action-scroll-that-moves-nothing-is-loud`, `action-press-carries-a-postcondition`, `action-wait-for-reaches-a-late-predicate`, `action-wait-for-that-never-holds-is-loud`, `action-go-back-returns-to-the-previous-page`, `observe-reaches-into-an-iframe`, `shadow-dom-value-is-reachable-and-grounded` · implements ADR-027 · amends ADR-010 (the declared, price-recorded loop-mode exemption) and, scoped to loop mode, ADR-018/ADR-024
- ADR-029 — the local `fast` ceiling moves 90 -> **105s**, the number ADR-013 Decision 3's rule derives from this tree's own committed ledger, after M42 grew the suite — the figure and its arrow are published once, in ADR-019 §2's band bullet, which `published-band-matches-the-ledger` grades, and are deliberately not retyped here; the raise is CASE-COUNT growth, which is the condition ADR-021 named when it ruled that per-case growth is answered by removing waste rather than by another raise, and trimming the cases to fit was refused because the three most expensive of them are the three that measure the milestone (a wait_for timeout burns the settle budget BECAUSE that is its postcondition); CI's two ceilings were re-derived from eval-gate run `32937020758`, the first CI run of this tree [historical] — superseded 2026-08-28 by ADR-019's cross-commit re-derivation, which is where the live CI numbers are, and published once in ADR-019 §5 where `ci-numbers-are-derived` reads them back against the workflow — this entry deliberately retypes neither (PR #57 R24); `invariant` is untouched at 20s, which its own rows still derive — enforced by `published-band-matches-the-ledger`, `fast-wall-clock-budget` (whose boundary rows move with the number), `evals/run.py` `over_budget()`; ~~`invariant` is untouched at 20s, which its own rows still derive~~ (struck 2026-08-28: ADR-036 re-derived it, republished in ADR-019 §3 at each rebase; the ceiling, the count and the band live there rather than being retyped here — PR #66 R15 found this entry and ADR-029's own Ruling still publishing a superseded number after the count beside it had been corrected) — enforced by `published-band-matches-the-ledger`, `fast-wall-clock-budget` (whose boundary rows move with the number), `evals/run.py` `over_budget()` · amends ADR-019 Decision 2 · amends ADR-019 Decision 2
- ADR-030 — M41: pre-registers the sec-10k inspector probe before it runs (two verbatim task texts, one deep-link start URL, 3 reps each via `POST /tasks` with no model override, ADR-025's four metrics never blended, and a row-status rule frozen in advance — 6/6 `supported`, 3–5 `unreliable`, 0–2 `unsupported`, any wrong-success overriding all of it), rules the inspector's `/api/extract/fixture` an eval-side-only ground-truth endpoint under rule 6's third allowance, and scopes the matrix row it declares to mode B with M44 owning the loop-mode column; takes number 030 rather than 028 because #56 and #57 already collide there — enforced by `sec10k-ground-truth-endpoint-eval-only`, `sec10k-doc-status-is-a-named-status`, `sec10k-item-count-is-in-the-named-status`, `sec10k-item-text-region-resolves`, `sec10k-item-text-region-is-past-the-observation-cap`, `sec10k-extract-buttons-are-distinguishable`, `live-sec10k-authored-wait-reaches-the-doc-status`, `live-sec10k-authored-wait-reaches-the-item-count`
- ADR-032 — a name tier is ONE match set, and the T-M42-20 case-fold is how that set is CHOSEN rather than a second set beside it: the case-exact matcher is counted first and the case-folded one used only when it is empty, so `near`, `index`, `many`, the `n == 1` uniqueness check and M38's ambiguity bookkeeping all read the same locator and a page that spells the name the plan's way never consults the fold at all (byte-for-byte pre-T-M42-20 behaviour, at every tier, for every rule). Written after two review rounds found the same shape twice — PR #60 R1 widened the set every selector reads (`{name: 'Add to cart', index: 0}` moved onto an `ADD TO CART` banner, silently), and PR #60 R10 re-based `index` per pass so `index: 0` and `index: 1` returned the same element and a plan asking for the second match was answered `success` with the first. **Fallback, not union**: a union puts the case twins back into the set `index` indexes, which is R1 by another route. A folded resolution is disclosed as `narrowed: name-case-folded`, both matchers stay whole-string (`resolver-substring-name`), and what it does NOT settle is named — the ROLE half of the same engine disagreement (`T-M42-20-D4`, support-matrix D32), anchors as a round trip, and composed disclosures (`T-M42-20-D10`) · amends ADR-026 · enforced by `resolver-index-selects-from-one-match-set`, `resolver-case-twin-index-picks-the-exact-spelling`, `resolver-case-twin-near-picks-the-exact-spelling`, `resolver-case-fold-is-recorded-in-the-trace`, `resolver-folded-name-with-a-slash-resolves`, `resolver-substring-name` (2026-08-27)
- ADR-033 — the deployment can finally say which commit it is running: `/version` reports the build sha from a file the Dockerfile writes at build time out of Zeabur's `ZEABUR_GIT_COMMIT_SHA`, refuses anything that is not a whole-string git sha, and never falls back to the local git checkout — so an unknown build answers `null` with a `source` saying why, rather than a real sha of a different tree. A file and not an environment variable, because a service-level variable shadows an image `ENV` at runtime and a hand-set sha is correct until the next deploy and a confident lie after it; there is deliberately no hand-set RUNTIME fallback, so a build that supplies nothing is published as `unavailable` rather than filled in by hand; what that does not buy is immunity to an operator who fills the build argument itself, which `source: "image"` cannot tell from a platform-supplied sha, so the only recommended remedy is a sha the build derives. Written for M44's "both build shas" acceptance clause and for the ceiling `deploy-smoke.yml` has stated in a comment since M5. Whether Zeabur passes that variable to a Dockerfile build is deliberately unverified and fails to the honest null either way — enforced by `version-never-guesses-a-build-sha` (2026-08-27)
- ADR-034 — the deployed `/version` answered `{"sha":null,"source":"unavailable"}` on 2026-08-28, settling ADR-033's open fact: Zeabur does not pass its Git-group variables to a Dockerfile build. The build now derives its own sha — a build-identity stage on the same base tag runs `git rev-parse HEAD` against the context (`.dockerignore` admits `.git`; the final image COPYs the one file, never the tree), a derivation that fails for any reason exits 0 and writes nothing, so `/version` answers `unavailable` rather than breaking the build or guessing, and the never-filled `ARG ZEABUR_GIT_COMMIT_SHA` is dropped because the only value still able to arrive through it would be an operator-typed one that every later build re-bakes (ADR-033 Decision 2's rejected path at build time). Whether the builder keeps `.git` in the context stays unverified either way — the next post-merge read settles it against the merge commit — enforced by `build-sha-is-derived-not-supplied` (2026-08-28)
- ADR-035 — M43: loop mode looks — every loop-mode observation carries the viewport screenshot that is already the trace step evidence (same `step_N.png`, referenced by filename, never a second capture and never inline base64; the pre-plan navigate gets one as the loop starts, filling its existing `screenshot` field), a loop-mode drill also captures an element-scoped `step_N_element.png` attached to the scoped observation it discloses, and the executor gains `click_at` (`value` = `"x,y"` viewport CSS pixels, no target, joins `verifier.STATE_CHANGING`) refused at tool-call time unless emitted from an observation bearing a VIEWPORT screenshot — an element-scoped drill image does not arm it — the gate reads the frame LABEL, which records that the crop was taken to show a sub-region, and never its geometry (PR #70 R10: the geometric justification is false for a crop that covers the viewport, where the refusal is still correct); `live_driver` sends the image as a data-URL content part beside the unchanged text; NO model is added — ADR-028's `anthropic/claude-opus-5` is already vision-capable per the frozen snapshot, so `ALLOWED_MODELS` and the graded exclusion are untouched and ADR-027 Decision 4's amendment rule is satisfied vacuously; offline cases grade the plumbing at $0, vision QUALITY is declared live-only (M44 + the S2 smoke this ADR scripts) — enforced by `loop-observation-carries-the-step-screenshot`, `loop-drill-observation-carries-an-element-screenshot`, `loop-click-at-resolves-and-records-coordinates`, `click-at-without-a-screenshot-is-refused`, `loop-click-at-from-a-drill-observation-is-refused`, `s2-bare-value-loop-vision-path-answers` / `s2-bare-value-mode-b-root-reach-is-pinned` (the contrast pair), the extended `opt-in-expect-keys-declared`, `driver-tools-match-the-executor`, `contract-trace-schema-loop-mode` · implements ADR-027 Decision 3 · amends ADR-019 Decision 2 (the local `fast` ceiling 105 -> 110, the number ADR-013 Decision 3's rule derives from the ledger at the new case count; the figure is published once, in ADR-019 §2/Ruling and README's band table, and is deliberately not retyped here)
- ADR-036 — a postcondition is checked in the document its action touched: `check_state`'s `text_visible`/`role_visible` are evaluated in the ONE FRAME `resolve` returned the step's target from — recorded on the trace as `resolved.scope`, the field T-M42-14's acceptance also names — a frame and not a document, which enforces frame liveness and in the same breath leaves a same-frame re-navigation open (a document swapped into that frame in place is read as though the action had touched it; `is_detached()` never fires), declared in ADR-036 §4's "What this guard does NOT close" block and owned by T-M42-14 — with a targetless step scoped to the main document, and three sites page-wide by nature (`url_contains`, and the whole `expected_state` of `navigate`/`go_back` and of `wait_for`, because a wait for a page that paints into an iframe legitimately wants the frame); an acted document that is GONE — detached while its own step ran, by that step (an in-frame click that re-mounts the widget, an in-frame `target="_top"` link) or by the page's own re-render — makes the postcondition null/unverifiable rather than reading silently false (PR #66 R3) or falling back to page-wide, which let a decoy iframe verify a no-op whose frame a timer detached and reopened this ADR's own hazard (PR #66 R6, §4 amended in place); the evidence pipeline (`page_text`, `grounded`, `page_changed`, the digest) is deliberately untouched; closes T-M42-4's accepted HIGH (a decoy iframe satisfying a click's postcondition, demonstrated by T-M42-11's repro on the committed `frames-host.html`, watched red as `status success` / `postcondition_ok true`) — enforced by `postcondition-decoy-iframe-cannot-satisfy-text-visible`, `postcondition-decoy-iframe-cannot-satisfy-role-visible`, `resolved-scope-names-the-acted-document`, `main-document-click-cannot-verify-a-frame-only-effect` (the ruling's cost, measured: the click's postcondition goes false and a `wait_for` replan recovers it), `postcondition-scope-detached-by-its-own-action`, `detached-scope-cannot-be-verified-by-a-decoy`, with `shadow-dom-value-is-reachable-and-grounded` and `replan-after-an-iframe-only-change-is-not-laundering` holding the directions that must not move · amends ADR-028 §7 (dated, `resolved.scope` admitted; the no-new-fields rule stands for anything further)
  Amendment 2026-08-29 (T-M42-14): supersedes the preceding summary's same-frame-navigation ceiling. An internal one-use marker now binds iframe predicates to the exact resolved document; replacement makes the postcondition null and is disclosed in the trace, while `resolved.scope` remains URL provenance and the evidence pipeline remains frames-aware.
- ADR-037 — M46: plan-then-loop escalation ships as a POLICY and not a third cadence — `run_task` gains `mode="escalate"` (its own `POST /tasks` spelling, both factories required, neither existing mode's behaviour touched), which runs mode B once and re-runs the same task in loop mode only if that leg ended in one of the seven failure classes AND attempted no state-changing action — ANY plan-leg step in `verifier.STATE_CHANGING`, whatever its `postcondition_ok` (a verification outcome is not an execution fact: `false` means the authored predicate did not hold and says nothing about whether the click took effect, `null` means nobody checked, and an unknown is not an absence — PR #78 R8 retracts the opposite sentence and gives each cell its own case), refuses escalation, so the plan leg's own failure is the run's answer with the refusal appended to `reason` in the same closed vocabulary; the cost is stated rather than hidden — escalation now reaches only plan legs that changed nothing (Decision 2a, PR #78 R1: the first version bounded this hazard with `screen()`, a keyword regex over task text whose declared false negatives are exactly imperatives — `Place my order and tell me the total` passes it — so the residual was not a duplicated form submission but a second real payment); `success` and `unsupported` never escalate either, the second because `screen()` refuses identically at the top of both legs, seeding the loop's opening note with `agent.escalation_note` — four slots, every one closed: the failure class from `FAILURE_CLASSES`, the dying step's index as an int, its verb from `ACTIONS`, and its target KEY NAMES from `TARGET_KEYS`, so no page text, no target value and no error string can cross between legs, which is rule 6 and the injection boundary in one clause; the two legs concatenate into ONE RunResult — the loop leg runs at `step_offset = len(plan leg trace)` so its `i` values and `step_N.png` names continue rather than overwrite the superseded leg's, and every plan-leg step not already superseded is marked `superseded_by` the loop leg's first step, which is both what a supersede means and what keeps the merged trace gradeable by the same `verify()` the run used — with budgets summed into `budgets_spent` and reported per leg in a new `legs` array whose entries carry the superseded leg's own extractions (superseded, never hidden), while `evidence.extractions` stays the FINAL leg's because that is what the verdict was computed from; the verifier and the judge run in the legs' own `finalize` and never in the merge — once per leg that reaches an answer, so a plan leg the judge rejected has already spent its own boundary call and the summed `judge_calls` says so — the RunResult's verdict is the final leg's, and the merged record is nonetheless assembled through `assemble_result` so INV-0/INV-1/INV-2 are re-applied rather than inherited on trust; the gateway runs both legs on ONE model defaulting to the loop's, because the loop leg is the one with a hard tool-calling requirement; no orchestration dependency (ADR-027's ruling — LangGraph adds no observation, no action, no verification and would re-wire the offline stub boundary), no auto-selection beyond "B first, loop on failure", and no third leg — enforced by `escalate-plan-success-never-starts-the-loop`, `escalate-fires-on-a-failed-plan-leg`, `escalate-refuses-after-a-completed-state-change`, `escalate-refuses-after-a-failed-postcondition`, `escalate-refuses-after-an-unverified-state-change`, `escalate-keeps-both-legs-observations`, `escalate-budget-exhaustion-stays-loud`, `escalate-seeded-note-cannot-smuggle-an-instruction`, `escalation-note-is-closed-vocabulary`, `contract-trace-schema-escalate`, the `escalate` row in `gateway-mode-selects-the-driver` and the extended `opt-in-expect-keys-declared`, each watched red first in `docs/evals/m46-red-first-ledger.md` · implements ADR-027 (Decision 1's peer-mode ruling, extended by policy rather than by a third cadence) · amends ADR-019 Decision 2 (Decision 9's local `fast` ceiling 110 -> 115, the number ADR-013's rule derives from the ledger at the new count of 246; `invariant` stays 35, which 97 cases still derive — the figures are published once, in ADR-019 §2/§3's band bullets, and are deliberately not retyped) (2026-08-28)
- ADR-038 — a page-held credential is seeded PER ORIGIN by the browser and never injected into pages: `BROWSER_AGENT_ORIGIN_STORAGE` (`{"<origin>": {"<key>": "<value>"}}`) is read by `agent.origin_storage_state()` and handed to `browser.new_context(storage_state=…)`, so the value is written into each origin's own partition before any page script runs; unset returns `None`, Playwright's own "no seeding", leaving every existing run byte-identical. `add_init_script` is REFUSED and the refusal is graded: this agent browses arbitrary sites, an init script reaches every page's JS context, and an origin guard inside it only decides whether to WRITE while the value is already there to read. Needed because the sec-10k inspector put its paid tier behind a key the PAGE holds and its deep-link start URL — already an `EXAMPLES` row here — extracts during load, before an agent could type into any field. Rule 6 allows a start URL, a rate limit and a ground-truth endpoint; a credential is none of them, so the host, the storage key and the secret are ALL configuration, the secret from the environment alone (rule 8), and malformed config RAISES including the two silent-no-op shapes Playwright matches against nothing (bare host, origin with a path). Also repairs a PRE-EXISTING defect that had made the local gate unpassable: `docs-numbers-are-derived` walked git-ignored `.claude/worktrees/` and graded another branch's checkout — six ceiling breaches on clean `main` that no commit here could fix — so `worktrees` joins `.git`/`.venv` in the prune set · enforced by `origin-storage-seed-is-scoped` (AST-graded, four conjuncts, three mutations watched red)
- ADR-039 — the observation waits for what the page is still FETCHING, and a refused identity anchor is a replan rather than the end of the run: (1) after its bounded `load` wait, `navigate` waits — bounded by the same `SETTLE_BUDGET_MS` a postcondition gets — for the requests that were in flight at `load`, tracked as a request SET by listeners attached before `goto` and torn down in a `finally`, so a static document pays one emptiness test where `networkidle` charged every navigation 500ms (measured twice and rejected twice: +34s at ADR-002, and +51.4s taking the fast suite from 93.5s to 144.9s over a 110s ceiling even when gated on the document carrying a `<script>`); (2) `cls == "act"` becomes `cls in ("act", "semantic")` in the executor's replan family, with the identity-anchor check moved AHEAD of the `answers`/`extractions` appends so a refused read cannot be carried into the replan's answer, and the laundering guard scoped by `not reads_without_acting([step])` so it still refuses a replan that drops a failed ACTION while allowing the re-read that is the correct recovery for a failed EXTRACTION. Closes the 2026-08-24 postmortem's S1 for the fetch-painted-control case after it was declared-and-unfixed for four days and then cost deployed run `46883372` (`intel 2025` on the sec-10k inspector: 42 filings in a `<select>` painted from `/api/meta` after `load`, zero of them in the planner's observation, an invented EDGAR accession number, `failure:semantic` with 0 of 2 replans spent). Does NOT close a fetch issued from a `setTimeout` after `load` (T-A39-1), and records that `live-sec10k-authored-wait-reaches-the-doc-status`'s `lacks` clause is now green for a different reason than its provenance states (T-A39-2) — enforced by `planner-sees-a-fetch-painted-select`, `replan-after-a-refused-anchor`; §6 (2026-08-28) charges an observation PER CONTROL rather than per page — an `option` keeps its place in `elements` but is charged to its own combobox/listbox/menu budget (`observe.MAX_OPTIONS`, a runaway bound of 60) instead of to `MAX_ELEMS`, the per-container idea `MAX_CHROME` has had per landmark since M32 — after §1's settle let the sec-10k inspector's 49-option `<select>` paint and push `status — 'doc_status'` out of the observation entirely (T-A39-3, measured not predicted); raising `MAX_ELEMS` is refused for the third time on `observe.py`'s own reason, and the slots freed put the extracted-text region M41 had DECLARED unreachable-by-observation back inside the cap, inverting `sec10k-item-text-region-is-past-the-observation-cap` from a declared limitation into a positive assertion; §6 also records that the freed slots exposed two ROLE ORACLES disagreeing — Chromium's snapshot says `columnheader`/`DisclosureTriangle` where `get_by_role` says `cell`/nothing — which the relocation ladder already answers, so the observe case reports `advertised_needed_relocation` rather than failing, and a resolver-side fallback tier is rejected on measurement because it turned four mutation cases green with no recovery performed · amends ADR-002 (the `networkidle` rejection stands and is re-measured), ADR-003 (a third dispatch class joins the act ladder), ADR-020 (the observation budget gains a per-control dimension) (2026-08-28)
- ADR-040 — `screen()` may narrow only a measured risky mention inside a read frame with no request frame, and may widen ambiguous `登陸`/`登陆` only inside an explicit action request; bare commands and unmeasured ambiguity still fail closed, while the Chinese and English informational rows are allowed to reach the browser — enforced by `screening-zh-term-inside-another-word`, `screening-word-boundary`, `l5-refuse-destructive-zh`, `l5-refuse-delete-determiners` (2026-08-29)
