{
  "id": "ui-no-url-guard-and-example-chips",
  "task": "browser",
  "suites": [
    "fast"
  ],
  "input": {
    "kind": "ui-form",
    "no_url_task": "how much did Situational Awareness sell to Citadel?",
    "site_task": "On books.toscrape.com, what is the price of the first book?",
    "viewport_width": 390,
    "scheme": "dark"
  },
  "expect": {
    "site_url": "https://books.toscrape.com",
    "guidance_contains": "start URL",
    "limits_contains": "2 declared limitations"
  },
  "provenance": "M35 eval-first. Deployment run 2ce3e5c8 (2026-08-22): a task with no start URL and no page behind it (\"how much did Situational Awareness sell to Citadel?\") was accepted by the form, the planner planned blind with no page observation, navigated once to a Google search and died at extract — a spent run with a guaranteed failure. This case drives the rendered page on the `_ui_page` render it shares with `ui-rendered-narrow` (390px dark; fake origin via route interception, /support-matrix fulfilled from the adapter's `_UI_MATRIX`, `window.fetch` stubbed to record and reject — no server, no network, no run, and no context of its own: the fast suite's first M35 gate runs measured 60.08-60.73s against the 60s ceiling, so the case was folded onto the existing render the way M30 folded its assertions) and pins: (a) that task with an empty URL field does not POST, shows `#err` guidance and leaves `#go` enabled; (b) a task naming a site fills `#url` from it and then does POST /tasks; (c) the cards rendered from a stubbed /support-matrix payload are the only example surface (owner amendment 2026-08-22): the fixture row does not render, every real-site row renders one card with exactly one `[data-example]` Try button, clicking it fills `#task` and `#url` with its EXAMPLES entry and POSTs exactly that pair, and an example's `note` renders on its card; the known-limits link carries the payload's declared count. Watched red against the pre-M35 page (main `2a11142`): `no_url` POSTed `{task, url: null}` with `#err` hidden, `site` POSTed `url: null`, and no `[data-example]` chip existed. Amendment watched red against the e07ac07 page: the fixture row rendered as a card (4 cards for 3 expected), `wikipedia.org` had no Try button, and the top `#examples` row still existed (3 chips outside any card).",
  "triage": {
    "class": "ux-guard",
    "note": "The API keeps accepting url=null (gateway cases pin it); this is the form refusing to spend a run on a task shape the planner cannot plan, and the example prompts being reproducible."
  }
}
