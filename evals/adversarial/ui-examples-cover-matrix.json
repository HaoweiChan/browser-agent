{
  "id": "ui-examples-cover-matrix",
  "task": "browser",
  "suites": [
    "fast"
  ],
  "input": {
    "kind": "invariant",
    "check": "examples-cover-matrix"
  },
  "expect": {},
  "provenance": "PR #32 R1 established that each displayed example must resolve to a support-matrix row. User report, 2026-08-30, narrowed the interview surface to finance; requiring equality with every real-site row would force irrelevant book and news examples back into the UI. The check enforces three integrity boundaries: every finance showcase is backed by the evidence matrix, all eight displayed statuses remain aligned with recorded evidence, and duplicate matrix rows are refused instead of silently taking the first. Cold review found the four new statuses and duplicate-row behavior unpinned; watched red on the Federal Reserve row, whose existing D28 failures require unreliable rather than not evaluated. T-R42's brace-aware parser probe remains: a mid-line object key must still be found.",
  "triage": {
    "class": "invariant-gap",
    "note": "The rendered case pins the selected finance workflows; this check prevents a showcase from losing its evidence row."
  }
}
