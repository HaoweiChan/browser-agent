# Support matrix — Task 1

**Report-assisted, human-declared** (see `docs/evals/evaluation-methodology.md`):
the latest `full`-suite eval report suggests a status; a human declares the
final status with a reason; README and the frontend render this same data.
A pass-rate does not threshold itself into "supported" — declaring is an
engineering-judgment act, and the reason column is the graded honesty evidence.

Entry shape (also served as JSON to the frontend):

```json
{
  "domain": "wikipedia.org",
  "task_type": "TC2 search-then-extract",
  "eval": "7/8",
  "declared_status": "unreliable",
  "reason": "Disambiguation pages still misroute the extraction step (case tc2-wiki-004)."
}
```

## Current matrix

Declared from the M6 baseline (`evals/report/20260817-124241-fast.json`, 65/65)
plus the M6 live runs; the M8 rows (quotes.toscrape.com, and limitations D5–D11)
rest on the M8 runs instead — `fast` 86/86 and `live` 9/9, 2026-08-20. Every
status **except the four M40 ones** rests on **stubbed plans** — the fixture rows
on offline fixtures, the live rows on hand-written plans against real DOMs — so
for those rows this table measures the resolver/executor/verifier path and says
nothing about planning quality on any domain. The four M40 rows
(companiesmarketcap.com, x-rates.com, multpl.com, and the changed
quotes.toscrape.com cell) are the exception and are marked as such below: they
were run end-to-end against the deployment with the real planner, so they do
measure planning quality on their domain, and they rest on no committed report
at all. Empty cells are shown, not hidden.

`books.toscrape.com` TC4 stays `—` rather than becoming a status: the case that
would fill it (`live-books-cheapest-travel`) is the only one needing a real
planner, and it has not been run.

`quotes.toscrape.com` is the M8 hostile domain and its TC1 cell was `unsupported`
on the strength of `live-quotes-js-role-tier-blind`: the run answers, reports
success, and answers with the pager. The page is not unreadable — the text tier
reaches its content exactly (`live-quotes-js-text-tier-reaches`) — it is
*unplannable*, because the observation the planner is given contains none of it.
D7 below has the measurements, and D27 records that M32's drill-down was measured
against this same page and does not change the verdict. M40 moves the cell to
`unreliable`, not to `supported`: the cited case still fails exactly as it did,
M32's drill-down still does not rescue it, and the domain's static author pages
now answer 3/3 end-to-end against the deployment (run_ids b973e350, 93085a40,
14833919 — "When was this author born?" on /author/Albert-Einstein/, all three
"Born: March 14, 1879 in Ulm, Germany"). One page shape works and one does not,
which is what `unreliable` means.

**Update (T-M40-5, 2026-08-24, build `8183dc2`).** The author-page evidence
above no longer holds on the current build: re-probed 3 times, it answered 0/3
(`c20b1fda`, `37fe5cec`, `2f12cf5e`) — a new label-without-value shape, not the
site-title shape this section already names. The cell stays `unreliable`
rather than moving to `unsupported` only because the underlying case
(`live-quotes-js-role-tier-blind`) and its `unreliable` declaration were never
actually resting on the author page — that page was cited context, not the
graded evidence — but "one page shape works" is no longer an accurate
description of this domain on this build. See `docs/analysis.md` §8a-4 and
D28's third paragraph (`docs/support-matrix.md`, same file, above) for the
full per-domain breakdown.

**Update (T-M40-5 round 2, 2026-08-24, build `c83febb`).** Re-probed a
second time, on a later `main` commit than the round above (deploy-smoke
run `32689266803`, provenance verified unchanged before/after, zero
contamination). The regressed set moved from 2/12 to **6/12 = 50.0%
correct — exactly at ADR-025's pre-registered ≥50% bar**, per-domain:
x-rates.com 1/3 → 3/3 (`19ae36c1`, `5ff8194f`, `eae1eb65`), multpl.com
1/3 → 2/3 (`026e10cb`, `bcdf4d38` correct; `46e9eb35` `failure:extract`),
quotes.toscrape.com's author page 0/3 → 1/3 (`4d0d3142` correct;
`480d71a4`, `f8945477` still the label-without-value shape T-M40-5-2
files), openlibrary.org unchanged 0/3 → 0/3 (`eaa16859`, `12fb368f`,
`53722c1a`, identical `"extraction returned empty text"` every time, both
rounds). Controls: companiesmarketcap.com 2/3 → 3/3, bankofcanada.ca
3/3 → 3/3. Zero wrong-success held across both rounds (36/36 clean).
**This round is explicitly NOT evidence for or against M38** (PR #42's
resolver narrowing) — 0/18 round-2 runs fired any M38 narrowing rung; the
recovery traces to the replan path recovering mid-run where it didn't in
round 1 (x-rates run 1, quotes run 9), and model-side variance between
builds cannot be excluded. Full write-up `docs/analysis.md` §8a-4 Round 2;
raw evidence `evals/report/20260824-042156-t-m40-5-probe-round2.json`.

The three M40 rows were re-declared once, and the reason is the most useful thing
this milestone found. They were first declared from 43 runs against the build
deployed before PR #34 (M32) — companiesmarketcap.com, x-rates.com and
multpl.com. When that build was replaced, a re-probe of the same tasks answered
0/3 on x-rates.com and 0/2 on multpl.com, so those two rows were withdrawn
rather than shipped, and bankofcanada.ca and ecb.europa.eu were probed and
declared in their place. D28 has both probes and the failure shape they share.
The rule this produced is worth stating plainly: **a row declared from live runs
is a claim about one deployed build, and it expires when the build does.**

**The pre-merge re-run, 2026-08-23.** ADR-022 Decision 1a requires every card and
every live-declared row to be re-run against the build being shipped, immediately
before merge. That pass found two more: `wikipedia.org`'s motto task failed
(7547e580) after passing earlier, and `quotes.toscrape.com` was 1/6 across both
of the tasks this milestone had put on its card. Neither was caught by anything
but the re-run. Final per-card counts on the shipped build — `books.toscrape.com`
3/3, `news.ycombinator.com` 3/3, `companiesmarketcap.com` 9/9,
`bankofcanada.ca` 4/4, `ecb.europa.eu` 4/5, `wikipedia.org` 3/4,
`quotes.toscrape.com` 3/3 on its third task and 1/6 on the two it replaced,
`openlibrary.org` 0/3 and deliberately so. Three of the eight cards therefore
changed task or status in this one pass, which is the rate at which this kind of
evidence goes stale.

The M40 rows are declared by a different method than every row above them, and
the difference is stated rather than blended in. Every other row was declared
report-assisted, from a committed `fast`/`live` report plus hand-written plans;
the M40 rows rest on ad-hoc submissions to the deployment with the real planner,
which produce no committed report and have no eval case behind them. That is
weaker in one way — nothing in `evals/` re-checks them, and no CI run can
reproduce them — and stronger in another: they are the only rows here that
measure the planner. Both halves are why D28 carries every run id rather than a
fraction, and why two of the three rows are `unreliable`.

| Domain | TC1 | TC2 | TC3 | TC4 | TC5 |
|--------|-----|-----|-----|-----|-----|
| shop fixture | supported | supported | supported | supported | — |
| forms fixture | — | — | — | — | supported |
| hello fixture | supported | — | — | — | — |
| books.toscrape.com (live) | — | — | unreliable | — | — |
| news.ycombinator.com (live) | unreliable | — | — | — | — |
| openlibrary.org (live) | unreliable | unsupported | — | — | — |
| quotes.toscrape.com (live) | unreliable | — | — | — | — |
| lamp-spec fixture | supported | — | — | — | — |
| wikipedia.org | unreliable | — | — | — | — |
| companiesmarketcap.com (live) | supported | — | — | — | — |
| bankofcanada.ca (live) | supported | — | — | — | — |
| ecb.europa.eu (live) | unreliable | — | — | — | — |
| whaleforce-sec10k.zeabur.app (live) | unreliable | — | — | — | — |

Statuses: `supported` / `unreliable` / `unsupported` / `—` (not yet evaluated).
Unsupported and unreliable rows must cite concrete failing evidence: a case id
where one exists, and otherwise — for the M40 live-declared rows only — the run
ids of the failures themselves, in D28. **Amended by M41 (2026-08-26), because
the inspector row is a third shape the sentence did not cover and a cold audit
caught it being used as if it did**: that row HAS cases, seven of them, and they
all pass — what fails is the deployment, so its failing evidence is run ids
(`e996cc7d`, `79c8dc32`) exactly like an M40 row, while its passing cases carry
the page-shape claims underneath. The rule is therefore: failing evidence is a
case id where a case can hold the failure, and run ids where the failure is a
run and no case can be. Widened here rather than quietly stretched, and the
weakness is the same one the next paragraph already names — a run id is a
receipt for something that happened once and cannot be replayed. The distinction matters and is not a
loosening dressed up as a rule: a case id is re-run by the gate on every commit,
a run id is a receipt for something that happened once and cannot be replayed
(`RUNS` is in-memory, D19). So `x-rates.com` and `multpl.com` carry the weaker
form of evidence, deliberately and visibly, until a case exists for either. No
check enforces this sentence — `support-matrix-cites-real-cases` verifies that
backticked case ids RESOLVE, and cannot tell which row a citation belongs to
(its own provenance says so). It is a rule this file keeps by hand.

## Chinese-language (zh) evidence — declared from live runs, not from any eval case (the ADR-022 exception)

No cell in this
table means "in English". Every status above rests on English task text, because
until 2026-08-25 every live probe this repo had ever run was English and every zh
case in the suites was an offline fixture with a stubbed plan — a fact nobody had
written down until an interviewer reported "中文都會失敗". The M45 probe puts
Chinese evidence behind exactly **four** of these rows, and behind no others:

| Row | zh task (ADR-031 frozen text) | zh result | Paired en result, same URL, same build, interleaved |
|---|---|---|---|
| shop fixture (TC2-shaped ask) | `在商品目錄搜尋 'Cobalt'，並回報找到的商品價格。` | **3/3** — `6bf8b235`, `df36ae83`, `643423c1` | 1/3 — `c299c98e` correct, `bdf2074c` / `b0206bff` `failure:locate` |
| books.toscrape.com (TC1-shaped ask; the matrix cell itself is `—`) | `這本書的價格是多少？` | **3/3** — `86417671`, `3a724f55`, `f5f3019b` → `£51.77` | 3/3 — `a315597c`, `2eed8f78`, `6a9fed4d` |
| companiesmarketcap.com (TC1) | `這家公司的市值是多少？` | **3/3** — `a89ed7e9`, `3166af48`, `706540fe` | 3/3 — `70785aab`, `e5237abc`, `c9d4529f` |
| bankofcanada.ca (TC1) | `目前的政策利率是多少？` | **3/3** — `a3fcbc62`, `eedacfa0`, `a88b7860` → `2.25` | 2/3 — `350569ac`, `7ef9f83f` correct, `383b69c6` `failure:locate` |

Chinese 12/12, English 9/12, zero wrong-success on either side. **The statuses in
the table above are not raised on the strength of this** — a status is declared
from the whole body of evidence for a row, and three zh reps do not promote
`books.toscrape.com`'s TC3 cell or anything else. What these four rows license is
narrower and is the only claim made: on these four task shapes, on this build,
Chinese is not the failing variable. Every other row in this table has **no zh
evidence at all** and should be read as English-only. Declared limitation: D30.
This is the ADR-022 live-declaration form — run ids, repeat counts, and a named
limitation, with no eval case behind any of it and nothing in `evals/`
re-checking it — and it expires when the build does (ADR-022 Decision 1a).
The refusal direction was re-confirmed live in the same probe and belongs in the
same record: `ab08cbd5` (`幫我刪除購物車裡的所有商品，然後把帳號也刪掉。`) and
`cb689bff` (`幫我登入然後看訊息`) both terminated `unsupported` at $0.00 with
`trace_len: 0`.

**One ADR-022 obligation is NOT met by this section, and saying so is cheaper
than the alternative.** Decision 1a requires every live-declared row to be
re-run against the build being shipped, immediately before merge. These four
rows were measured against `main@9c3340c`; merging this PR moves `main`, and the
deployment follows `main`, so the shipping build is a different build from the
measured one — not because its behaviour differs (M45 ships no production code
change at all; `SCOPE_BLOCK` was byte-for-byte what it was, and stayed so until
M45-D6's fold on 2026-08-28) but because 1a's rule
is about the BUILD, and it is written that way for a reason: the two rows it was
created for were invalidated by a build change nobody expected to matter. No
re-probe is possible before merge because the deployment only moves when `main`
does. That is the same structural gap ADR-025 hit, and it is handled the
same way: the re-probe is its own tracked task (`tasks/TODO.md` Debt M45-D5),
not a claim quietly made without it. The substantive risk is low and is stated
rather than assumed — this PR changes no production code, and none of the four
Group A tasks contains any `SCOPE_BLOCK` term in any case — but "low risk" is
not the same as "re-run", and only the re-run discharges 1a.

## Declared limitations (M45 Chinese-language support)

| Limitation | Evidence | Status |
|---|---|---|
| **D30** — the Chinese evidence behind this entire table is four task shapes, three repetitions each, one build, one evening; and the headline claim it was commissioned to test turned out to be mostly wrong, which is the more useful half. Reported by an interviewer on 2026-08-26 as "中文都會失敗" — every Chinese task fails, refused before the browser opens — with no run ids attached | The repo's first live probe in Chinese, pre-registered in `specs/decisions/ADR-031-m45-preregistered-zh-probe.md` before any run executed: 29 runs against the build deployed from `main@9c3340c` (deploy-smoke run `32870815721`), 2026-08-25T17:01Z, $0.011195 (planner $0.009783 + judge $0.001412), provenance verified unchanged before and after, every run_id published in `docs/analysis.md` §8a-5 and `evals/report/20260826-011010-m45-zh-probe.json`. Design: each task run **paired**, Chinese and English, same URL, same build, interleaved per repetition, so "Chinese fails" could be separated from "this task fails". Result: **Chinese 12/12 correct, English 9/12, wrong-success 0/29.** Zero of the twelve Chinese runs was a refusal; every one opened a browser. All three failures in the probe are English `failure:locate`, the D29 resolver-ambiguity shape | `supported` **on these four shapes and nothing wider** — and the width is the limitation, not a footnote to it. Four shapes is not a language. There is no zh evidence for any other row in the matrix, none for mixed-script tasks, and none for simplified-script input beyond two screening rows. Nothing in `evals/` re-checks any of it (ADR-022's declared concession), and it expires when the build does (ADR-022 Decision 1a). The offline zh cases (`tc1-shop-price-zh`, `tc2-shop-search-zh`, `tc3-shop-detail-nav-zh`, `tc4-shop-sort-zh`, `tc5-forms-submit-zh`) run on every commit but use stubbed plans, so they measure the resolver/executor/verifier path in Chinese and say nothing about planning in Chinese — which is precisely what this probe measured and they cannot |
| **D31** — the scope screen over-refuses in Chinese on named shapes, and M45 did not fix it: three narrowings were written and all three were falsified, so no NARROWING ever shipped. `SCOPE_BLOCK` stayed byte-for-byte as it was until 2026-08-28, when M45-D6 folded every trad/simp pair to a character class — a widening, which moves this row's over-refusals in the *more* direction rather than repairing them (see the Status cell) | The one part of "中文都會失敗" that reproduced. CJK terms match as bare substrings — Python `re` has no `\b` inside a CJK run — so a blocked term fires inside a longer word meaning something else. Three demonstrated live at $0.00 with an empty trace: 密碼 inside 密碼學 (cryptography, run `8304ee3b`), 購買 inside 購買力平價 (purchasing power parity, run `be20ba6a` — a read task in this repo's own declared target domain), 刪除 inside 刪除的檔案 (*deleted* files, run `038bc371`). Each attempted repair was a per-term negative lookahead and each was falsified by an ordinary Chinese sentence it un-refused: `[刪删]除(?!的)` allowed 把購物車裡要刪除的商品都刪掉; `[購购][買买](?!力平[價价])` allowed 我要購買力平價這本書 ("buy the book *Purchasing Power Parity*" — on a bookstore, a live domain in this table) and 请帮我购买力平价指数基金; `密[碼码](?![學学])` allowed 幫我重設密碼學生帳號 (學生帳號 = student account). All three shapes and all six counterexamples are pinned in `screening-zh-term-inside-another-word`, each watched red before the attempt it killed | `unsupported` — the over-refusal is **declared, not fixed**, and the reason is a property of the problem rather than a shortfall of effort: to a regex, the continuation that makes a term part of a different word is indistinguishable from the first character of the object a real request acts on. Every narrowing therefore buys a correct refusal back at the price of a false ALLOW in a security-adjacent screen, so the screen fails CLOSED and the refusals stand. **Still refused, and each is a real cost:** 密碼學 (cryptography), 購買力平價 (purchasing power parity), 刪除的檔案 (deleted files), 美元的購買力 (purchasing power), 密碼子 (codon), 購買量 (purchase volume), 刪除按鈕 (delete button — whose English counterpart `l5-refuse-delete-determiners` pins as unblocked). **Shared with English, not a Chinese defect:** 下載 and 付款 refuse on a bare mention, and so do `download` and `payment` — `screening-word-boundary` pins "What are the download statistics shown on the page?" as blocked. What might actually work is a rule about the REQUEST FRAME rather than the term's neighbours — every false positive here is a question about a page, every false negative an imperative — which is unprobed in either language and filed as `tasks/TODO.md` M45-D8. **Widened 2026-08-28 (M45-D6, and half of M45-D3):** every traditional/simplified pair is now a character CLASS rather than two spellings, because a spelling that mixed the two scripts inside one word matched neither alternative and sailed through — `screen('幫我輸入验证碼')`, `screen('幫我购買這個商品')`, `screen('購买這個商品')` and `screen('幫我輸入驗证码')` all returned None, and input methods, zh-TW/zh-CN copy-paste and OCR all produce that shape. Nine spellings moved from allow to refuse (登錄, 購买, 购買 and the six mixed forms of 驗證碼); only 驗證碼 and 購買 have more than one variant character and therefore a mixed form at all. This is a deliberate widening of a screen that fails CLOSED, not a neutral refactor, and every one of the nine is a row watched red first. The over-refusals listed above widen with it by construction — 购買力平價 now refuses exactly as 購買力平價 always did, and 登錄資料 (*to record* data) joins the list. **Still uncovered, deliberately:** 登陸 and 登陆, ordinary and in the mainland dominant spellings of "log in", because they also mean *to make landfall* — 颱風什麼時候登陸？ and 登陸月球 are read tasks, so a verbatim add would manufacture a fresh instance of this very row. All three readings are pinned in the ALLOW direction in `screening-zh-term-inside-another-word`, so a later careless widening turns red instead of shipping false refusals. M45-D3 therefore closes at three of five spellings, and the remaining two wait on M45-D8's request-frame rule |
| **D32** — the `observe` -> `resolve` round trip is enforced for NAMES and not for `near` ANCHORS, and not for roles at all | T-M42-20 found that the two ends compute an accessible name with different engines (Chromium's snapshot applies the page's CSS text-transform, Playwright's locator engine does not) and closed it for the name tiers; PR #60 R5 found the same asymmetry in `near` and closed the anchor's exact pass (`resolver-near-anchor-is-case-insensitive`). What is GRADED generically is names only: `observe-uppercase-label-name-resolves` feeds every advertised `{role, name}` back through `resolve` and requires it to reach as many elements as the observation claims. Nothing feeds an advertised string back as a `near` anchor, and nothing checks the ROLE half at all — which is how the file input on our own inspector stayed invisible: it is role `button` to Chromium's snapshot and role `textbox` to Playwright, so `{'role':'button','name':'UPLOAD A FILING (.HTM / .HTML / .TXT)'}` resolves at no tier and no casing fixes it. It is excluded by name from that case, with `WebArea` (the document root, not an addressable role) | **declared, partly unfixed.** The name half is enforced on one fixture (`T-M42-20-D1`); the anchor half is fixed in the matcher but ungraded as a round trip; the role half is neither (`T-M42-20-D4`). A page whose observation advertises a role the locator engine computes differently will fail `locate` on a target the planner had every reason to write, and the only signal is the loud failure |
**The inspector row is a MODE-B row, and the column it does not have is named.**
`whaleforce-sec10k.zeabur.app` is Task 2's own deployed inspector — the one
external site this project also controls — and it is here because the
2026-08-24 demo failed against it in front of an audience
(`docs/evals/2026-08-24-demo-sec10k-inspector-postmortem.md`). Its `unreliable`
is decided by a rule frozen before the runs existed (ADR-030 §Thresholds: 6/6
`supported`, 3-5 `unreliable`, 0-2 `unsupported`), and the probe returned 4/6.
It measures **mode B** — plan once, then execute — because that is the only
mode that exists on the deployed build. Loop mode (ADR-027, M42) is unprobed
here, in either direction, and **M44 owns folding a loop-mode column into this
row**; the mode label is ADR-030's own
ruling, not an older one — ADR-022 gives the live-declaration rule this builds
on and says nothing about execution modes, so nothing before this milestone
required a row to name one. D30 below has every run id, both build
shas, and what is not claimed.

It is also the first row here declared BOTH ways at once, which is why it is
stronger than the M40 rows above it and is worth saying rather than leaving to
be inferred. It is **report-assisted** in this file's original sense — a
committed `live` report, `evals/report/20260826-080559-live.json` (11/11,
$0.0000), with hand-written plans behind
`live-sec10k-authored-wait-reaches-the-doc-status` and its item-count twin, plus
six offline cases in the `fast` gate — five that re-check the page shape on
every commit and one, `sec10k-ground-truth-endpoint-eval-only`, that re-checks
the ground-truth boundary in `invariant` too — AND it carries the M40-style end-to-end evidence that measures the
planner, six runs against the deployment with the real planner. The M40 rows have
only the second half and D28 says so; this row has both, and the two halves
disagree in a useful way: the hand-written plan answers 2/2 because it authors
the wait, and the real planner answers 4/6 because it does not always.


## Declared limitations (M40 investment-domain probe)

| Limitation | Evidence | Status |
|---|---|---|
| **D28** — asked to cover the domains an investment reviewer actually uses, the agent covers exactly one shape of finance page and fails on the others; and the rows that say so expire when the deployed build changes, which is the second and larger finding. Two probes, both against the deployment, both on 2026-08-23: **43 runs across 22 finance domains** on the build deployed BEFORE PR #34 (M32), then **33 runs across 17 domains** on the build after it. T-M40-5 (ADR-025) extends this same entry with a third, pre-registered probe: 18 runs (6 tasks × 3 reps) against the build that ships T-M40-2's plan-lint refusal, `8183dc2` (deploy-smoke run `32683725839`), 2026-08-24 — see `docs/analysis.md` §8a-4 for the full write-up and every run_id. | **What answers, and why.** The value has to sit in an element of its own — for companiesmarketcap.com it is literally the accessible NAME of a heading (`extract {role: heading, name: "Market cap: $4.514 Trillion USD"}`), so no plan ever needs a container. That domain answered 7/7 pre-M32 and **8/8 post-M32** (d0b63c7e, f2c8c624, 65bb1028, 03eedb79, 2a058974, 4cec8304, 215e511a, f8925a42, across six pages). bankofcanada.ca answered 3/3 post-M32 (e36edcc1, 93fc8e6f, 5125b503 → "2.25"). ecb.europa.eu answered 2/3 (c8f04424, 9597fab9 → "2.25"; 35c4e211 rejected by the judge, which read the historical table instead of the current rate — a correct rejection). **What the build change did.** x-rates.com answered 3/3 pre-M32 (570f4c04, 0909909e, d9ad84b7 → "1.168361 USD") and **0/3 after** (b8b95067, 133264ee, 81155e22). multpl.com answered 3/6 pre-M32 (97912676, 434335eb, 3ec2b4d5) and **0/2 after** (bdc38f65, c7fa2623). quotes.toscrape.com's author page answered 3/3 pre-M32 and 0/1 after (6811f8bf — it extracted the site title "Quotes to Scrape" and the M36 judge rejected it). openlibrary.org failed in both. The post-M32 failures share ONE shape that none of the pre-M32 shapes covers: the planner emits `extract {role: WebArea, name: "<the page title>"}` — the document root, named by `<title>` — and the relocation rung retargets as `{text: "<the page title>"}` and resolves nothing. That is logged as T-M40-2, with what is NOT claimed written into the block: this is one task phrasing per page, the deployment moved model-side as well as code-side, and two pre-M32 runs (8c1a3344 stooq, c80b1dd0 coingecko) already showed WebArea targets. **The pre-merge confirmation pass.** All eight cards re-run once against the shipped build, then the two failures re-probed: wikipedia.org 3/4 (4bdbd12f, f31a05c8, a51a772f; 7547e580 failed at locate) — declared `unreliable` on that, its first status after sitting at `—` since M6. quotes.toscrape.com 1/6 on the two author-name tasks (63d8a48f the only pass; 7d752e64, 6811f8bf, f7dd7f52, 7d05d5e2, 79572b33), all of them the resolver-ambiguity shape main's M38 is queued to fix — "3 matches at tier text" for a name that appears three times, or the post-M32 WebArea/site-title shape on the author page. Its card moved to a third task, "Which tag is listed first under Top Ten tags?", 3/3 (51bc4a0a, eb5ed750, b131ac78), which asks for a value that is unique on the page. Two further one-run probes failed and are named rather than dropped: 2e1a35f5 (`role: blockquote` resolves to nothing) and a8fe1b01 (answered "Description:", a page label, rejected by the judge). **The pre-M32 failure taxonomy**, kept because it is what those 30 failures were: (1) CONTAINER DUMP, 8 runs — `main` or a whole table extracted and the page returned: stockanalysis.com (ac1dd2f1, f17afbee, 9f624ba5), federalreserve.gov (993aede7), cnbc.com (ab3225a7), newyorkfed.org (68de8ff1), finance.yahoo.com (29279c32), multpl.com (e0b4a2e3) — every one caught by the verifier as `failure:semantic`, never a wrong answer, which is `extract-container-dump-is-not-the-answer` live. (2) NOTHING RESOLVES, 18 runs: coingecko.com (c80b1dd0), coinmarketcap.com (c57de7ef), stooq.com (8c1a3344), slickcharts.com (9d2d83f3, 7977cb1f), marketbeat.com (ef4d37c8), sec.gov EDGAR (7a5fed48, 17184671, 6c29c944), fred.stlouisfed.org (58699716), macrotrends.net (31bfb2d7), investor.gov (fb13acb4), berkshirehathaway.com (b5f84cde), 8marketcap.com (96d27bc6), google.com/finance (e492553a), tradingeconomics.com (3c1cec38), multpl.com (a9d565b2, 602d70be). (3) INVENTED ANCHOR, 2 runs: tradingeconomics.com (8e9470c3), x-rates.com USD→JPY (0f8e532f). (4) two one-offs: federalreserve.gov's 088e7962 hit the M31 aggregate guard on a rate table, and finviz.com's cb86c0d7 answered with the LABEL "PE Ratio" and the M36 judge rejected it — "merely repeats the label ... without supplying the required numeric value (35.49)" — the first live evidence for the grading quality D25 declared unverified. 8 + 18 + 2 + 2 = 30. **The T-M40-5 post-fix probe (build `8183dc2`, 2026-08-24, ADR-025).** Regressed set, 2/12 correct (16.7%, vs the 0/7 this row already records post-M32): x-rates.com 1/3 (`591cf2dc`; `110e9e8f`, `48b60ee3` now hit a NEW shape — ADR-024's lint fires and replans, but the replanned step dies on "identity anchor 'EUR to USD' absent from the page the answer was read from" even though the correct value (1.168062/1.168190 USD) was sitting in the extraction evidence both times, e.g. `110e9e8f`). multpl.com 1/3 (`df42fa8f`; `86935b56`, `dbd3e3ca` fail:extract, empty answer after the Table click). quotes.toscrape.com's author page 0/3 (`c20b1fda`, `37fe5cec`, `2f12cf5e`) — a DIFFERENT new shape from the site-title extraction this row already records (`6811f8bf`): the extractor now lands on the adjacent label instead of the value ("Description:" or bare "Born:"), judge correctly rejects all three. openlibrary.org 0/3 (`bdd9ebf7`, `b9e6ddd9`, `b43acc40`), identical "extraction returned empty text" at step 2 every time. Controls: companiesmarketcap.com 2/3 (`a93832c7`, `98d9f1c7`; the miss, `97677d75`, is a judge crash — "AttributeError: 'NoneType' object has no attribute 'strip'" — on a CORRECT extraction, "Market cap: $4.514 Trillion USD"; fail-closed held, the crash is a bug, not a wrong answer). bankofcanada.ca 3/3 (`ff4f793a`, `4ea9ee33`, `6c247c24`). Zero wrong-success across all 18 runs — the first probe round with that property. Full per-task tally, ground-truth re-verification and the four pre-registered threshold verdicts are in `docs/analysis.md` §8a-4; raw evidence committed at `evals/report/`. **The T-M40-5 round-2 probe (build `c83febb`, 2026-08-24, ADR-025 Round 2).** Regressed set improved to 6/12 = 50.0% — exactly at the pre-registered bar, not comfortably above it: x-rates.com 1/3→3/3 (`19ae36c1`, `5ff8194f`, `eae1eb65`), multpl.com 1/3→2/3 (`026e10cb`, `bcdf4d38` correct; `46e9eb35` fail:extract), quotes.toscrape.com's author page 0/3→1/3 (`4d0d3142` correct; `480d71a4`, `f8945477` the same label-without-value shape round 2's other quotes runs already show), openlibrary.org unchanged 0/3→0/3 (`eaa16859`, `12fb368f`, `53722c1a`, identical "extraction returned empty text" every time in both rounds). Controls: companiesmarketcap.com 2/3→3/3 (`c4653dbe`, `6580fc85`, `584936a3`), bankofcanada.ca 3/3→3/3 (`f18bb1c6`, `6544eb51`, `6ecdf5b8`). Zero wrong-success held across both rounds (36/36 clean). Two findings this round adds on top of the threshold numbers: (i) **0/18 round-2 runs fired any M38 narrowing rung** — this probe is NOT evidence for or against M38 (PR #42's resolver narrowing), and the round-1→round-2 recovery traces instead to the replan path recovering mid-run where it previously died (x-rates run 1 `19ae36c1`, quotes run 9 `4d0d3142`, both `replans: 1`), with model-side variance between builds not excluded; (ii) **rep-level nondeterminism within round 2 itself** — multpl.com and quotes-author each disagree across their own three reps on the identical build/task/URL (multpl 2/3 vs 1/3 fail:extract; quotes-author 1/3 vs 2/3 fail:semantic), so a single rep is not a reliable read of either task's true rate. Full write-up `docs/analysis.md` §8a-4 Round 2; raw evidence `evals/report/20260824-042156-t-m40-5-probe-round2.json`. | `unsupported` for the shapes that dominate real finance sites: a value inside a dense table or grid, any page whose numbers are painted by script after load, and — post-M32 — any page whose answer is not already the accessible name of a small element, since the planner reaches for the document root instead. 19 of 22 domains never answered once pre-M32; 12 of 17 never answered post-M32. **And `unsupported` for the durability of every row in this section**: they are claims about one deployed build. Two of the three first declared here were withdrawn before merge because the build moved under them, and nothing detected that — no gate, no case, no CI job re-runs any of it. It was caught by hand, once, because a merge conflict prompted a re-probe. What is NOT claimed, beyond that: (a) that these runs measure DOMAINS rather than these tasks on these pages — one phrasing per page, one to three repeats, no held-out set; (b) that the answering domains are stable — ecb.europa.eu is `unreliable` for exactly that reason and companiesmarketcap.com's 15/15 across two builds is the only row here with a real repeat count behind it; (c) that the failing domains are unreachable in principle, each failed on one phrasing of one task; (d) that M32 CAUSED the regression — T-M40-2 states the confounds T-M40-5's pre-registered verdicts: round 1 on `8183dc2` (a) PASS — zero wrong-success (HARD gate); (b) FAIL — regressed set 2/12 = 16.7%, required ≥50%, **the fix is insufficient**; (c) PASS — both controls within one miss of their prior post-M32 rate; (d) 0 refusals. Round 2 on `c83febb`, same thresholds: (a) PASS — 0/18 (36/36 across both rounds); (b) PASS — 6/12 = 50.0%, **exactly at the bar**, not comfortably above it; (c) PASS — both controls 3/3; (d) 0 refusals. Net: `unsupported` still holds for openlibrary.org (0/3 unchanged across both rounds) on this build; x-rates.com and multpl.com move off the round-1 `unsupported` floor but round 2's per-rep nondeterminism (multpl 2/3, quotes-author 1/3, each disagreeing with its own other reps) and the fact that neither round exercised M38 at all mean this is declared as evidence of an exactly-at-threshold, unstable recovery, not a clean `supported`; quotes.toscrape.com's author page can no longer be cited as the working page-shape behind that domain's `unreliable` declaration — see the note added below and ADR-025's Outcome section. |

## Declared limitations (M38 resolver narrowing)

| Limitation | Evidence | Status |
|---|---|---|
| **D29** — a target that resolves to several elements is narrowed by the page instead of failing the run, in three shapes and no others; the test that decides "these matches are interchangeable" reads an element's `role` attribute falling back to its tag name, not the computed ARIA role, and the test that decides "this task asks for one thing" is a regex over natural language that misses plural asks in both directions | M38 (ADR-026) adds three rungs to `resolve()`, tried only after every tier has had its chance at a unique match, and only for a READING step whose task asks for one thing — all three, including the loosened anchor passes, which sit in the `near` branch and were ungated for one round (PR #42 R7): the step's identity `anchor` reused as a proximity anchor (`resolver-narrows-by-anchor-proximity`), the first match in document order when the matches are interchangeable (`resolver-narrows-identical-matches`), and a `near` anchor matched through typographic quote/dash variants and then by its first 40 characters (`resolver-near-normalises-typography`), that last one gated by `resolver-refuses-plural-on-a-loose-anchor` and `resolver-refuses-a-click-on-a-loose-anchor`. What is NOT gated, deliberately: `near`'s own exact and substring matching, which M6 shipped and which stays available to any step — an anchor the page actually contains is the proximity the plan asked for, and Playwright normalises whitespace inside those two passes itself. The rung is named in the trace step's `note` and wears no `retry_or_recovery` label. Every conjunct of the guard has a case that is red when and only when that conjunct is removed, measured by ablating each one over the whole suite (PR #42 R2/R3 found three of the original five cases passing for reasons unrelated to the guard they claimed to pin): the reading-step conjunct by `resolver-refuses-narrowing-a-click` (two buttons, one accessible name, the same reading, different effects — widen `READS` and the run presses the first), the singular-task conjunct by `resolver-refuses-plural-with-anchor` and `resolver-refuses-plural-wording` (one per rung, since the test gates both) plus one case per phrasing, the role half by `resolver-refuses-mixed-roles` and the text half by `resolver-refuses-different-readings` (same role, different readings, no usable anchor, and the first match in document order is the WRONG user). `l4-shop-duplicate-labels` is NOT one of these pins — three documents claimed it was, and PR #42 R2 falsified that — it keeps its own job, role+name uniqueness breaking and being rescued by relocation | `unsupported` for three named shapes, declared rather than guessed at. (1) **The role test is a tag test.** Two elements with the same tag and different computed ARIA roles — a scope-less `<th>` beside a `<td>` (the shape `near-excludes-its-own-anchor` was built on), an `<a>` with no `href` — read as interchangeable, so the document-order rung may pick between them where a computed-role probe would refuse. No fixture demonstrates it and none is invented here; the upgrade is a computed-role probe per candidate, one `evaluate` per match. (2) **A confidently-wrong identity anchor is answered, not refused.** Rung 1 fires whenever the anchor identifies exactly one place on the page, and an anchor that sits nearer the wrong candidate produces a confident wrong answer where the pre-M38 code produced a loud failure — the accepted risk of ADR-026, visible in the artifact because the trace names the rung, and caught by nothing else in a live run (the same hole D7 and the `trap-*` cases describe: only ground-truth L2 catches a focused wrong answer). (3) **The plural test is a regex over natural language and misses phrasings nobody has probed.** It covers quantifiers, `name/list/give/show (me) the <plural noun>s`, `who are` / `what are`, and the CJK markers 所有/全部/列出/哪些/每個/各個 — one case per phrasing, each watched red first (PR #42 R4). It does not parse grammar: a plural ask phrased outside those shapes (`Which users posted here?`, a plural carried only by an inflected noun, a language whose plural markers are not listed) reaches the rungs and is answered from one match, wrong by omission. Same ceiling and same widening rule as `agent.SCOPE_BLOCK` and `verifier._AGGREGATE` (D21): widen on a phrasing a probe found, never on synonyms nobody has seen. (4) **The plural test fires in both directions, and each has its own case.** The `name/list/give/show the <noun>s` branch is a spelling rule, not a grammar: it excludes `ss` before the final `s` and nothing else. That one exclusion is correct both ways — an English plural of a word ending in `ss` is `-sses`, so no plural carries `ss` immediately before its final s, which is why `the address`, `the business` and `the class` narrow (`resolver-narrows-singular-noun-ending-in-s`, red when the exclusion is removed). `u` and `i` were excluded beside it for one round and no such claim was true of them: `the status` and `the menus` both end in `us`, `the analysis` and `the taxis` both in `is`, so ordinary plurals — menus, taxis, skis, kiwis, gurus, alibis, bikinis, bayous — stopped being recognised and were answered from one match (`resolver-refuses-plural-menus`, red when the exclusion widens past `ss`). Separating those needs a lexicon, so the milestone keeps the OVER-firing deliberately: every singular noun ending in any other letter plus s — `the status`, `the genius`, `the analysis`, `the lens`, `the news` — reads as plural and refuses to narrow. That direction costs a loud `locate` failure; the other costs a confident wrong answer, and the asymmetry is chosen rather than split |

## Declared limitations (M32 observation drill-down)

| Limitation | Evidence | Status |
|---|---|---|
| **D27** — the drill-down reaches an answer that fell off the end of a capped observation, and it does that only where the planner can NAME a container the capped observation actually showed | M32 (ADR-020) adds one action, `observe`, whose target names a container from the observation; the executor re-runs the observation scoped to that subtree with the whole MAX_ELEMS budget spent inside it and a 1,500-character text head, and returns it through the same observation+note arguments a replan already uses, spending the existing MAX_REPLANS budget. Two refusal paths end the run loudly rather than falling through to the steps a plan queued behind the `observe` (`observe-refused-drilldown-stops-the-run`, `observe-drilldown-no-progress-stops-the-run`), an `expected_state` on an `observe` step is refused as `failure:task` (`observe-step-cannot-carry-expected-state`), and a drill into a landmark gets the page budget rather than the chrome sub-budget (`observe-drill-into-chrome-gets-the-page-budget`). Neither replan path will accept a plan that reads without acting while a failed action that changed nothing is outstanding — where "changed nothing" covers both an attempt that ran and moved nothing and one that never got far enough to be compared (`observe-cannot-launder-noop-action`, `observe-drilldown-cannot-launder-noop-action`, `observe-drilldown-cannot-launder-unchecked-action`), and an `observe` attempt neither wears the `recovery` label nor consumes a `superseded_by` pointer. Measured on the new fixture `deep-spec.html`, whose specification table falls past the cap in document order: the page-level observation is exactly 60 elements ending mid-table, the drill target sits at element 51 and the answer cell at element 77 of 77 (`observe-cap-hides-the-answer-element`). Without the drill-down the best plan that capped observation affords extracts the container and returns the whole nine-line table as the answer — `failure:semantic` on `not_a_dump`, with the answer literally ending in the value it failed to isolate (`observe-blind-plan-dumps-the-container`), which is M10 probe #4/#5/#7's page-dump shape reproduced offline. With it the same task and page return the value (`observe-drilldown-past-max-elems`), and that case grades the DISCLOSURE per planner call — the string absent from call 1's observation, present in call 2's — not the hand-written plan that follows it | `unsupported` for three named shapes, declared rather than guessed at (CLAUDE.md rule 2). (1) **A page that exposes no content roles is not reached and D7 is unchanged.** Measured against the live hostile domain on 2026-08-22 by hand probe — one page load, $0.00, no committed report behind the sweep itself, unlike the case results cited beside it: the page-level observation of quotes.toscrape.com/js is 11 elements, and re-observing every one of the ten resolvable containers among them — heading, three links, navigation, list, listitem, contentinfo, two footer links — disclosed zero quote content, with "Albert Einstein" absent from all ten scoped text heads and present in the 1,499-character body. `live-quotes-js-role-tier-blind` keeps its honest marker unchanged: still `success` with the pager text as the answer, still `answer_is_known_wrong`, and the `live` suite is 9/9 after this change. (2) If the drilled subtree is itself larger than MAX_ELEMS the same cliff recurs one level down; the drill-down is not recursive and no fixture demonstrates this. (3) WHEN to drill is a planning decision, and planning quality is unmeasured here as everywhere else in this repo — every plan in the suite is hand-written, so nothing measures a real planner choosing to ask |

## Declared limitations (M29 post-merge confirmation)

| Limitation | Evidence | Status |
|---|---|---|
| **D23** — the inviolable property (criterion 5) does not hold on the deployed build, on a task shape neither M10 fix touches, and the violation is intermittent | ADR-015's live re-confirmation (`docs/analysis.md` §8a-3), run against merged main (`788e8e9`): "tell me the price of the first book in the Travel category" on `books.toscrape.com` reported `status: success` / `verdict: PASS` with `answer: "Warning!"` in 3 of 4 post-merge runs (`run_id`s `d00d2be0`, `470a4ebe`, `2343e0b4`) and `answer: "Travel"` in the fourth (`5c574a44`), against a verified ground truth of £45.17. All nine verdict checks passed on every run, including `grounded` — "Warning!" is a real string on the page (`books.toscrape.com`'s own demo-site disclaimer banner, confirmed by `curl`: `<div class="alert alert-warning"><strong>Warning!</strong> This is a demo website for web scraping purposes...</div>`, present on every page), so groundedness cannot distinguish "on the page" from "answers the question." `aggregate_needs_comparison` (M10's fix) does not fire because this is a plain single-hop extraction, not a superlative/aggregate question — the exact shape M10's guard was scoped to and no wider. **The same task is nondeterministic**: its M10-round-2 twin (`run_id 9a21ed14`) answered correctly (£45.17), and M10 round 1 on the identical task failed loudly instead (`failure:locate`) — three different outcomes now observed on one task against one build across two probe cycles | `unsupported` — this is the M34 defect (`tasks/TODO.md`), demonstrated a third time and now on the deployed build specifically, not just offline. Nothing in this repo currently checks whether an answer is *responsive* to the question asked, only whether it is grounded and non-empty (same gap as the unnumbered "An answer is never checked for being responsive" row above, M7 section) — `grounded`/`not_a_dump`/`identity_anchors`/`answer_nonempty` all pass on a page-furniture string. Declared, not guessed at: fixing this is explicitly out of scope for M29 (record-correction only) and is M34's whole job, which CLAUDE.md rule 2 requires to land as a watched-red case before any fix |

## Declared limitations (M34 responsiveness fix)

| Limitation | Evidence | Status |
|---|---|---|
| **D24** — the M34 fix (`not_page_furniture`) catches page furniture (nav/banner chrome) and does not false-positive on an ordinary listing->detail repeat (a title or price shown once in a catalogue row and again on that item's own detail page), but three shapes remain uncaught | `verify()` (`src/browser/verifier.py`) fails an extraction when its LOCAL CONTEXT — the value plus `PAGE_CONTEXT_WINDOW` (20) raw characters either side of it, anchored to the actual DOM occurrence it was read from — is also verbatim on a DIFFERENT page the same run visited (`other_page_text`, tracked per-URL by agent.py's `page_bodies`). Comparing NEIGHBOURHOODS rather than the bare value is what lets one rule cover both non-numeric chrome and a listing->detail repeat: a repeated WIDGET (nav link, banner) is the same template fragment everywhere it recurs, neighbours included, while a coincidentally-repeated fact sits beside different neighbours each time — a catalogue row reads "Aurora Desk Lamp $39.00" (title beside its row's price), that product's own detail page reads "Aurora Desk Lamp $39.00 LAMP-STD Anodised aluminium..." (title beside its OWN SKU/Material), and the two windows diverge within a few characters. When the same value legitimately occurs more than once on ONE page (a decoy mention beside the real answer), `agent.py`'s `TEXT_OFFSET_JS`/`_closest_occurrence` anchor the window on the DOM-resolved occurrence via an approximate offset, not whichever occurrence a bare `find()` reaches first. Cases: `verifier-responsive-not-page-furniture` (chrome — a sidebar link with no landmark role, the shape books.toscrape.com's own sidebar actually is, confirmed live by `curl`: no `<nav>`, no `role=navigation`), `verifier-listing-detail-title-not-furniture` and `tc2-shop-search-zh`/`trap-near-miss-entity` (listing->detail title and price repeats), `verifier-context-anchors-real-occurrence` (same-page decoy plus real occurrence). The fixtures `nav-heavy-home.html`/`nav-heavy.html` share a 35-item category sidebar with each other, deliberately a representative subset of books.toscrape.com's own sidebar shape (many identical sibling entries surrounding the target link) rather than a claim of full parity with the real site's ~50 categories or their exact order — the fixture only needs to exercise "the same repeated widget recurs on both pages", not reproduce the real site byte-for-byte | `unsupported` for three named shapes, each declared rather than guessed at (CLAUDE.md rule 2): (1) a task whose run never leaves a single page has no second page to compare against — `other_page_text` is empty, the check is a silent no-op, same posture as `not_a_dump` below `MIN_PAGE_CHARS`. (2) `PAGE_CONTEXT_WINDOW=20` is swept only on the shapes the cases above demonstrate: furniture whose immediate neighbours genuinely vary page-to-page (personalized chrome, a nav that highlights the current item) could false-negative, and a listing/detail pair whose immediate neighbours happen to align by coincidence could still false-positive — neither is demonstrated by any case here. (3) `TEXT_OFFSET_JS` is an approximate DOM-offset heuristic (summed preceding-sibling text length up the ancestor chain, not a real innerText-equivalent computation) picking the CLOSEST occurrence of a repeated value to that hint — two occurrences close enough together, or a page structure the heuristic under- or over-counts badly enough, could still pick the wrong one; no case here demonstrates that failure. Separately, and not to be conflated with any ceiling above: **this fix has been demonstrated OFFLINE only** — the `fast` suite and the unmodified `live` suite (`live-books-travel-price` against the real books.toscrape.com Travel page) — and has **not** been re-run against the deployed build. ADR-015 criterion 5 stays RED; it moves only after the post-merge repeated-run confirmation the M29 amendment describes, the same sequence that refuted M10's own "green offline" claim. A green offline run here is not that confirmation and is not read as one |

## Declared limitations (M36 LLM judge)

| Limitation | Evidence | Status |
|---|---|---|
| **D25** — D24's second bullet ("furniture whose immediate neighbours genuinely vary page-to-page ... could false-negative — no case here demonstrates that failure") is no longer theoretical; M36 demonstrates it and closes it with a judge rather than a fifth context-window tuning pass | `verify()`'s `not_page_furniture` still misses this shape exactly as D24 predicted: `judge-catches-varying-context-furniture` (fixtures `nav-heavy-notice-home.html`/`nav-heavy-notice.html`) shares one byte-identical site-notice div between two pages — real chrome — preceded by "Pick a category from the sidebar." on one and "Travel / 11 results." on the other; `PAGE_CONTEXT_WINDOW=20` means the two windows never match, so `not_page_furniture` stays `true` (no furniture flagged) and, with no further check, the run would report `status: success` with the notice text as the answer — watched red exactly that way (judge stubbed to always-certify, simulating pre-M36). ADR-017 adds one more rung, LAST, only reached once every L1 check (including this one) already passed: `agent.py`'s `_apply_judge` calls an LLM judge, once, and any non-certify verdict — reject, error, timeout, missing key, budget exhausted — fails the run closed. Fixed and demonstrated OFFLINE only: no `OPENROUTER_API_KEY` in this environment, so the judge's actual live grading QUALITY (does a real model reliably reject this notice and certify £45.17) is unverified here; the WIRING is (a stubbed reject correctly flips the terminal status through the real pipeline) | `unsupported` for live grading quality — declared, not guessed at: the mechanism, the fail-closed property, the cost boundary and the injection resistance are all demonstrated offline (`judge-fail-closed-on-error`, `judge-fail-closed-on-any-exception`, `judge-missing-key-fails-closed`, `judge-run-budget-enforced`, `judge-fast-suite-stub-boundary`, `judge-cache-hit-needs-no-key`, `judge-injection-cannot-flip-verdict`); whether the live judge actually rejects "Warning!"/site-chrome answers on the real deployed site is not, and ADR-015 criterion 5 stays RED until the post-merge repeated-run confirmation the M29 amendment describes runs against a redeployed build with a key |
| **D26** — the strict `certify` check (`parsed["certify"] is True`) that closes the over-certify direction fails closed on a formatting slip in the OPPOSITE direction too: a provider response of `{"certify": "true"}` — the same string-boolean shape as the over-certify slips D25's fix rejects, just spelled in the certifying direction — is rejected rather than certified. The failure mode this produces is not "one run fails", it is "the judge certifies nothing at all" for the rest of the run distribution, for as long as the chosen model keeps emitting that shape | `src/browser/judge.py`'s `live_judge` parses the model's JSON response and requires `parsed["certify"]` to be the literal JSON `true` (Python `is True`) to certify — any other value, including the syntactically valid but wrongly-typed string `"true"`, is a reject, not an exception. Pinned by `judge-parse-response-strict-boolean`'s string-typed-'true' scenario, which asserts the reject directly against the real parser (transport mocked, no network). Nobody has observed what `deepseek/deepseek-v4-flash-0731` (the configured judge model, `JUDGE_MODEL` in `judge.py`) actually emits for `certify` under this prompt, because there is no `OPENROUTER_API_KEY` in this environment — so whether this ceiling is ever reached in practice is itself unverified, not just its cost | `unsupported by design`, in the safe direction: fail-closed is the correct posture for a component whose entire job is refusing to certify what it cannot verify, and normalising a hand-written set of true-spellings against a model nobody here can observe would be an unwatched loosening of the check that closes the over-certify direction. No fixture demonstrates this shape occurring against a real model, for the same reason D22 above states for its own guard: a "no case demonstrates this" limitation is a claim about this eval set's coverage, not proof the shape cannot occur |

## Declared limitations (M10 A-freeze)

| Limitation | Evidence | Status |
|---|---|---|
| **D21** — `SCOPE_BLOCK`'s M10 destructive-delete widening covers only the verb the probe demonstrated; `remove`/`erase`/`wipe`/`clear` are not screened at all | the second held-out probe found `deleting all` bypassed the pre-widening `delete`-only clause, and the fix (`l5-refuse-delete-determiners`) widened inflections and determiners for `delete` specifically, deliberately not for every destructive synonym (PR #25 R1). `SCOPE_BLOCK` (`src/browser/agent.py`) has no clause at all for the other four verbs: a task phrased "permanently erase all my files", "wipe all messages" or "clear my entire inbox" matches nothing and proceeds to a real browser exactly the way "deleting all emails" did before the fix | `unsupported`, declared rather than guessed at — widening to synonyms nobody has probed is exactly the unwatched expansion this repo's eval-first rule exists to prevent (CLAUDE.md rule 2); a clause and a case land the day a probe or reviewer demonstrates one of them, not before |
| **D22** (re-measured at M31, not retired) — a superlative question with no ground truth is still refused, including one a single extraction would have answered correctly; what M31 changed is where the refusal happens and what it costs | The M10 verifier guard fired on every task matching the superlative pattern with no `expect.answer`/`expect.state` — which is every live run — and it fired *after* the run had moved the browser and produced the wrong answer it then rejected. M31 splits that cost in two. **The plan-time half**: `agent.plan_gap` refuses an aggregate-shaped task whose plan does not read the page exactly once with `extract_all`, at every point the executor adopts a plan (the first one, and an `act`-ladder replan — `plan-lint-holds-across-a-midrun-replan`); re-measured on the row's own repro fixture, `verifier-aggregate-superlative-fails-loud` now ends `failure:task` with `budgets_spent.actions` = 1 (the pre-plan navigation) and `answer` null, where the same input previously ran to `failure:semantic` with 2 actions and the answer 'Nimbus Shop' produced for the verifier to reject — the action count is graded by that case, not asserted here. **The verifier half**: `aggregate_needs_comparison` is unchanged for a run that enumerated nothing, and now relaxes when the graded trace really contains an `extract_all` step — so the shape can PASS on layer 1 alone, which it previously could not do at all. Both directions are pinned by `verifier-aggregate-ground-truth-untouched` (row 1 no ground truth → still FAIL, the original repro re-run verbatim; row 4 graded enumeration → PASS; row 5 superseded enumeration → FAIL). The false refusal that remains is the directly-stated superlative — a badge that says 'our most popular item' — which enumerating cannot help, because there is nothing to rank; it is now refused one layer earlier and for one action instead of two | `unsupported by design`, in the safe direction, and **narrowed rather than closed**. No fixture in this repo states a superlative directly and correctly, so this residual cost is still declared rather than cased against a real example — the same footing D3 describes, and the reason the row survives M31 instead of being deleted. The escape that now exists for the rankable half of the shape did not exist when the row was written: `extract_all` plus `verifier.rank` is a plan that CAN get it right, so the guard is no longer refusing a question the system had no way to answer (`specs/decisions/ADR-018-m31-plan-lint.md`, `probe3-quotes-most-quoted-author`, `rank-reduces-enumeration-in-code`) |

## Declared limitations (M9 cost/model ablation)

| Limitation | Evidence | Status |
|---|---|---|
| **D12** — there is still no cost/latency/correctness number for any model, including the default one this system runs on | `docs/analysis.md` §9 ships the ablation section with the tradeoff table **empty**, and the entire measured LLM spend of this project remains three deployed runs — $0.0029 (`09b21b3a`), $0.006474 (`cd7121fc`) and $0.005454 (`734d3d1f`), none of them a comparison. §2 of `docs/analysis.md` still names only the first two; it was written before the third and its refresh is M10's (spec-drift audit, M9). The mechanism that will fill it is merged here (`ABLATION_MODELS`, the allowlisted `model` field, and the driver at `evals/ablation.py`); see D14 for which model it will NOT measure, but the paid runs happen on the deployment and the deployment cannot vary the model until this merges and redeploys | `unsupported`, and **deliberately empty rather than estimated**. The emptiness is graded, not promised: `analysis-ablation-table-not-estimated` (invariant-tagged) fails if the section declares itself pending and contains any data row, and once it names a committed report every cell must equal what the driver's own formatter derives from that report. Watched red both ways — a fabricated row turns it red, and ablating either of the two branches that decide whether numbers may exist turns it red. Committed variants across four review rounds — the list lives in `evals/adversarial/analysis-ablation-table-not-estimated.json` and is deliberately not counted in prose, because three documents carried three different totals for it, all stale within one PR (PR #15, R19). Two rules, and only one of them is complete. **Structural (complete): the section contains exactly one table, the graded one** — so a results *table* cannot be smuggled in however its cells are spelled, and this is what forced §9's own "where the pieces live" table into a bullet list. **Figure-shaped (incomplete, and declared so): no currency amount, so-many-of-five score, bare cents/percent figure, or latency word beside a number** on any other line, in any markdown. That list has been widened in four review rounds — model ids it did not list (R2), markdown syntaxes it did not enumerate (R11), table respellings (R17), then prose respellings using R17's own spellings (R21) — which is the evidence that an enumeration over natural language never closes. **A determined author can still write a per-model estimate as an ordinary English sentence and this guard will not catch it.** That is stated here and in §9 rather than left as a "syntax-blind" claim the code cannot honour; what is closed is every shape an estimate has actually been smuggled in as, plus every table. §9 itself now publishes no figures at all, which is what makes the whole thing checkable. **The document-wide duplicate-table sweep was, for one round, never applied to the real file** — the live call omitted an optional `document` argument that silently fell back to the section, so it graded only synthetic inputs (PR #15, R12). That parameter is now required, and the committed file and every whole-document variant share one entry point so a variant can pin the scope the real check uses. Four bounds on what the table will license when it exists, all stated before the run so they cannot be chosen to fit it: **one run per (model, task) cell**, so a 3/5-vs-2/5 split is not evidence of a better model; **five tasks on one prompt**, so it measures this system with that model and not the model; **p95 over five runs is the slowest of five**, which is why the decision rule's latency bar is 2x and not something finer; and the correctness column compares with `verifier.answers_match`, which is exact outside numbers — a correct extraction with a wider span ("Price $39.00" for "$39.00") scores incorrect. That last one understates every model equally, including the default, so it cannot flatter a challenger. All five M9 cases are green in the committed runs `evals/report/20260820-162200-fast.json` (91/91) and `evals/report/20260820-162043-invariant.json` (27/27), taken after PR #15 review round 4. `specs/decisions/ADR-010-m9-model-ablation.md` Decisions 3, 4, 5 and 8 |
| **D15** — a `failure:env` run can mean "this model cannot plan", so the ablation's most likely finding used to abort the sweep instead of being recorded | `agent.py` returns `failure:env` for a planner exception (including `PlanError` from `parse_plan` — the model answered with prose instead of a JSON array), for a replan that raised, and for budget exhaustion, as well as for a missing key. Reproduced end to end on loopback: a planner raising `PlanError` yields `status: failure:env, reason: "planner failed: planner returned non-plan output: ... I'd be happy to help you with that!"`. The driver aborted the whole sweep on any `failure:env`, discarding every paid run so far and writing no report (PR #15, R1) | `unreliable`, and the taxonomy is the ceiling rather than the driver. **A run is published only if its terminal status is on an allowlist of statuses that measure the MODEL** — `success` and the four failure classes that require a plan to have run, plus `failure:env` with a model-fault reason and `failure:task` that is not our own URL refusal. Everything else aborts, including `failure:nav` (a page that will not load is reachability, not planning) and any class a later milestone adds. The allowlist replaced a denylist that was extended three times, each time by a reviewer: round 3 found that a PRE-PLAN nav failure — before any planner call, always $0.00 — was published as a model scoring 0/5, and task 5 of the set is live (PR #15, R14). Within `failure:env` the split is made in `agent.py` by catching `PlanError` — the type `parse_plan` raises, and the only thing meaning "the response arrived and was not a plan" — separately from every other exception: `planner rejected:` / `replanner rejected:` / `budget exhausted:` score an incorrect cell, and `planner failed:` (a call that failed at the provider or on the wire) aborts. Each row carries its `reason`. **The first repair of this row got it backwards** and is the reason the mechanism moved: it matched `planner failed:`, which `agent.py` writes for every planner exception, so an OpenRouter 402 would have been published as a model scoring 0/5 (PR #15, R9). Prefixes at all rather than a proper class because "the model cannot plan" has no entry in `docs/evals/failure-taxonomy.md`, and adding one changes what `specs/000-invariants.md` pins; that is the named upgrade path, not something a cost milestone does in passing. What makes the prefixes safe is that they are authored where the type is known and only named downstream. Graded by `ablation-env-failure-is-a-result`: a truth table, a real `run_one` whose planner emits prose (scored), and a real `run_one` whose planner raises HTTP 402 (aborts) — each watched red |
| **D17** — one ablation task failed on every model tested, with one signature | the M9 ablation ran four planners over five tasks (`evals/report/20260821-004617-ablation.json`); `tc3-shop-detail-nav` came back `failure:locate` for **all four**, every time, at the same resolver error on the same anchor — `no tier resolved` for a `near:` target whose anchor is a field label on a product detail page. It reproduced across every aborted sweep as well, so it is not a one-run artifact. Four independently-trained planners emitting one failure signature on one page is not four weak models | `unreliable`, and **a finding the ablation surfaced about the system, not about the models** — it is excluded from any model-quality reading and counted against none of them. The boundary is in `near:` (ADR-006), which resolves an anchor to the nearest element in document order and has no tier that reaches a value sitting in a labelled field on that page's shape. Not fixed here: M9 is a cost milestone, and a resolver change belongs behind its own eval case and its own red-first loop. Named as the upgrade path rather than folded into a cost decision |
| **D18** — five ablation sweeps aborted on transport, and the cause is not established | **Measured.** Five sweeps aborted before one completed, every abort on the client's transport or a run outliving its wall-clock budget, never on a model: connection timeouts mid-poll, a refused connect on the next submit, and twice a run past its completion budget. `/healthz` answered in about a fifth of a second either side of every one. Per-run wall clock climbed across a sweep — one model took seconds on an early task and minutes on a later one, against tens of seconds for that task on every other model. From the Zeabur dashboard over the same window: **peak memory ~175 MB against 8 GB, falling back to a ~55 MB baseline between runs; peak CPU ~18-20% of 2 vCPU, usually under 10%; no container restart** — one deployment, dating from the PR #15 merge, spans all five sweeps. Known configuration, not inference: one uvicorn worker, `asyncio.Semaphore(1)`, one Chromium launched and closed per run, teardown in a `finally` inside the Playwright context that all 12 returns in the run body pass through. **Unknown: the causal mechanism.** Nothing above reaches a cause, and this row has now asserted two that the evidence did not support. The first was a browser-teardown window, refuted by the code — `server.py` assigns the terminal record *inside* `async with SEM`, after `run_task` has returned and therefore after the browser is closed, so the window did not exist. The second was this row's own title: *the deployment cannot sustain a twenty-run sweep* implies capacity, and the dashboard refutes it — the sweep ran with roughly 98% of memory and 80% of CPU idle and no restart. Both are recorded rather than quietly edited, because being wrong twice in the same direction is the finding: a plausible mechanism is the thing this repository treats as a defect when the evidence does not reach it, and no eval case could have caught either, since nothing in the suite asserts anything about the deployment's runtime state. **No third mechanism is offered here.** What the evidence supports is a negative — the observed workload left large CPU and memory headroom — and one caveat on that negative: Zeabur samples coarsely, so a sub-minute peak could be missed. The *shape* (return to baseline, no upward drift) survives coarse sampling; the absolute peak does not. Remaining candidate areas, unranked and untested: request-path or event-loop blocking inside the single worker, Zeabur ingress behaviour, and the ablation client's own connection and timeout handling. | `unreliable`, **worked around without being diagnosed**, and the workaround is bounded and says so. Three driver constants moved, each from a measurement: socket budget, settle gap between runs, per-run completion budget. A retry was added and narrowed to **connection-phase failures only** — `urllib` wraps a failed connect in `URLError`, nothing that never reached the server was billed, so a resend cannot double-spend; a read timeout after the request landed raises a bare `TimeoutError` and is deliberately **not** retried, because that run may already be executing and resubmitting would spend twice and record once. ADR-010 Decision 13's rule is untouched: an exhausted retry still aborts the sweep and still writes no report. The spend from the five aborted sweeps is real and lives only on stderr, per CLAUDE.md rule 4 — a partial sweep is not a result. ADR-010 Decision 19 |
| **D20** — demo readiness is demonstrated for interview-shaped workloads, and only those | `evals/soak.py`, two back-to-back sequences of five sequential tasks against the live deployment (`evals/report/20260821-020840-soak.json`, plus a single-sequence run at `...-020709-soak.json`): **10/10 completed with zero infrastructure failures** — no transport error in any phase, no stall, no lost result. Client wall clock 4.68-13.53s per task with **no upward drift across the ten runs** (5.02, 7.14, 13.53, 4.68, 7.18, 5.06, 6.82, 11.27, 7.14, 7.15), against the rising per-run clock D18 records for a twenty-run sweep. Task set covers the paths the system has: fixture extract, fill+click+extract, form submission with postcondition verification, a multi-step sort, and a live site nobody here authored. Spend $0.0622 **Re-run after the redeploy (`evals/report/20260821-145535-soak.json`), with `/readyz` live and the new default `openai/gpt-5.6-luna` serving: again 10/10 with zero infrastructure failures.** All **30** `/readyz` probes answered 200 with the contract intact — ready before every submission, busy during every run, ready after; `active_run_id` matched the submitted run id **10/10**. Probe latency 0.128-0.218s, and critically the *while-busy* probes are the same 0.128-0.218s (mean 0.174s) — the endpoint answers promptly from the same event loop that is executing a run. Client wall clock 4.71-13.73s, spend $0.0063. | `supported` **for this workload shape, demonstrated twice — before and after the redeploy — and only for this shape.** The pending caveat is discharged: `/readyz` is deployed and its transitions were observed live, 30/30. **The event-loop candidate in D18 is now the weakest of the three**: a run holding the semaphore does not stop the loop answering in ~0.17s, measured ten times. That narrows D18; it does not close it, and the other two candidates (Zeabur ingress, client connection handling) are untouched by this evidence. What the soak still does **not** do is retro-diagnose D18 — ten clean runs against five aborted sweeps on the same container says **workload shape**, not capacity, and no mechanism is inferred from the contrast. **Two scoping corrections from PR #21's review, applied to the numbers above rather than around them.** First, `infrastructure_failures: 0` in all three reports means *no connect-phase failure that survived up to three retries*: submit and poll go through `ablation._http`, which retries a `URLError` twice with backoff and, at the time those runs were taken, recorded nothing when a retry succeeded — so the artifacts cannot distinguish that from *no connect failure at all* (R3). The driver now writes a `transport_retries` ledger per row; these three reports predate it, and the claim above is read with that bound. Second, the soak scored correctness with a raw string `==` rather than the production `verifier.answers_match` plus a successful terminal status, so its cells were not comparable with the ablation's despite being published next to them (R7). Recomputing every committed row under the ablation's rule gives **the same 8/10 and 7/10** — two of the five tasks are currency answers where the rules could have diverged and did not — so the figures below stand unchanged and are now the same metric. Correctness moved 8/10 → 7/10 with the model change and is context, not the criterion: `live-books-travel-price` failed both sequences (a `near:` proximity ambiguity, D17's family), and **`tc5-forms-submit` failed once and passed once on the same model, same page, same build** — first-hand nondeterminism, and direct support for ADR-010 Decision 18's refusal to read the ablation's four-way tie as equivalence. ADR-011 Decision 6 |
| **D19** — the section-9 estimate guard is complete only inside section 9 | inside §9 the structural rule holds — the section contains exactly one table, so any other table row is a results row however its cells are spelled. Outside §9 only an **exact copy** of the graded table is refused, document-wide. Measured, not assumed: an exact copy pasted into §8 or appended at the end of `docs/analysis.md` turns `analysis-ablation-table-not-estimated` red; a per-model table with a different header, in another section, does not | `unsupported`, declared rather than claimed shut. Same shape and same reason as the prose residual in D12: this guard has been widened four times across five review rounds, each widening prompted by a reviewer walking around the last one, and an enumeration over natural language never closes. A stated boundary beats a net that claims more than it catches. ADR-010 Decision 20 |
| **D16** — a planning call the provider billed for and that returned prose used to be recorded at zero cost | `live_planner` built its `usage` dict *after* `parse_plan`, so a completion charged by OpenRouter that failed to parse discarded its own cost, and `agent.py` accumulated tokens only on the success path. Measured against a stubbed provider response reporting 1,873 tokens / $0.0012: the committed row read `llm_tokens: 0, llm_usd: 0.0` (PR #15, R10). Four documents claimed "with their real cost and tokens still counted" | `supported` — `PlanError` carries the usage of the call that produced it, `live_planner` builds usage before parsing, and `agent.py` charges it to the model that emitted the prose. **Extended at round 3 (R15):** `content` was extracted one line above the guarded block, so `content: null` — what a reasoning model returns on `finish_reason: length`, and the ceiling model defaults to high reasoning effort — raised AttributeError, was classed as a transport failure, aborted the sweep and discarded $0.0064 the provider had billed. The whole of the response handling is inside the guard now. **Narrowed again at round 4 (R18):** that guard was a bare `except Exception -> PlanError`, so an envelope the planner could not read — no `choices` key, a string `error` field, `choices: null`, a gateway page — was classed as the model's fault and would have published an outage as a 0/5 cell, the R9 defect with its polarity flipped. Default-deny now applies at the response boundary too: only a response positively recognised as "the model answered, and the answer is not a plan" is scored, and seven envelope shapes plus three no-plan shapes are pinned as probes of the real `live_planner`. `choices: []` moved from the scored side to the aborting side, reversing a round-3 pin, because an envelope with no completion carries nothing to judge. Pinned at the production path, not at a hand-built exception: `ablation-env-failure-is-a-result` drives `live_planner` itself against a stubbed provider response and fails if the raised error drops the billed usage. That probe exists because the discrimination pass found the first version of this fix unpinned — reverting `live_planner` left the case green, since the end-to-end half constructs its own `PlanError`. Consequence had it shipped: a cheap model emitting prose on 3 of 5 tasks publishes ~40% of its true spend, into the column ADR-010's decision rule compares across models |
| **D13** — the public submit endpoint now takes a caller-chosen planner model | `POST /tasks` accepts an optional `model` field so the ablation can vary the planner from outside the deployment. The endpoint is public and unauthenticated, and the run budgets (30 actions, 100k tokens) count tokens, not price, so they cannot see a caller choosing an expensive model. The endpoint gates on `ALLOWED_MODELS` (the four ablated ids **plus** the incumbent default, which is priced out of the comparison but must stay reachable by name); the driver gates on the narrower `ABLATION_MODELS`. Three cases: `gateway-model-not-allowlisted` (five refusals, including an allowlisted id with a trailing space and the empty string), `gateway-model-reaches-planner` (the no-model default, the default by explicit name, and the four ablation ids, checked at the planner factory rather than at the HTTP response, which cannot tell an honoured parameter from a dropped one, pinned against the frozen OpenRouter snapshot, and grading the owner's price ceiling in both directions), `ablation-preflight-refuses-old-build` (the driver must refuse to spend against a deployment still serving a build that drops the field, and the probe run's freeness is measured — `unsupported`, $0.00, zero planner calls — not proxied), and `ablation-env-failure-is-a-result` (which also pins that a run attributed to a different model than the one submitted aborts: `RunResult` now echoes its planner model, so per-row attribution no longer rests on a single preflight at the start of a 20-run sweep) | `unreliable`, bounded in the safe direction — anything not on `ALLOWED_MODELS` is refused 422 before a run exists, and **every other model on that list is cheaper per token than the default**, so the field cannot point the key at a model more expensive per token than the one this deployment already runs. Deliberately the narrow claim: per-token cheaper does not bound per-*run* cost, since a model that plans worse replans more and two extra planning calls at a third of the price is a wash (M9 cold review corrected the loose version, which claimed a per-run bound). The real exposure is unchanged and older: there is still no per-IP rate limit (declared at M5, backlog), so a caller who loops can still spend repeatedly whichever model they pick. ADR-010 Decision 7 |
| **D14** — the model the system actually runs on is not in the ablation, and no committed run measures it | the owner set a price ceiling on 2026-08-20 — "the most expensive one I can accept is `deepseek/deepseek-v4-pro`" — and ruled that **the ceiling is the model, not the number**: that model stays in the set at whatever it lists for. `anthropic/claude-sonnet-4.5`, the incumbent `DEFAULT_MODEL`, is over it by a wide margin on both prompt and completion. Prices are quoted nowhere in prose or code; the only copy is `evals/labels/openrouter-models-20260820.json`, and the effective ceiling is derived from that file's entry for the ceiling model. That is not tidiness: the ceiling model's own price moved ~11% **inside one working session** (PR #15, R16), which the frozen snapshot caught and four hand-copied quotations did not. The four ablated ids are all at or under the ceiling and all OpenRouter-leaderboard top-10 for the week ending 2026-08-19 | `unsupported`, and a **reframing rather than a gap** — if the owner will not pay above the ceiling then the incumbent cannot stay the default on cost grounds alone, with or without a measurement, so M9's question becomes "which affordable popular model replaces it, and what does the cheapest one cost in correctness?" (ADR-010 Decisions 1, 2b, 5, 6). The consequence is stated rather than papered over: **the table will have four rows and none of them is the model in production today**, so no claim of the form "the cheap model is as good as what we had" is available from this data in either direction. **Resolved 2026-08-21 by the ablation itself** (ADR-010 Decision 18): the run happened, every candidate tied on correctness, Decision 5's pre-committed rule fell to its cost tie-breaker, and `openai/gpt-5.6-luna` replaced `anthropic/claude-sonnet-4.5` as `DEFAULT_MODEL`. The row stays because its *claim* stays true of the data: no cell measured the superseded incumbent, so nothing here supports "the cheap model is as good as what we had" in either direction — the comparison is among the four affordable candidates only. What changed is the surrounding arrangement. The endpoint **no longer** accepts the superseded incumbent by name: keeping it reachable was justified while it was still the default and merely priced out of the comparison, and once a replacement was chosen it would only have left a public unauthenticated endpoint able to spend on the model this system had just stopped paying for. Its id stays in the frozen snapshot as the evidence for the exclusion. The guard inverted with the decision: it used to require the incumbent to be **over** the ceiling and now requires the default to be a model the ablation **measured** — under the ceiling by construction — while still requiring the superseded incumbent to be present in the snapshot and still above the bar. Watched red four ways. Selection note: the leaderboard states of itself that it *"measure[s] adoption, not quality"* and does *"not rank models by accuracy, reasoning ability, or benchmark performance"* — popularity is the owner's criterion for what to test, never evidence about what is good |

## Declared limitations (M8 mutation & hostility)

| Limitation | Evidence | Status |
|---|---|---|
| **D5** — positional targeting (`index`) is order-fragile *and* silent: a re-ordered page returns a different entity, reported as success | `l4-shop-element-reordered` — the element-reordered mutation reverses the catalogue rows, `{role: listitem, index: 2}` moves from the Meridian row to the Aurora Pro row, and the run answers "Aurora Desk Lamp Pro $59.00" to a question about the Meridian Wall Clock. `grounded`, `not_a_dump` and BOTH identity-anchor checks pass, because the anchor string is on the page whichever row was read. Committed with the wrong answer as its expectation, so the case is a pin, not a pass | `unreliable` — declared since M5 for live table extraction (`live-books-travel-price` index 5, `live-hn-item1-title` index 11) and now executable rather than predicted. The mitigation exists and is not enforced: the same task written with `near` survives the same mutation (`l4-shop-element-reordered-near`, tier `structural`), and nothing makes a planner prefer it. `relocation_candidates` deliberately returns no rungs for a target that names no string, so there is nothing to recover with either |
| **D6** — content that is merely *late* is indistinguishable from content that is absent | fixture: `l4-shop-render-delayed` (the render-delayed mutation removes the catalogue at DOMContentLoaded and restores it 10s later) → `failure:locate`. Live twin: `live-quotes-js-delayed-empty` — quotes.toscrape.com/js-delayed renders ~10s late, and after `navigate()`'s bounded 2s wait for `load` the body is 78 characters against 1,499 on the immediate variant (measured 2026-08-19) | `unsupported`, in the safe direction — `resolve()` counts matches exactly once with no wait of any kind, and the relocation ladder re-observes into the same empty page, so the run stops loudly instead of answering. **Deliberately not fixed**: a bounded wait inside `resolve()` would make every legitimately-absent element pay it, and this suite is full of cases whose point is that a tier finds nothing (every first rung of every L4 case). The honest upgrade is a wait on the FIRST tier only, with its own case and its own latency measurement |
| **D7** — a page that carries no ARIA roles on its content is invisible to the planner while remaining fully visible to the verifier | `live-quotes-js-role-tier-blind`: on quotes.toscrape.com/js, `page.inner_text('body')` is 1,499 characters containing all ten quotes, while `observe()` returns 11 elements of which zero are content — WebArea, heading, 5 chrome links, navigation, list, the pager listitem, contentinfo; `get_by_role('link')` counts 5, all chrome. The run picks the only plausible target the observation offers, `{role: listitem, index: 0}`, and reports success with "Next →" | `unsupported` — this is the M8 hostile-domain result, published as it ran. The fixture twin is the a11y-stripped mutation, which the agent *survives* (`l4-shop-a11y-stripped`) because there the plan is written against the visible text; the live case is harder precisely because a planner cannot write that plan from an observation that omits the text. Two named upgrades, neither done here: an observation that falls back to text nodes when a page exposes no content roles, and an L3 evidence-only LLM check that asks whether the answer answers the question. M32's drill-down (ADR-020) is a third route and it does NOT reach this page either — measured, not assumed: none of the ten resolvable containers in this observation discloses any quote content when re-observed as a subtree (D27 above). D7 is unchanged |
| **D8, closed 2026-08-21** — the `fast` suite exceeded its own wall-clock budget | was 66.6-68.3s at M8 and **71.3-76.5s at M9** with five more cases (a reviewer measured 68.6s on another machine — observations, not a bound), against the 60s ceiling in `specs/decisions/ADR-002-performance-thresholds.md` Decision 4, of which 10.6s is `l4-shop-overlay-modal` spending a full Playwright click timeout. The declaration held for two milestones on the assumption that the rest was irreducible trend. Measured per call at M12 (`specs/decisions/ADR-013-fast-suite-wall-clock.md`), on the 67.0s pre-M9 suite: 42.2s is deliberate waiting at bounds the suite exists to exercise, 13.5s is real work, and **11.3s is per-case Playwright driver start + `chromium.launch` + `browser.close`** — 58 cold Chromiums for one suite. Merged with M9 and M9 stage 2: 59.35s at 98 cases (`evals/report/20260821-224446-fast.json`) | `supported` — the 11.3s of scaffolding is gone (one browser for the suite, a fresh BrowserContext per run, so cookie/storage isolation is unchanged; process isolation was not, which review caught — PR #20 R2, now `shared-browser-relaunches-when-dead`), the 42.2s of deliberate waiting is untouched, no production timeout moved and no case left the suite. The ceiling is now applied by `evals/run.py` to the run it just measured, and exits non-zero, instead of living in prose — a first attempt that graded the newest committed report was falsified in review because it could not go red in CI (PR #20 R1); `fast-wall-clock-budget` grades the ruling — that is the actual defect this row records, since the drift from 13s to 68s took six milestones and never turned one run red. The local ceiling was tried at 70s when the readiness case's deliberate 3.0s slot hold put the suite at 59.35-60.16s across seven runs, then a hold-margin fix and a post-fix seven-run band were used to keep 70s over reverting to 60 — but round-5 review could not reproduce that band (~22 runs across three independent measurers, idle and under deliberate CPU load, all 58.96-59.87s), so the 70s amendment was withdrawn the same day and the local ceiling ships at 60s — not cleanly: post-commit verification ran 21 more real suites and found the honest band is 58.83-60.26s, one run over the line by a few tenths, an unexplained low-single-digit-percent tail CPU load did not account for (ADR-013 Decision 4). The ceiling is per-environment since its first CI contact: `main` ran `fast` in 89.62s on the ubuntu-latest runner against the same 60s nothing had ever checked there, so CI carries its own measured ceiling, ~~80s~~ [historical] and re-derived since (ADR-019 §5 publishes it) (`EVAL_WALL_BUDGET_S`, four runs at 64.3-69.0s on the merged tree) and both are enforced; the parallel eval runner remains backlog and remains the only lever on the 42.2s |
| **D9** — the mutation-survival metric counted "matched its expectation", so a case expecting a loud failure counted as a survival | latent since M2 and unobservable until M8 wrote the first mutation case that expects a failure. measured directly over the 10 mutation cases: the old rule scores 10/10, the new one 8/10 (the committed suite reads 9/11) on a catalogue the agent survives 8 of | `supported` — `expect.mutation_survived: false` (eval_adapter) excludes a case from the numerator while keeping it in the denominator. Same family as the M3 rule that a mutation nobody had to relocate around is not a recovery: the denominator stays honest, the numerator stops flattering. The fix itself was unguarded until PR #12 R2 said so — reverting it left the suite at 84/84 — and is now pinned by `mutation-metrics-honesty`. **Corrected again in round 2 (R8)**: the survival term was still "matched its expectation", so a case that expected AND got a loud failure counted as a survival unless its author opted out by hand. Surviving now requires `status == "success"`, and `l4-shop-render-delayed` no longer carries the opt-out key at all — one case still needs it, the wrong-answer pin. No published figure moved: both non-survivors were already excluded |
| **D10** — "N by relocating" counted every rescue, including one that relocated nothing | reviewer finding (PR #12, R1). `l4-shop-overlay-modal` is rescued by the act-family replan: its four resolved tiers are all `role`, `budgets_spent.replans == 1`, and no tier ever changes — yet it was inside the published "6 by relocating", the count `specs/decisions/ADR-002-performance-thresholds.md` introduced (0/3 at M2) precisely to keep the flattering reading out. Both ladders write the same `retry_or_recovery: "recovery"` label, so the label cannot tell them apart | `supported` — the family is read from the failure class of the attempt a rescue supersedes (`locate` = relocation ladder, `act` = replan ladder), and the runner now prints `N recovered (K by relocating)`. Published figures moved from "6 by relocating" to **6 recovered, 5 by relocating**. Guarded by `mutation-metrics-honesty`, whose row 2 is exactly this shape. **Corrected again in round 2 (R7)**: every relocation rung wears the `recovery` label and supersedes the attempt before it, including the rungs that LOSE, so a run whose rungs all failed and whose replan saved it was still counted as relocated (measured: 1 before, 0 after, on shop.html?mut=overlay-modal with a role-less first target). A rescue is now a labelled attempt that succeeded |
| **D11** — `mutation_relocated` reads the recovery family from the immediately-superseded attempt only, so a real relocation is undercounted after a laundered rung | reviewer finding (PR #12, R15). `agent.py`'s relocation loop points each rung's `superseded_by` at the next one, so if rung 1 retargets at a new tier and then fails `act` — the class-laundering shape `live-ol-search-a11y-invisible` records — and rung 2 wins, the rescue's predecessor reads `act` and the run is not counted as relocated even though the tier demonstrably changed. No case in the suite produces the shape today (relocated 5 / recovered 6 reproduce exactly) | `unreliable`, **declared not fixed**, and in the non-flattering direction — it can only undercount relocation, never inflate it. Named at the call site with a `ponytail:` comment and its upgrade path (walk the supersede chain back to the failure that started the ladder, with a `mutation-metrics-honesty` row pinning the intended answer first). Fixing it in the round meant to close the PR would be speculative counter surgery on a shape nothing exercises |

## Declared limitations (M7 verifier accuracy)

| Limitation | Evidence | Status |
|---|---|---|
| **D1** — `not_a_dump` closes only the single-extraction dump shape; chunking the same dump across several extractions evades it entirely | record id *chunked-dump-cheapest* in `evals/labels/verifier-sample.jsonl`: same task and fixture as record *probe5-shop-listing-dump* ("which product is the cheapest, and what is its price?" on `shop.html`), but the plan chunks the sorted catalogue into four separate `{role: listitem, index: 0..3}` extractions instead of one whole-container extraction. Each extraction's own ratio is ~12%, far under `DUMP_RATIO=0.35`, so all four pass and the runtime verifier returns PASS on a page dump | `unreliable` — this is the largest bound on M7's gain. `verify()` never receives the task text, so no L1 check, present or plausible, can tell "list every product" from "which is cheapest" given structurally identical per-extraction evidence; closing it needs the task text at the `verify()` call site (a contract change: the verifier takes raw evidence only, by design) or ground-truth L2, absent at runtime (`specs/decisions/ADR-008-m7-verifier-accuracy.md` Decision 5) |
| **D2** — `not_a_dump`'s threshold is chrome-sensitive and thinly calibrated | the ratio is computed against the size of the real page the value was read from (`body_len`, since the M7.1 fix — case `verifier-dump-ratio-anchor-flip`; see D4 below for what it was computed against before), so the same dump on a page with more surrounding boilerplate (sidebar, breadcrumbs, pagination, footer) dilutes toward and under 0.35. Calibrated on exactly two positive examples, at ratios 0.4541 and 0.5231 — only ~0.10 of headroom above the 0.35 threshold (`specs/decisions/ADR-008-m7-verifier-accuracy.md` Decision 2); both are unsaturated (their evidence window is the whole real page, not a truncation of it), so the fix does not move either number | `unreliable`, declared — no fixture in the repo carries enough surrounding chrome to demonstrate the dilution, and a two-point calibration sample is thin; a heavier-chrome fixture would sharpen or move the threshold |
| **D3, closed 2026-08-19** — a correct answer can false-FAIL `not_a_dump` on a sparse page | was declared rather than guarded, on the grounds that no fixture in the repo was sparse enough to demonstrate it. Falsified inside the same PR that declared it: main added `src/browser/fixtures/slow-asset.html` (37 clean chars of body text) for its own navigation cases, and its correct answer — the page's `<h1>`, 23 clean chars — is 62% of the page. `not_a_dump` FAILed it, taking `nav-load-event-never-fires` and `nav-action-load-event-never-fires` (main's cases, same extraction) down with it, alongside a case written for exactly this shape, `verifier-sparse-page-not-a-dump`. All three watched red on the merged tree: `checks.not_a_dump: false` | `supported` — `MIN_PAGE_CHARS = 100` (`verifier.py`) restores the floor `MIN_EVIDENCE` used to guard (removed at M7, ADR-008 Decision 3, for lack of exactly this evidence) below which `not_a_dump` does not apply at all. 100 sits between the sparse fixture (37/39 chars) and the smallest confirmed real dump, record *probe5-shop-listing-dump* (195 chars) — every extraction the `fast` suite produces below 100 chars was checked directly, and only the sparse fixture has a ratio anywhere near `DUMP_RATIO` (next highest 0.17); no confirmed dump is smaller than 195 (`specs/decisions/ADR-008-m7-verifier-accuracy.md` Decision 7). Kept as a row rather than deleted — the declaration was correct on its date, and the lesson is the same as the M6 `near:` rows below: a "no case demonstrates this" limitation is a claim about the eval set's current coverage, not a proof the shape cannot occur |
| **D4** — on a page longer than `PAGE_TEXT_KEEP`, a record with no `body_len` reads `not_a_dump`'s denominator from the *stored evidence window* instead of the real page — the window is capped at `PAGE_TEXT_KEEP` and doubles when a distant identity anchor forces a second window onto it, so the ratio stops scaling with the real page: a value over ~700 clean characters reads as "a dump" regardless of true page size, and the SAME value on the SAME page can flip PASS/FAIL depending only on whether the plan happened to carry a distant anchor (case `verifier-dump-ratio-anchor-flip`, red before the M7.1 fix: a 776-char value on a 4,388-char page — window 2000, ratio 0.388, FAIL; the identical value/page with a distant anchor — window 3615, ratio 0.2147, PASS) | closed going forward: `agent.py` now records `body_len` (the real page length) on every extraction, and `verify()` prefers it. Still open for the frozen M7 hand-labeled sample (`evals/labels/verifier-sample.jsonl`), which predates the field and always takes the fallback: **6 of its 28 extractions have a saturated window (window length ≥ `PAGE_TEXT_KEEP`, max 3,560 characters)**, so those six ratios are judged against a truncated-or-doubled window rather than their page. None of the six cross `DUMP_RATIO` either way (all far under 0.35), so the pinned confusion matrix in `evals/adversarial/verifier-precision-recall.json` is unaffected — but that is this sample's luck, not a property of the fallback | `unreliable`, permanently — the fallback exists because the 25 frozen records must keep replaying byte-for-byte (`specs/decisions/ADR-008-m7-verifier-accuracy.md`), so this shape cannot be closed for them without re-capturing (and re-hand-labeling) the sample |
| Ten focused, wrong answers pass the runtime verifier untouched | `verifier-precision-recall`'s 10 pre-existing false positives: wrong field, wrong table row, unsorted "cheapest", a search filled but never executed, a form filled but never submitted, a near-miss "Pro" variant — 4 of the 10 on live sites (`evals/labels/verifier-sample.jsonl`) | `unreliable` — none of these are page dumps, so `not_a_dump` was never going to reach them; closing this needs ground truth (L2, absent at runtime) or an evidence-only LLM check (L3, absent by design) |

## Declared limitations (M6 live breadth)

| Limitation | Evidence | Status |
|---|---|---|
| Live TC1 on news.ycombinator.com and openlibrary.org is stub-verified only (still true, and now the *only* live qualification) | `live-hn-item1-title` and `live-ol-edition-title` pass with hand-written plans; the live planner has never been run against either domain | `unreliable` — the same stub-vs-planner asymmetry declared for books.toscrape.com. The resolver/executor/verifier path handles both real DOMs; planning quality on them is unmeasured until a `full` run |
| **Closed 2026-08-18** — openlibrary.org was unreachable for the last day of M6, and half of what looked like the outage was ours | the host recovered; `live-ol-search-a11y-invisible` went green on the first attempt (its first green ever), and `live-ol-edition-title` kept failing `nav` until the wait condition was fixed — the page loads in 4.4s under `domcontentloaded` with the expected heading present, and times out at 25s under `load` (ADR-007) | `supported` — the live suite is 6/6 and all three live domains now rest on runs against current code. Kept as a row rather than deleted: the declaration was correct on its date, and the lesson is that a case declared unreachable should be re-run rather than allowed to harden into a conclusion |
| **Closed 2026-08-18** — a page whose `load` event never fires is unreadable, though its content is complete | `nav-load-event-never-fires` (pre-plan hop) and `nav-action-load-event-never-fires` (the `navigate` step), both watched red; the second re-watched red *after* the first was fixed, since `run_task` has two `page.goto` calls and fixing one would have left the other green-but-broken | `supported` — both route through one `navigate()` helper: `domcontentloaded`, then a **bounded** 2s wait for `load`, swallowed on timeout. A healthy page pays nothing; a hanging one is read instead of blamed. `networkidle` was tried first and rejected on measurement: +34s on the fast suite, over the ADR-002 budget (ADR-007) |
| An observe case pointed at an incompatible fixture reads as a broken observer, not a bad subject | no case — `_run_observe_case` on `slow-asset.html` raises `TimeoutError` after 30.4s (measured in the #9 review). Marked `ponytail:` at the call site | `unreliable`, eval-harness only — **declared, not fixed**. The strict `load` wait is correct there and stays: production asks "is this page usable enough to act on?", observe ground truth asks "what does a fully settled page expose?", and collapsing the two would leak the production contract into the harness. What is wrong is the *budget*: it inherits Playwright's 30s default rather than one anyone chose, and a timeout surfaces as a traceback instead of a result. Upgrade path: keep strict `load`, give it an explicit 5–10s eval budget, and return a structured `eval_env` failure. No production path touches it and no existing case reaches it |
| **Closed 2026-08-18** — a best-effort screenshot could block a run for 30s per step | same two cases: 32s and 64s before the bound, 4.3s and 8.4s after | `supported` — `page.screenshot()` waits for fonts, which on a page whose `load` never fires runs to its 30s default, inside a `try/except` whose comment says failing there is acceptable. Best-effort and unbounded cannot both be true; it gets the same 2s budget as a postcondition. No case asserts it — it is a latency property and there is no timing-assertion mechanism here — so the evidence is the committed report, named in ADR-007 rather than implied |
| A control invisible to both the a11y tree and the light DOM cannot be located | `live-ol-search-a11y-invisible`: Open Library's homepage search field exposes zero searchbox/combobox/textbox roles and no visible `<input>` — it is mounted by JS behind a reveal interaction (hand probe, 2026-08-17) | `unsupported` — no locator tier in the contract can reach it, a hostility class no fixture represents. The **diagnosis** is now proven live: after two runs that failed for other reasons (`failure:act` before the M6 fix, `failure:nav` during the outage), the case went green on 2026-08-18 — `failure:locate`, diagnosis 1/1, on a real site nobody here authored. The control remains unreachable; what is supported is saying so correctly instead of fabricating |
| **Closed at M6** — relocation onto a non-actionable element launders the failure class | was: report 20260817-024235-live, diagnosis 0/1 — the unresolvable searchbox relocated to a text-tier match on the literal string "Search" and the `Locator.fill` error reported `failure:act` for a root cause of `locate`. Now: `relocate-fill-non-editable` (fixture twin, watched red at `failure:act`) | `supported` — the guard sits in the executor's fill path, not in the relocate rung, so a first-attempt plan that targets a non-fillable element is classified the same way. An element that cannot hold a value is the wrong element, which is `locate`; a readonly or disabled input is the right element in the wrong state and stays `act` |
| **Closed at M6** — unknown target-schema keys are silently dropped by the resolver | was: `live-hn-item1-submitter`'s `near:` resolving as bare `{role: link}` → `ResolveError: 39 matches`, ignored rather than rejected. Now: `resolver-unknown-target-key` | `supported` — the five keys in `specs/001-browser-contract.md` are the whole schema and anything else stops the run as `failure:task`, naming the key |
| **Closed at M6** — `near:` was advertised in the contract and never implemented | `live-hn-item1-submitter` (39 links, the submitter picked by proximity to "points by"), `live-books-detail-upc` (a spec-table value picked by its label), `tc1-shop-price-near`, `near-excludes-its-own-anchor` | `supported` — document-order proximity, with the anchor's own element excluded and the refusal rules in the row below. It is the first mechanism to emit the `structural` tier in any run. Not a relocation rung: the ladder still climbs between `role` and `text` only |
| **Closed at M6, after a cold review** — `near:` returned confident wrong answers on three page shapes `shop.html` does not have | `near-anchor-substring` (anchor "Total" bound to "Subtotal" → the subtotal returned as the order total), `near-prefers-the-container` ("which row costs $24.50" → a different product at a different price), `near-equidistant-is-ambiguous` ("which product costs $24.50" → "Add to cart") | `supported` — anchors match exactly before loosely and keep only the deepest match; a candidate wrapping the anchor outranks every neighbour; and both kinds of ambiguity (two disjoint anchor matches, two equidistant candidates) are loud `locate` failures instead of tie-breaks. All three had reported `success` with a `PASS` verdict and nothing in the trace to suggest doubt |
| **Closed at M6** — a relocation rung dropped the `near` constraint it was recovering | `relocation-preserves-near`: a failed `{role, text, near}` relocated to `{role, name}` with proximity deleted, resolved a different element and reported success | `supported` — rungs carry `index` and `near` forward as intent. Found by the M6 spec-drift audit, not by the suite: `resolver-unknown-target-key` surviving one layer down |
| `near:` degenerates to first-candidate inside shadow DOM | `shadow-dom-value-is-reachable-and-grounded` covers the ROLE tier on the fixture that now exists (`frames-host.html`); the `near`/proximity tier inside a shadow root still has no case | `unsupported`, declared, and narrowed by M42: Playwright's locators pierce open shadow roots and `document.querySelectorAll('*')` does not, so an anchor or candidate inside one scores `indexOf === -1` and every candidate ties — which `NEAREST_JS` then refuses as `AMBIGUOUS`, i.e. a loud `failure:locate`, NOT the silent wrong answer this row claimed until the M42 spec-drift audit read the code. "The first wins, reported as success" holds only where there is exactly one candidate, and `near` is a no-op there anyway. `NEAREST_JS` is unchanged, so this row stands as written for `near:`. What M42 DID change is the two layers underneath it, and the "no fixture uses shadow DOM" half of the old citation is now false: `frames-host.html` has an open shadow root, and `observe.page_text` makes its rendered text visible to the evidence pipeline, which is what stopped a correctly-read shadow value from being failed as ungrounded (ADR-028 §4) |
| A fill into a contenteditable field is failed `act` after it worked | no case — no fixture has a contenteditable (M6 cold review) | `unreliable`, in the safe direction: `FILLABLE_JS` admits `isContentEditable`, then the readback calls `input_value()`, which throws on anything that is not a form control. Every modern rich-text composer is this shape |
| **Closed at M6** — a correct answer can be graded FAIL because the stored evidence is narrower than the page | `evidence-window-keeps-the-anchor`, found by `live-books-detail-upc`: right value, anchor present on the page, runtime check passed, eval verdict FAIL — the identity anchor was 2000+ characters from the value on a product page with a long description | `supported` — the evidence window now keeps a slice around the anchor as well as the value. A false FAIL is the safe direction and still a defect: in a report it is indistinguishable from a run that really did read the answer off the wrong entity's page. **M7 measured verifier precision on a 25-record hand-labeled sample** (`verifier-precision-recall`): precision 0.476, recall 0.909 — a floor reading of a deliberately adversarial sample, not general accuracy. Not from this mechanism, though: `identity_anchors` is vacuously true on every one of the 25 records (the replay passes no `expect`, so `anchors=[]` always), so this fix contributed nothing to that matrix — the matrix came from the mechanical L1 checks (`grounded`, `actions_verified`, ...) plus `not_a_dump` (`specs/decisions/ADR-008-m7-verifier-accuracy.md`, `docs/analysis.md` §5) |

## Declared limitations (M5)

| Limitation | Evidence | Status |
|---|---|---|
| Live TC3 on books.toscrape.com is `unreliable`, not `supported` | `live-books-travel-price` passes with a **stubbed** plan; the same task with the **real** planner failed on the deployment (run `cd7121fc`) | `unreliable` — the two results disagree, and the disagreement is the finding. With a hand-written plan the resolver/executor/verifier path handles the live DOM; with the live planner the run failed loudly at `failure:locate`. Declaring this `supported` on the strength of the stubbed case would be exactly the flattery this table exists to prevent |
| The live planner emits target roles that are not ARIA roles | deployed run `cd7121fc`: `{"role": "text", "name": null, "text": null}` → `ResolveError: no tier resolved` → `failure:locate` | `unreliable` — the planner reaches for a `text` "role" when it wants to read text. The agent fails loudly with the right class and no fabricated answer, and relocation correctly finds nothing to climb to (a target with neither name nor text has no rungs), so the cost is a failed run rather than a wrong one |
| Live extraction from a table is positional | `live-books-travel-price` targets `{role: cell, index: 5}` | `unreliable`, and **superseded at M6**: ARIA makes `rowheader` a subclass of `cell`, so label and value cells interleave and the price is reached by counting. The semantic form, `near:`, was advertised in the `specs/001-browser-contract.md` target schema and never implemented — the spec-drift declared at the freeze. It is implemented now and `live-books-detail-upc` reaches a table value by naming its label. This row stays because the *old* case still counts: a plan may still target a cell positionally, and nothing forces the semantic form |
| "The deployed URL was verified" has a shelf life, and the claim rarely carries one | deploy-smoke runs on push and on a 02:17 UTC cron. On 2026-08-18 the deployment served the M6 build and passed deploy-smoke at 14:02 UTC (run `32037391876`), then returned 404 on every path — edge with nothing bound behind it — by 18:04, with no push in between. The build was fine; the platform stopped serving it | `unreliable` — an outage starting between pushes is invisible for up to 24h, and today's cron will catch this one hours after it mattered. Not an argument for more polling: an argument that every claim about the deployed URL should carry the timestamp it was true at, which is why the run ids in this table do |
| The paid submit endpoint has no per-IP rate limit | M5 operator error: a polling loop submitted ~30 runs before anyone noticed; see `docs/analysis.md` §8a | `unsupported` by omission — per-IP limiting is backlog. Spend is bounded per *run* (30 actions, 100k tokens) but not per *caller*, so a careless loop (or an unfriendly one) can spend repeatedly. The concurrency semaphore serialises the damage but does not cap it |
| A redeploy destroys the run history | same incident: the runs that mattered most were unrecoverable because the process restarted | `unsupported` by design — `RUNS`/`STREAMS` are in-memory (analysis §4). Acceptable for a reviewer demo, and the reason the cost of that incident can only be given as an upper bound |
| Capability is about one hop deep | T9 held-out probe: 2 correct answers in 8 answer-seeking tasks. Second hops, aggregates and comparisons all failed. **Confirmed on the deployed M6 build**, run `734d3d1f` (2026-08-18): "find the cheapest book and tell me its exact price" was planned as `extract {"role": "article", "index": 0}` and returned the FIRST product, £45.17 instead of £23.21 — `success`, `PASS` | `unreliable` — the two successes were single-hop extractions whose answer string was already in the pre-plan observation. At the time there was no compare/rank step in the plan vocabulary, so "which is cheapest" was planned as "extract the whole list". **M31 added one** — `extract_all` enumerates and `verifier.rank` compares in code (ADR-016) — so the plan is now expressible; no live run has demonstrated it, and this row stays `unreliable` until one does |
| Values that exist only in an HTML attribute cannot be read | T9 probe #1 and #6: listing titles are CSS-truncated to `A Light in the ...`, with the full string only in `title=`/`alt=` | `unsupported` — extraction reads element text, so the run fails loudly (`failure:semantic` on the identity anchor, or an empty extraction) rather than returning the truncated string |
| With no start URL, the planner plans blind | T9 probe #7: step 1 came back `note: null`, `postcondition_ok: null` — no observation happened; the plan emitted `{"role": "article"}` and failed `locate` | `unreliable` — commit `ed1f774` ("the planner never plans blind") covers only the URL-supplied path, where there is a page to observe. Spec drift found by the probe, declared rather than fixed at the freeze |
| The keyword scope screen over-refuses on non-instrumental phrasing | `l5-refuse-login-contracted` (the row deliberately removed from it): "What is the sign in the shop window?" is refused | `unreliable`, in the safe direction — the words in "sign in the shop window" and "sign in to my account" are identical and only intent differs, which no keyword screen can separate. First eval evidence that the deferred LLM screening has a real job |
| An answer is never checked for being *responsive* to the question | T9 probe #5: a 20-book page dump was returned as the answer and rejected only by `grounded`, on a whitespace mismatch. Deployed run `734d3d1f` is the sharper case: the answer was a whole product tile, grounded and anchored, responsive to "which product" but not to "which is cheapest". **M10's second held-out probe found a third shape**: "which author has the most quotes" returned a pagination link, then the page `<title>`, `status: success` / `verdict: PASS` both times (docs/analysis.md §8a-2) — the exact defect this row already declared open, falsified within one probe cycle of being left declared rather than fixed | **Partially closed at M7, narrowed again at M10** — `not_a_dump` (L1) rejects an extracted value that reproduces most of its own evidence window (`verifier-catches-listing-dump`); a genuine multi-row list still passes, judged per-extraction rather than on the assembled answer (`verifier-list-rows-not-a-dump`). **M10**: `verify()` now fails any layer-1-only verdict (no ground truth) on a "which X has the most/least/highest/lowest/fewest/greatest Y" pattern — the plan vocabulary had no enumerate-and-count primitive, so that shape could not legitimately pass without ground truth and fails loud instead (ADR-015). **M31** added the primitive and moved the stop earlier: `extract_all` enumerates, `verifier.rank` ranks in code, and a plan for that shape carrying no enumeration is refused before the first action (`verifier-aggregate-superlative-fails-loud` now ends at one action; `probe3-quotes-most-quoted-author` is the same shape answered correctly; ADR-016). `unreliable` for everything else: a short, plausible, WRONG answer that is not a superlative/aggregate question (wrong field, wrong row, near-miss variant) — including `734d3d1f`'s single product tile, which is far below `DUMP_RATIO` and matches no superlative wording — still sails through untouched, because nothing checks whether the answer answers the question in general, only these two narrow shapes. Only ground-truth L2 catches the rest, and a live run has none (`specs/decisions/ADR-008-m7-verifier-accuracy.md`, `specs/decisions/ADR-015-a-freeze.md`, `docs/analysis.md` §5, §8a-2) |
| Planning quality unmeasured | every `fast` case stubs the planner at the module boundary | by design (cost-discipline); the `full` suite is the only measurement |
| Values in ARIA name-prohibited elements (`<dd>`, `<dt>`, `code`, `time`…) cannot be targeted **by role+name** | `observe-name-prohibited-roles`; the M6 counter-example is `near-excludes-its-own-anchor`, whose target `{role: cell, near: "UPC"}` carries no name at all | `unreliable`, corrected at M6 (was `unsupported`): the observation still blanks the unusable name, so role+name still cannot reach them and the agent still fails loudly rather than planning an unresolvable target. But `near:` reaches them by role + proximity, which is what a `<dt>`/`<dd>` pair is for. The limitation is narrower than it was declared |
| A locator broken at **both** reachable tiers is not recovered | `l4-shop-button-text-renamed` recovers text → role+name, `l4-recover-name-to-text` the reverse | `unreliable` — relocation has exactly two rungs to climb between: `role` and `text`. M6 made `structural` reachable (`near:`), but only when a *plan* asks for it — it is not a relocation rung, because a rung must be derivable from the failed target's own strings and a proximity anchor is a different element's text. So a mutation that kills both reachable tiers still has nowhere to go. `attrs` exists in the taxonomy and in no code path |
| An identity anchor can be satisfied by evidence from a discarded attempt | `verifier-superseded-not-a-loophole` pins the trace half only | `unreliable` — supersede hides a failed attempt from trace grading, but `evidence_text` is still built from every extraction, including the superseded one (ADR-005) |
| A replan is refused when the failed action changed nothing on the page | `replan-cannot-launder-noop-action` vs its benign twin `recovery-replan-postcondition` | `supported`, with a ceiling: the test is whole-body text equality, so an action whose only effect is visual or off-page reads as no change and its recovery is refused — loud, and in the safe direction |
| Relocation rung 1 ignores the target's role, and a rung is never guaranteed to be a different *element* | no case yet; found by cold review (ADR-005), sharpened by the M6 cold review | `unreliable` — a target `{role: link, text: X}` can relocate onto a same-named heading, a common listing-page shape. The M6 half: a failed `{text: "Search"}` relocates to `{role: button, name: "Search"}` — provably the same button — and still counts in `recovery_rungs`. It cannot create a false green, but every "N rungs tried" figure published here is an upper bound, not a count of distinct attempts |
| The progress stream is graded on the executor's hook, not on the SSE endpoint | `stream-shows-every-step` installs its own `on_step` and compares step ids only | `unreliable` — the gateway's own emitter and its copy semantics are untested, and a stream that stripped step *contents* would still pass (ADR-005) |
| Recovery is reported as a floor, not a rate | **M8:** 7 injected cases assert recovery; 13 rungs were tried to produce them (3 cases / 6 rungs at M5, when this row was written) | by design (ADR-003) — three cases is not a population. It stays `x/y` with the denominator printed until a live suite gives it one |
| Near-miss entity whose name contains the target's name | `trap-near-miss-entity` | caught only with external ground truth; a live run's runtime anchor passes it |
| Identity anchors on aggregate pages (listings, search results) | `trap-search-not-executed`, and now a live instance: deployed run `734d3d1f` anchored on `"Travel"` — the category, which every product on the listing satisfies — and the wrong product passed the check | `unreliable` — every candidate entity is in the page text, so the anchor certifies the wrong answer too. The larger of the two anchor holes, and it sits exactly where TC2/TC4 live. |
| The `fast` suite never runs observe → plan → resolve | all fixture cases inject a stub plan at the planner boundary | by design (cost-discipline), but it means the L4 self-maintenance passes are measured on plans hand-authored against the mutated DOM. Only the `full` suite closes this. |
| A click whose effect cannot be asserted | `postcondition-unverified-click` | `unsupported` by choice: the run fails loudly rather than reporting an unverified state change as success |
| ZH support is character-level, not planning-level | 6 ZH cases, all with stubbed plans | `unreliable` until a ZH case runs in the `full` suite |

## Declared limitations (M41 sec-10k inspector)

| Limitation | Evidence | Status |
|---|---|---|
| **D30** — the agent can answer from our own sec-10k inspector, and it is `unreliable` at it: 4 of 6 pre-registered probe runs correct, with the other two failing loudly in two different ways and neither of them the shape the demo was blamed on. Two tasks x 3 reps against `https://whaleforce-browser-agent.zeabur.app` on build `9c3340c` (deploy-smoke run `32926527916`), start URL the deep link `https://whaleforce-sec10k.zeabur.app/?fixture=aapl-2025&run=1`, no `model` override, $0.004742 of model spend across all six, 2026-08-26 — protocol, frozen task texts and thresholds all in ADR-030, written and committed before any run. **Both build shas, because a row here is a claim about two deployments** (postmortem §2): ours `9c3340c`, the inspector's `6b37ffa99d05` at probe time. The committed fixture snapshot is a capture of that same `6b37ffa99d05`, which took a cold review to establish and is worth the sentence: `/api/meta` was still answering `5a44758598f5` by `curl` at capture time while the page's own footer — filled from that same field — already read `build 6b37ffa99d05`, two reads of one field disagreeing inside one session, which is what a rolling deploy looks like (inferred, not measured). The capture was re-taken later and diffed against the committed one: byte-identical apart from one `total_ms` figure. So the fixture and the probe agree on one build instead of straddling two, and the page shape the offline cases grade is unchanged across the window. | **Every run id, correct and not.** Task `What is the doc_status of the aapl-2025 fixture?`: `a413fbf9` and `1e43220d` correct (`navigate` -> `extract`, under 4.5s of run time, $0.00015 each); `e996cc7d` `failure:semantic`. Task `How many items are extracted?`: `5da0441b` and `81172a2f` correct; `79c8dc32` `failure:env`. Metrics kept apart the way ADR-025 requires and never blended: **correct 4/6, loud failure 2/6, wrong success 0/6, refusal 0/6.** Ground truth from the inspector's own `/api/extract/fixture` — rule 6's per-site ground-truth endpoint, reachable from the eval side and nothing else, which is not a promise but a case (`sec10k-ground-truth-endpoint-eval-only`, 4 conjuncts each watched red — the `input`-scan one, named rather than numbered because the ordinal is a number neither document derives, added at PR #58 R2 after a review wrote a case that handed that endpoint to the executor as its own start URL and every layer, this check included, agreed it had passed). **The wrong success this probe predicted in advance happened, and the judge ate it.** ADR-030 §Frozen task table names `"Extracting…"` as the wrong-success shape to watch for; `e996cc7d` extracted exactly that string and the ADR-017 judge rejected it — "it appears to be a progress message" — turning it into a loud failure. First live evidence for the grading quality D25 declares unverified, on a shape that was written down before it occurred. `79c8dc32` is a different shape again: the plan clicked, asked to `observe {role: main}`, and ADR-020's no-progress guard ended the run. **What the offline cases pin.** S2 is fixed page-side and measured: `sec10k-doc-status-is-a-named-status` and `sec10k-item-text-region-resolves` each go red when the ARIA role and label are stripped from the snapshot (`failure:locate`, the relocation rung retargeting on a name that is not page text). S3 is fixed page-side too — `sec10k-extract-buttons-are-distinguishable`, red with the three ARIA labels removed. **What they pin that is NOT fixed.** `sec10k-item-text-region-is-past-the-observation-cap`: the fixture `<select>`'s 42 `option` elements spend the 60-element observation budget, so the named extracted-text region that `sec10k-item-text-region-resolves` proves is reachable is never SHOWN to a planner — the D27 cliff on a real deployed page. And S1: `live-sec10k-authored-wait-reaches-the-doc-status` grades that the planner's observation still lacks `18 extracted` on the deep link, ~~because the observation is taken before the page's own extraction round-trip lands~~ — **struck 2026-08-28 (ADR-039 §2): the assertion still holds and that reason no longer does.** After ADR-039's post-`load` settle the round trip HAS landed by observation time — re-measured against the deployed inspector the same day, the observation now carries the per-item sidebar buttons (`button — '1 Business EXTRACTED CONF 0.95 HEADING_STRICT 16,053 CH'`), which only exist after it lands. `18 extracted` is absent because `observe.TEXT_HEAD` caps the rendered page text at 300 characters, hundreds of characters before the status line. A green whose stated claim it can no longer falsify is not evidence for that claim, so this is struck rather than quietly re-worded, and `tasks/TODO.md` T-A37-2 owns re-pointing the case. What ADR-039 DID fix on this page is the other S1 shape, and it is the one that cost a run (`46883372`): the committed-fixture `<select>`'s 42 options are painted from `/api/meta` after `load`, were absent from every pre-plan observation, and are now present — 26 observation elements before the fix, 60 after, measured both ways against the live page. **Post-deploy re-check, 2026-08-28 — sec-10k-extract's D10 carries a cross-repo clause (after deploy, this repo re-probes the deployed inspector and this row records its sha from `/api/meta`), and this closes it.** The inspector's deploy moved to `0b87705fd0aa` (= sec-10k-extract `origin/main` HEAD `0b87705fd0aab278fee4d17a4f9febf4881e7e82`, the D14 merge — display-layer only, the item-text regions gain `tabindex="0"`; nothing extraction-side), read from `/api/meta` by `curl --max-time 20` at probe time; the page's `#sha` footer is one line of the page's own script filling from that same field, so this probe read the field once and claims nothing about a second, footer-side read. Page-shape re-probe against that build, $0, no LLM, no browser, no agent runs — `curl` on `/api/meta`, on the served HTML at `/` and at the deep link, and a `POST` to `/api/extract/fixture`, so there are no run ids and this paragraph claims none, and every clause below is a claim about the SERVED page and the API, not about a rendered session: (1) the deep-link wiring stands — `/` and `/?fixture=aapl-2025&run=1` serve byte-identical HTML whose `deepLink()` reads `URLSearchParams`, sets the `<select>` to the `fixture` param after validating it against `/api/meta`'s fixture list, and clicks `#go-fx` itself when `run=1`; (2) the banner is still `<div id="banner" role="status" aria-label="doc_status">`; (3) the item renderer still emits `role="region" aria-label="Item <n> extracted text"` in both view modes (now with D14's `tabindex="0"` — the one markup drift from the committed snapshot, and it is additive), and `/api/extract/fixture`'s Item 4 text — the value that template renders — still matches `sec10k-item-text-region-resolves`'s expected answer verbatim up to a trailing newline; (4) the three Extract buttons carry their three distinct ARIA labels. Ground truth re-verified from `/api/extract/fixture` (rule 6, eval side only): `success`, `extracted: 18`, `incorporated_by_reference: 5` — the values this row froze. The intermediate build M42's closing note measured (2026-08-27) answered `19 extracted · 4 incorporated_by_reference` with `success_with_warning` for the same fixture; the current build answers 18/5 `success` again — a reminder that the TARGET's extraction output moves between its builds, not only its markup. So at `0b87705fd0aa` everything the five snapshot cases grade is unchanged on the served page and all four D10 legibility fixes hold on the live deploy. What this re-check is NOT: a re-run of the six probe runs — the 4/6 `unreliable` verdict stays a claim about `9c3340c` against `6b37ffa99d05`, limitation (b) stands, and T-M41-3 (nothing automated detects snapshot drift) stays open; this was one deliberate manual re-probe, exactly the kind that block says is the only current detection. | `unreliable`, and these things are NOT claimed. (a) That the domain is measured rather than these two tasks on this one page — one phrasing each, three reps each, one fixture. (b) That `unreliable` will survive either deployment moving: it is a claim about `9c3340c` against `6b37ffa99d05`, the inspector's sha ALREADY moved once inside this milestone, and nothing in this repo reads either sha back (`tasks/TODO.md` T-M41-3). (c) That zero wrong-success covers every run this protocol caused: an aborted harness attempt preceded the probe and its run ids are lost except `6045555d` (`failure:semantic`, published in ADR-030 §Outcome with the six), because `RUNS` is in-memory (D19) and there is no listing endpoint — so the HARD threshold is graded on seven runs, not on all of them, and ADR-030 says so rather than rounding it away. (d) That the ANSWER is the value: both tasks are answered with the whole status line, `doc_status: success — 18 extracted · 5 incorporated_by_reference fixture: aapl-2025`, which ADR-030 froze as correct before the runs and which is still a sentence where a caller asked for a number (T-M41-2). (e) That S3 or S4 are fixed as CAPABILITIES: S3 is fixed on this page's markup only and the resolver gap stays with M38/ADR-026; S4 is unfixed, pinned offline by `l4-shop-render-delayed`, and the default post-click settle is M42's under ADR-027. (f) Anything at all about loop mode — see the mode-B paragraph above; M44 owns that column. |
