{
  "id": "view-proxy-refuses-private-and-redirects",
  "task": "browser",
  "suites": [
    "fast"
  ],
  "input": {
    "kind": "view-proxy",
    "start_url": "https://example.com/",
    "must_refuse": [
      "http://127.0.0.1:8000/",
      "http://localhost/",
      "http://[::1]/",
      "http://169.254.169.254/latest/meta-data/",
      "http://10.0.0.5/",
      "http://192.168.1.1/",
      "http://172.16.0.1/",
      "http://2130706433/",
      "http://0x7f000001/",
      "http://127.1/",
      "file:///etc/passwd",
      "gopher://example.com/",
      "ftp://example.com/"
    ],
    "must_allow": [
      "https://example.com/",
      "https://companiesmarketcap.com/apple/marketcap/"
    ],
    "must_refuse_redirect": [
      "http://127.0.0.1/",
      "http://169.254.169.254/latest/meta-data/",
      "http://10.1.2.3/",
      "file:///etc/passwd"
    ],
    "max_redirects": 5,
    "max_bytes_ceiling": 5000000,
    "source_fragments": [
      "Content-Security-Policy\": \"sandbox\"",
      "class _GuardedRedirect",
      "if not url_ok(newurl):",
      "if not url_ok(resp.url):",
      "resp.read(VIEW_MAX_BYTES + 1)",
      "sandbox referrerpolicy=\"no-referrer\""
    ]
  },
  "expect": {},
  "provenance": "M40 owner amendment: the page view showed a screenshot, and the owner asked for the real page, scrollable \u2014 'right panel \u6211\u662f\u8981\u770b\u5230\u539f\u751f\u7db2\u7ad9\u5167\u5bb9 \u4e0d\u662f screenshot \u800c\u5df2 \u5c31\u662f\u8b93\u6211\u80fd\u6ed1\u52d5'. The sites worth demoing refuse to be framed (X-Frame-Options / frame-ancestors), so the page is fetched server-side and served same-origin, which turns a read-only panel into an SSRF surface on a public unauthenticated endpoint. This case grades that surface against the real handler's own pieces \u2014 `url_ok`, `_GuardedRedirect.redirect_request`, `VIEW_MAX_BYTES`, and the response headers \u2014 with no network. The redirect half is the half that matters: `urllib` follows redirects INSIDE `urlopen`, so checking `resp.url` afterwards means the request to the private address has already been made, and the guard has to live in the handler. `169.254.169.254` is in the list by name because it is the cloud metadata endpoint every SSRF write-up starts with, and this service runs on a PaaS. TAG NOTE (2026-08-24): this case is `fast` only, and it is not because an SSRF guard is less than must-always-hold \u2014 `url-guard-literal-ips` guards the task path's twin and is `invariant`. It is measured: tagged `invariant` as well, CI's invariant suite ran 17.58s at 59 cases against 13.12s locally, and 17.58 derives a ceiling of 25 where the committed one is 20 \u2014 item 3 (same-ceiling) red on CI and green locally, every time, which is T-M32-13's second symptom exactly (merge note, T-R44: that symptom is closed as of this merge \u2014 every ledger row carries an `env` tag and ADR-019 \u00a76 item 9 (environment) keeps a CI row out of a `local` band's ledger, so the constraint described here no longer holds in the form it is described in. The tag was NOT restored: that is T-M40-3's call and needs its own watched-red) (its own block already records the shape at 17.39s, before this case existed). CI's invariant row was 14.88s at 58 cases (ADR-021), so this case did not cause the gap so much as arrive next to it. Dropping the tag is the smaller move than raising a published ceiling, which is an owner ruling in this repo; the guard still runs on every commit, because `fast` IS the pre-commit gate and CI runs it too. T-M40-3 carries the restoration.",
  "triage": {
    "class": "security-boundary",
    "watched_red": "each half against a deliberately weakened build. Dropping the `url_ok` call from `view_page`: accepted_a_blocked_url lists all 10 http(s) private/loopback/IP-literal forms (the three non-http schemes are refused by the scheme check that remains). Removing `_GuardedRedirect` from `build_opener` so redirects follow unchecked: redirect_allowed lists all four hops. Raising VIEW_MAX_BYTES past the ceiling: cap_too_large. Dropping the CSP header, or the iframe's `sandbox` attribute: missing_source.",
    "note": "Ceilings, declared. (1) This is a pure-code check: no case fetches a real URL through this endpoint, so nothing here proves the fetch path behaves as described against a live server \u2014 same posture as `url-guard-literal-ips` for the task gateway. (2) DNS rebinding is NOT covered, exactly as `url_ok`'s own docstring already declares for the task path: a hostname that resolves to a public address at check time and a private one at connect time defeats both. The proxy inherits that hole rather than adding one. (3) The cap is on bytes read, not on time: a slow-loris host holds a worker for up to VIEW_TIMEOUT_S."
  }
}
