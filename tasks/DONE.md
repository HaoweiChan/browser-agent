# Done — one line per merged task

Append-only index. Details live in each milestone's ADR/PR and
`tasks/pr-loop-ledger.jsonl`; the full milestone-table narrative is in git
history (`tasks/TODO.md` as of `98de1a6`, blocks as of `127bd58`).

- M0 — Harness (2026-08-15) — ADR-001
- M1 — Walking Skeleton (2026-08-15) — specs/001, prompts/002, run 09b21b3a
- M2 — Eval Backbone (2026-08-16) — ADR-002, docs/evals/scope-checkpoint.md
- M3 — Reliability (2026-08-16) — ADR-003
- M4 — Reviewer UI (2026-08-16) — ADR-004; M3+M4 cold review: ADR-005
- M5 — B-Freeze (2026-08-17) — docs/plans/completed/task1-b-level-plan.md, docs/analysis.md
- M6 — Live breadth & depth (2026-08-17) — ADR-006; post-M6 nav fix: ADR-007
- M7 — Verifier accuracy (2026-08-18) — ADR-008, PR #10, support-matrix D1–D4
- M8 — Mutation & hostility hardening (2026-08-19) — ADR-009, PR #12, support-matrix D5–D11
- M9 — Cost/model ablation (2026-08-21) — ADR-010, PR #15 (mechanism) + PR #19 (numbers), support-matrix D12–D19; default moved to `openai/gpt-5.6-luna`
- M12 — Fast-suite wall-clock over budget (2026-08-21) — ADR-013, PR #20; ceiling is per-environment now, ADR-002 D4 ends unmoved at 60s
- M18 — TinBoker reviewer UI restyle (2026-08-21) — ADR-014, PR #23, trace in tasks/reviews/pr23-r*.json
- M10 — A-Freeze (2026-08-22) — ADR-015, PR #25, trace in tasks/reviews/pr25-r*.json; probe #2 ran RED on the inviolable property, fixed in-PR for the aggregate shape only — criterion 5 reopened as M29/M34 by the post-merge live verification
- M30 — Interview demo execution UI (2026-08-22) — PR #24
- M29 — criterion 5 is red on the deployed build, record corrected (2026-08-22) — ADR-015 amended, PR #28, trace in tasks/reviews/pr28-r*.json
- M34 — an answer that is page furniture can no longer be reported as success (2026-08-22) — ADR-016, PR #30, trace in tasks/reviews/pr30-r*.json; closed the aggregate/furniture shape offline, but the post-merge confirmation refuted it on the real site (6 wrong-answer-as-success in 9 runs) — ADR-015 criterion 5 stays RED, follow-up is M36
- M31 — Plan lint + `extract_all` (2026-08-22) — ADR-018, ADR-019, PR #29, trace in tasks/reviews/pr29-r*.json; 5 rounds, 2 circuit breakers
- M36 — Responsiveness judged by an LLM, last rung of the ladder (2026-08-22) — ADR-017, PR #33, trace in tasks/reviews/pr33-r*.json
- M35 — Visitor-facing console: verified example prompts, plain-language limits, no-URL guard (2026-08-23) — PR #32, trace in tasks/reviews/pr32-r*.json
- T-R34 — the band's slack becomes a declared ceiling (2026-08-23) — ADR-019, PR #35, trace in tasks/reviews/pr35-r*.json; 4 rounds, 1 circuit breaker
- M37 — Swap the HN Try example for one that reproduces on the deployed build (2026-08-23) — PR #37, efb2711
- T-R56 — the band subsystem's documents and its own strings say what the code does (2026-08-23) — ADR-019, PR #36, trace in tasks/reviews/pr36-r*.json; 5 rounds, 1 circuit breaker; bundles T-R45/46/47/48/49/52/54/55
- M28 — a verifier-rejected run carries no answer: the dump stays in evidence, the reason cites it by preview (2026-08-23) — PR #38
- M32 — Observation drill-down: the planner can ask for a deeper view of the page (2026-08-23) — ADR-020, ADR-021, PR #34, trace in tasks/reviews/pr34-r*.json; 6 rounds, 1 circuit breaker, 7 merges of main; ADR-020 written then reverted as superseded
- T-M32-13 — the ledger's `ts` becomes a valid ordering key: stamped UTC, not naive local (2026-08-23) — found and root-caused on `task/M32` from PR #34's CI red; closed by T-R44 on both halves (UTC stamp + per-row `env`), ADR-019 §7, cases `ledger-ts-orders-real-time` and `band-is-graded-against-its-own-environment`
- T-R78 — §7 stops claiming §5 names a demonstrating mutation for every graded item (2026-08-24) — PR #41 R17, the fifth instance of the class; the pointer now names `ci-numbers-are-derived`'s `watched_red`, closed in T-R44's merge commit

- T-M40-1 — a browser check takes the run slot instead of reading it (2026-08-24) — PR #45, trace in tasks/reviews/pr45-r*.json; 3 rounds, 1 circuit breaker (human ruled option B: R8/R9 shipped as T-R89/T-R90). Nine debt items T-R82..T-R90, renumbered from T-R73..T-R79 after the final merge found seven ids defined differently on both sides with tasks/TODO.md auto-merging clean
- T-M40-2 — the document root is not an answer: the plan lint refuses it at both adoption points (2026-08-24) — ADR-024, PR #46, trace in tasks/reviews/pr46-r*.json; 3 rounds, approved
- M38 — Resolver disambiguation: a target with several matches is narrowed, not failed (2026-08-24) — PR #42, trace in tasks/reviews/pr42-r*.json; 6 rounds, approved
- M39 — a malformed judge completion is retried once, then fails closed exactly as before (2026-08-24) — ADR-023, PR #44, trace in tasks/reviews/pr44-r*.json; 5 rounds, 2 circuit breakers, 12 findings (2 HIGH / 4 MEDIUM / 6 LOW), 13 debt items T-M39-1..T-M39-13, gate green at every commit. Reading a verdict out of an uncontrolled completion fail-opened four times — fence strip, `re.fullmatch`, unguarded object scan, then the published claim about the guard that fixed it — three of them regressions against main, every one through a green gate, because nothing asserted over the *completion* until this PR. Two of the twelve findings originated on the review side. Five merge laps (T-M39-11); the last red was contention rows never in the index, not a regression
- T-M39-5 — `docs/analysis.md`'s coverage counts stop being stale under a preamble that says they're current (2026-08-24) — closed in the M39 merge commit; `docs-numbers-are-derived`'s new `analysis_section1` block was widened to grade the pair (and the "remaining {remaining}" sentence) too
- T-R61 — the task field's placeholder still advertises the retired HN prompt (2026-08-24) — closed inside M40's PR #43; acceptance's second half (a `ui-form` pin on placeholder text) explicitly not met, so it can regress silently
- T-M32-9 — three published wall-clock ceilings are not the enforced ones, CLAUDE.md included (2026-08-24) — PR #40, trace in tasks/reviews/pr40-r*.json; pinned by `ceiling_sweep_rows`
- T-R44 — a published band is graded only against rows from its own environment (2026-08-24) — ADR-019 §7, PR #41, trace in tasks/reviews/pr41-r*.json; bundles T-R51, closes T-M32-13's second half
- M40 — the demo surface tells the truth about itself, and the matrix covers the domains a reviewer reaches for (2026-08-24) — PR #43, trace in tasks/reviews/pr43-r*.json; support-matrix D28; also closed T-R61
- T-M40-4 — deploy-smoke treats "refused because a run is executing" as not-a-failure, and says UNCHECKED when it exhausts retries (2026-08-24) — PR #47
- M41 — the agent answers from its own sec-10k inspector, and the matrix says exactly how far that goes (2026-08-26) — ADR-030, PR #58, trace in tasks/reviews/pr58-r*.json; 4 rounds, 1 circuit breaker (human ruled option A), support-matrix D30 declared `unreliable` at 4/6 with zero wrong-success; eight debt items T-M41-1..8.
  **Re-checked 2026-08-28** against inspector build `0b87705fd0aa` (sec-10k-extract D10's post-deploy cross-repo clause — that repo re-deployed, this repo re-probes): all four page-side legibility fixes hold on the live deploy, the deep-link preload-and-auto-extract wiring stands in the served HTML, and ground truth from `/api/extract/fixture` is back at 18 extracted / 5 incorporated_by_reference `success` (the 2026-08-27 build answered 19/4 `success_with_warning`). Page-shape re-probe only — `curl`, $0, no LLM, no browser, no run ids; the D30 verdict is NOT re-declared — details appended to the D30 row.
- M42 — loop mode: the model chooses every step, and the machinery that grades it does not move (2026-08-26) — ADR-028 (loop mode) + ADR-029 (local `fast` ceiling 90 -> 105), PR #57, trace in tasks/reviews/pr57-r*.json; 7 rounds, 4 human interventions; observation now pierces iframes and open shadow roots, a no-progress harness ends a circling run loudly, and ADR-024/ADR-018's guards are re-homed as tool-call-time refusals.
  **What M42 demonstrated live, and what it did not.** DEMONSTRATED on the deployment, 3 reps per mode, 2026-08-26: mode B fails this S1 task 3/3 (`6c37e768`, `11f9a92d`, `ffed8d46`, all `failure:locate`) — D28's declared boundary, met; the no-progress harness fired on all three loop runs (`5a33aa80`, `9ff2056e`, `7abacb97`), its first live firing, ending each at the 4th revisit of the same page state with the reason named rather than grinding the budget in a circle; and per-run cost is visible in the trace. NOT DEMONSTRATED: the headline clause — loop mode ANSWERING an S1 task on the live deployment. All three loop runs ended `failure:env`, and the cause was neither the loop nor the site but a defect underneath both, found by T-M42-20: `observe()` reads the accessible name from Chromium's snapshot, which applies CSS `text-transform`, while the locator engine does not, so the fixture picker reached the planner as `SELECT A COMMITTED FIXTURE` and resolved at no tier in either mode. T-M42-20 fixes that (anchored case-insensitive matching in the role and text tiers) and the separate empty-at-load half (`select_option` waits out a fetch-painted control), both red-first; T-M42-20 fixed that and merged as PR #60. **CLOSED 2026-08-27**: the 3-rep re-run against the redeployed build answers the clause — mode B 0/3 (`c6f901cb` `failure:env`, `3ffc1347` and `aab825fa` `failure:act`), loop mode **3/3 correct** (`111d39a9`, `f2c7f439`, `09ed99ed`), each answering `doc_status: success_with_warning — 19 extracted · 4 incorporated_by_reference` in 6 actions at $0.1384, against ground truth `counts.extracted = 19` taken from the inspector's own API. So M42's central claim — an S1 page mode B cannot plan, answered under loop mode — is demonstrated on the deployment, not only offline. One correction to the record above: the 2026-08-26 runs used the task "report the period end date", which was UNANSWERABLE — `period_end` exists only in `/api/extract/fixture`'s JSON and never in the rendered page. Those runs still died at resolution, before any answer, so the root cause they established is unaffected; but no conclusion about loop mode's ability to ANSWER could be drawn from them. The closing runs use a question the page actually renders.

## B-floor exit criteria — final status


All 6 met: criterion 2 (coverage/live breadth) was partial at the M5 freeze
and closed by M6 (3 live domains, 3 live task classes, live 6/6 after
ADR-007). Full criterion table with evidence: `tasks/TODO.md` at `98de1a6`
and `docs/plans/completed/task1-b-level-plan.md`. Standing qualification:
green live cases run hand-written plans, so live *planning* quality is
unmeasured (ADR-007) — the M5/M10 held-out probes are the counterweight.
- M45 — Chinese tasks reach the browser, and the matrix carries zh live evidence (2026-08-26) — PR #56; the headline did not reproduce, the word-boundary bug did — debt M45-D1..D9
- T-M42-20 — observe and resolve agree on a name again: the CSS `text-transform` name mismatch that killed every M42 live run, plus the empty-at-load `<select>` half (2026-08-27) — ADR-032, PR #60, trace in tasks/reviews/pr60-r*.json; 4 rounds, 24 findings, 13 repaired, 2 circuit breakers, 15 debt items T-M42-20-D1..D15. Closed M42's headline live clause: loop 3/3, mode B 0/3 on the deployment. Two wrong-successes were introduced and caught by the loop itself before merge — `index`/`near` bypassing the uniqueness check, then two passes re-basing `index` — which is what forced ADR-032 to define a match set.
- M43 — loop mode sees the page: screenshot observation for a vision model (2026-08-28) — ADR-035, PR #70, trace in tasks/reviews/pr70-r*.json; 3 rounds, 16 findings, 12 repaired, 0 rejected, circuit breaker armed and not tripped, 7 debt items M43-D1..D7. Every loop observation now carries the viewport screenshot the trace already keeps as step evidence, a drill carries an element-scoped crop, and `click_at` is refused at tool-call time unless emitted from an observation bearing a VIEWPORT frame. No model was added: ADR-028's `anthropic/claude-opus-5` is already vision-capable, so ADR-027 Decision 4's amendment rule is satisfied vacuously. Also moved the local `fast` ceiling 105 -> 110 (derived three times at three bases, 110 each time).
  **The live half is NOT closed, and this line is why the block did not simply disappear.** DEMONSTRATED offline at $0.00: the S2 fixture shape — `7.41` in a bare `<pre>` painted by a bare `<div>` control, unreachable by role tier by construction — answered through the loop's screenshot + `click_at` + text-tier extract path, with `s2-bare-value-mode-b-root-reach-is-pinned` pinning mode B's document-root reach on the same page as the contrast case. NOT DEMONSTRATED: the acceptance clause's other half — the same task answered **on the deployment, 3 reps, run ids published**. It spends paid LLM tokens and needs a deployment serving this build, so it follows M42's precedent of closing post-merge. ADR-035 scripts the exact command (`POST /tasks` with `"mode": "loop"` against `/fixtures/s2-nameless-readout.html`, 3 reps, plus a mode B contrast arm at the same URL); ground truth is the fixture's own constant `7.41`, and the declared pass shape is loop 3/3 with mode B 0/3 — mode B's failure is part of the claim, not a by-product. Runs are sequential (D19: the gateway holds one run slot) and `RUNS` is in-memory, so evidence must be recorded at probe time. **What changed since the ADR was written**: `/version` on the deployment now answers a real sha (`source: "image"`) rather than `{"sha":null}`, so the smoke's rows can name the build they measured — which is what the postmortem rule requires and what M43-D2's acceptance wants. Vision QUALITY is graded by nothing offline and says so; M44 owns the loop-mode matrix rows that measure it.
- M44-P1 — the deployment can report the build it is running (2026-08-28) — ADR-033 (`/version`) + ADR-034 (the build derives its own sha), PRs #65 and #67, trace in tasks/reviews/pr65-r*.json and pr67-r*.json; 3 + 4 rounds, debt M44-P1-D1..D8. **Closed by its own post-merge read**: against the deployment running #67's merge, `curl https://whaleforce-browser-agent.zeabur.app/version` returned `{"sha":"6a416dfd2e4e2d53d2a04018d1cfbf1ac5377e65","source":"image"}` — equal to merge commit `6a416df`, which is the condition the block set for closing. So Zeabur's builder DOES hand `.git` to the build, the second fact ADR-033 left open is settled in the affirmative, and `BUILDKIT_CONTEXT_KEEP_GIT_DIR=1` is not needed. **M44's acceptance clause is unblocked**: a matrix row against our own deploy can now name the build it measured. What is NOT closed and is named in ADR-034: the guarantee is that an accidental context copy is caught, NOT that `.git` cannot reach the image — three evasion classes are demonstrated there, with the CI image-build check filed as M44-P1-D8.
- M45-D6 — mixed-script CJK spellings passed the scope screen in every term (2026-08-28) — PR #75; 1 round, 5 findings, all repaired. Every CJK term in `SCOPE_BLOCK` was a traditional|simplified PAIR, so a spelling mixing the two scripts INSIDE one word matched neither alternative and reached the browser — 幫我輸入验证碼, 幫我购買這個商品, 購买這個商品 and 幫我輸入驗证码 all returned None. Each pair is now a character class, which fixes it per CHARACTER rather than per spelling. **The real surface is two terms, not seven**: only 驗證碼 (2^3 spellings) and 購買 (2^2) have more than one variant character and therefore a mixed form at all; 密[碼码], [刪删]除 and 下[載载] are the same two strings rewritten. A deliberate WIDENING of a fail-closed screen, not a neutral refactor — exactly **nine** spellings moved from allow to refuse (登錄 購买 购買 and the six mixed forms of 驗證碼), counted by diffing the two string sets — pinned by **eleven** `true` rows and **five** `false` rows in `screening-zh-term-inside-another-word`, watched red on the pre-fold regex with the report committed (`evals/report/20260828-045556-invariant.json`, 83/84). The over-refusals in support-matrix D31 widen with it by construction. **M45-D3 stays OPEN at three of five spellings**: 登錄 arrived free with the same fold, but 登陸/登陆 are excluded because they also mean *to make landfall*, and its unfixed half is now a tripwire in both directions — three landfall reads pinned as `false` guards and the two auth asks pinned as DECLARED false negatives, so when M45-D8's request-frame rule lands they flip green on purpose and drag their author into the file. Also corrected six documents of record that still asserted in the present tense that `SCOPE_BLOCK` ships byte-for-byte unchanged (T-M42-20-D13's rule), one of them the provenance of the very case whose new rows go green only because it changed.
- T-M39-15-D1 — the three committed `fast` rows at 230 cases can no longer force an unmeasured ceiling move (2026-08-28) — closed unbuilt, superseded by ADR-035 Decision 7 (PR #70) raising the committed local `fast` ceiling 105 -> **110**, `evals/run.py:103` as of `94637ef`: ADR-013's rule applied to the 91.76s row now derives exactly 110, so no amendment is forced, and `fast` is at 238 cases anyway. What does NOT close is the general class — ledger rows at an abandoned case count outliving the tree that produced them — which lives in `T-M39-15-D3` (inert today by coincidence of count growth, not by rule)
