{
  "id": "l4-shop-button-text-renamed",
  "task": "browser",
  "suites": ["fast"],
  "level": "L4",
  "tc": "TC4",
  "mutation": "button-text-renamed",
  "input": {
    "task": "Sort the catalogue by price, low to high, and name the cheapest product.",
    "fixture": "shop.html",
    "mut": "button-text-renamed",
    "stub_plan": [
      { "action": "click", "target": { "text": "Sort by price (low to high)" },
        "expected_state": { "text_visible": "Sorted by price (low to high)" } },
      { "action": "extract", "target": { "role": "link", "index": 0 } }
    ]
  },
  "expect": { "status": "failure:locate" },
  "provenance": "the only B-floor mutation that actually breaks a tier the agent is standing on: the plan targets the sort control by visible TEXT, and button-text-renamed changes the visible copy to 'Cheapest first' while the accessible name stays 'Sort by price (low to high)'. Today the resolver has no relocation step, so the run stops loudly at failure:locate. Recorded as the current, honest behavior.",
  "triage": { "class": "capability-gap", "note": "M3 flips this expectation to success via the relocation loop (text tier stale -> re-observe -> role+name tier). The report history is meant to show the flip; do not 'fix' it by rewriting the plan to use role+name." }
}
