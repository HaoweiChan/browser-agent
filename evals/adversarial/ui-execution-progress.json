{
  "id": "ui-execution-progress-is-trace-derived",
  "task": "browser",
  "suites": [
    "invariant"
  ],
  "input": {
    "kind": "ui-progress",
    "fragments": [
      "id=\"progress\"",
      "aria-label=\"Run progress\"",
      "aria-live=\"polite\"",
      "data-phase=\"planning\"",
      "data-phase=\"browser\"",
      "data-phase=\"reading\"",
      "data-phase=\"action\"",
      "data-phase=\"verification\"",
      "data-phase=\"complete\"",
      "data-state=\"upcoming\"",
      "grid-template-columns:repeat(6,minmax(0,1fr))",
      "[data-state=\"current\"]",
      "[data-state=\"complete\"]",
      "[data-state=\"failed\"]",
      "SUPPORT_LABELS",
      "Read a page",
      "Search and read",
      "Navigate and read",
      "Compare or sort",
      "Submit a form",
      "const EXAMPLES",
      "const LIMITS",
      "const EXPLAIN",
      "function siteInTask(task)",
      "data-example",
      "${m.limitations.length} declared limitations",
      "docs/support-matrix.md",
      "What works today",
      "Known limits",
      "id=\"pageview\"",
      "id=\"pvshot\"",
      "id=\"pvtext\"",
      "id=\"pvcap\"",
      "id=\"pvlink\"",
      "class=\"live-grid\"",
      "data-step=\"${idx}\"",
      "id=\"statusline\"",
      "id=\"pvframe\"",
      "class=\"pv-tabs\"",
      "id=\"pvlive\""
    ],
    "script_fragments": [
      "function resetProgress()",
      "function phaseFor(s)",
      "function setPhase(name)",
      "function setTerminal(status)",
      "resetProgress();",
      "setPhase(phaseFor(ev.step));",
      "setTerminal(ev.result.status);",
      "function showPage(idx)",
      "function renderScraped(r)",
      "renderScraped(r);",
      "function pvTab(which)",
      "function showLive(url)",
      "function urlChanged()"
    ],
    "step_phases": {
      "navigate": "browser",
      "observe": "reading",
      "extract": "verification",
      "extract_all": "verification"
    },
    "forbid": [
      "setInterval(",
      "setTimeout(",
      "$(\"examples\")",
      "built-in page"
    ]
  },
  "expect": {},
  "provenance": "M30 eval-first. Before the execution surface existed, this case failed with every required progress hook, accessible phase, reader-facing support label, concise four-item limitations summary, and compact guard copy missing. It rejects timer-based progression and pins the only permitted trace interpretation: navigate is browser/page work, observe is read-only page inspection, extract moves into verification, and state-changing executor steps remain action work. The existing stream-shows-every-step case continues to grade that the underlying trace itself is complete and honest. M35 owner amendment (2026-08-22): the `$(\"examples\")` chip row and the built-in/real-site card tag are forbidden in the script (the eyebrow line is HTML; the rendered `ui-form` case pins it gone) \u2014 the per-site cards are the only example surface. Watched red against the page as it stood while it still carried BOTH forbidden strings \u2014 the state before the execution surface replaced the built-in page, which is what makes the assertion meaningful: `\"forbidden\": [\"$(\\\"examples\\\")\", \"built-in page\"]`. M31 added `extract_all`, which is verification work like `extract`; the adapter's mapping regex was widened to `[a-z_]+` in the same edit, because an action name with an underscore in it would otherwise have been invisible to this check. M40 owner amendment (2026-08-23): the trace gained a page view beside it \u2014 the screenshot the executor already took for the step being shown, and after the run the extractions with the page text each value was read from. It is pinned here rather than described because it is the surface a reviewer uses to check an answer against the page, and because the same `forbid` list already rules out the timer-driven version of it. Watched red by construction against the pre-M40 page: none of the seven new fragments or three new script fragments appear in `git show HEAD:src/browser/server.py`, verified by grep before the case was widened. M40 owner amendment 2 (2026-08-23): the panel gained a LIVE PAGE tab \u2014 the real page, fetched through `/view` and framed sandboxed so it renders and scrolls \u2014 because a screenshot answers 'what did the browser see' and not 'what does this page actually say'. It loads as soon as there is a URL, before any run, which is why `statusline` is pinned too: opening the panel to show a page used to leave the status chip reading `running` with the spinner going over an empty trace, which is the same class of lie this case's `forbid` list exists to prevent. **Sha citations corrected 2026-08-28 (T-R40).** The red-first evidence above was cited by commit sha, and none of those shas is reachable from any branch after the rebase onto `2e94bed` \u2014 `git branch -a --contains` returns nothing for `2a11142`, `e07ac07` OR `b7daac4`, the last of which is the replacement T-R40's own acceptance proposed. So re-pointing at it would have relocated the problem rather than fixed it, and that is why the acceptance's SECOND branch is taken instead: the red-first is described by its STATE, not named by a commit. The observations themselves are unchanged and are what a reader needs; a sha that `git gc` can take with it was never the evidence, only a pointer to it.",
  "triage": {
    "class": "ui-semantics",
    "red_first": "T-M32-1: on main@cc63ac6, data-phase=reading was absent and the parsed phase mapping had no observe entry, so observe fell through to action. The six-column grid assertion was then watched red independently against the old five-column declaration."
  }
}
