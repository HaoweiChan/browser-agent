{
  "id": "smoke-stream-takes-the-run-slot",
  "task": "browser",
  "suites": [
    "fast",
    "invariant"
  ],
  "input": {
    "kind": "smoke-guard"
  },
  "expect": {
    "reaches": "launching",
    "refused_with": "error",
    "page_branch": "String(ev.error || \"\").startsWith(\"busy\")",
    "disconnect_releases_within_s": 5
  },
  "provenance": "PR (M40) round 2 cold review, task T-M40-1. `/smoke/stream` launches a real Chromium \u2014 the same resource a run launches, on a PaaS container sized for exactly one \u2014 and on this base it took nothing at all: no `SEM`, no check. Two tabs on the Browser check button launched two Chromiums, a `POST /tasks` inside a check's ~15s window launched the second, and `/readyz` answered `{ready: true, busy: false, active_run_id: null}` the whole time, because it reads `SEM` and the smoke path never touched it. The second Chromium is the OOM the single run slot exists to prevent, and it lands on the agent's record. Watched red against the unguarded base with all five: `first_stream_never_launched` absent (it did launch), `second_stream_launched_a_second_browser: [start, launching, error]`, `second_stream_not_refused`, `slot_not_held_while_smoke_runs: false`, `readyz_ready_while_a_browser_is_up: {ready: true, busy: false, ...}`, `slot_not_held_at_launching: [start, launching]`.\n\nGraded through the real endpoint on the server's own loop, because the reported repro is two concurrent `curl -N`. No Chromium is launched and no timing margin is derived, because there is no race to leave margin for: playwright's entry point is swapped for a stub that parks at `launch()` on a `threading.Event` this case sets once the concurrent assertions are made, so the held window is exactly as long as the checks take (~20ms) instead of a `hold_seconds` sleep like `readyz-tracks-the-run-slot` needs. The stub caps its own park at 10s so a wiring mistake fails loudly rather than hanging the suite (CLAUDE.md rule 4).\n\nThe first assertion is the adversarial one: a guard that refuses EVERY browser check satisfies every other line here, so the case requires stream one to reach `launching` and only stream two to be refused. The last two are the leak: a semaphore taken and not returned bricks the service permanently, which is strictly worse than the bug being fixed. The error path is covered through HTTP (the stub raises where the real launch would, and `/readyz` must be idle after), and the client-went-away path in-process \u2014 `smoke_events()` driven to `launching`, then `aclose()`, which is the `GeneratorExit` a closed tab produces once Starlette cancels the stream task.\n\nPR #45 R1 strengthened the refusal assertion. It used to require only that the refusal event's `error` CONTAIN \"busy\" somewhere, while the console branch it exists to protect is a PREFIX test \u2014 so rewording the server message to \"the single run slot is busy \u2014 \" left every assertion green with the panel rendering \"chromium failed\" for a refusal in which no Chromium was launched, which is this case's own stated defect. The prefix is now parsed out of `expect.page_branch` and the observed `error` must satisfy it; the same `page_branch` is still required verbatim in `S.PAGE`, so the server's string and the page's predicate are pinned to one another rather than to two independent spellings of \"busy\". Watched red against exactly that mutant: `refusal_does_not_satisfy_the_page_busy_branch: {prefix: \"busy\", events: [... \"the single run slot is busy \u2014 a browser check is running. No browser was launched; try again in a moment.\"]}`. T-R84 (P1) closed here 2026-08-28. The `/readyz` `reason` was only NEGATIVELY asserted \u2014 non-empty and free of the substring `\"None\"` \u2014 so a regression to a bare `\"a run is executing\"` with no id interpolated passed both halves and still sent an operator hunting a run that was never printed, which is the defect server.py's own comment says the wording exists to fix. A negative assertion cannot pin wording; it pins one way of getting the wording wrong. Now split by slot-holder, positively: with `active_run_id` null the browser check holds the slot and the reason must BE `\"a browser check is running\"` (this probe); with a run id it must CONTAIN that id (`readyz-tracks-the-run-slot`). Both watched red against the block's own repro mutant \u2014 `\"reason\": \"a run is executing\" if busy else None` \u2014 which the previous form passed green. **T-R82 (2026-08-28):** the case graded the GENERATOR contract \u2014 drive to `launching`, `aclose()`, assert the slot returns \u2014 and not the layer that produces the `GeneratorExit`. Starlette cancels its stream task when it sees `http.disconnect`; if that ever stops happening (a Starlette change, a proxy holding the socket open) the slot stays held with every assertion here still green, and a leaked slot BRICKS the service: `/readyz` says busy and every run queues behind a browser that is gone. A conjunct now disconnects a real HTTP client mid-stream \u2014 a raw socket, because urlopen's close is polite about a stream it has been reading and politeness is the one thing this is not testing, with `SO_LINGER 0` so the server sees an RST rather than a FIN (a closed laptop lid, strictly harder to notice). Bounded by the same `release` event the launch stub parks on, so there is no sleep to tune, and it costs 0.1s. Watched red by removing `smoke_events`'s `finally`: `slot_never_came_back_after_a_real_disconnect`. The first draft measured NOTHING while looking green in three of its four fields \u2014 the earlier half of this case had already set `release`, so the stub returned instantly and the slot was back before the socket read a byte; `release.clear()` is what makes the window real.",
  "triage": {
    "class": "resource-guard",
    "note": "The fix is a non-blocking try-acquire, not `async with SEM`: a browser check that queues behind a 60s run and then opens Chromium after the visitor has gone is a worse outcome than one that says busy immediately. `error` is the refusal event rather than a new `busy` event because the frontend's EventSource handler closes the stream on `done`/`error` only \u2014 a new event name would leave the spinner running forever, and the refusal text is what the panel shows. The console branch is graded as a substring of `PAGE` rather than by rendering it: `ui-rendered` is the machinery for the latter and it costs a browser launch.",
    "not_covered": "The success path's release is not graded \u2014 the stub raises where the real launch is, so only the exception path and the generator-close path run their `finally` here. Both share one `finally` with the success path, so the untested arm is the same three lines. Nothing here launches a real browser or proves the container survives one; `agent-launches-its-own-browser` is the case that opens Chromium."
  }
}
