# Support matrix — Task 1

**Report-assisted, human-declared** (see `docs/evals/evaluation-methodology.md`):
the latest `full`-suite eval report suggests a status; a human declares the
final status with a reason; README and the frontend render this same data.
A pass-rate does not threshold itself into "supported" — declaring is an
engineering-judgment act, and the reason column is the graded honesty evidence.

Entry shape (also served as JSON to the frontend):

```json
{
  "domain": "wikipedia.org",
  "task_type": "TC2 search-then-extract",
  "eval": "7/8",
  "declared_status": "unreliable",
  "reason": "Disambiguation pages still misroute the extraction step (case tc2-wiki-004)."
}
```

## Current matrix

Declared from the M6 baseline (`evals/report/20260817-124241-fast.json`, 65/65)
plus the M6 live runs; the M8 rows (quotes.toscrape.com, and limitations D5–D11)
rest on the M8 runs instead — `fast` 86/86 and `live` 9/9, 2026-08-20. Every status below rests on **stubbed plans** — the
fixture rows on offline fixtures, the live rows on hand-written plans against
real DOMs — so this table measures the resolver/executor/verifier path and
says nothing about planning quality on any domain, live or not. Empty cells are
shown, not hidden.

`books.toscrape.com` TC4 stays `—` rather than becoming a status: the case that
would fill it (`live-books-cheapest-travel`) is the only one needing a real
planner, and it has not been run.

`quotes.toscrape.com` is the M8 hostile domain and its TC1 cell is `unsupported`
on the strength of `live-quotes-js-role-tier-blind`: the run answers, reports
success, and answers with the pager. The page is not unreadable — the text tier
reaches its content exactly (`live-quotes-js-text-tier-reaches`) — it is
*unplannable*, because the observation the planner is given contains none of it.
D7 below has the measurements.

| Domain | TC1 | TC2 | TC3 | TC4 | TC5 |
|--------|-----|-----|-----|-----|-----|
| shop fixture | supported | supported | supported | supported | — |
| forms fixture | — | — | — | — | supported |
| hello fixture | supported | — | — | — | — |
| books.toscrape.com (live) | — | — | unreliable | — | — |
| news.ycombinator.com (live) | unreliable | — | — | — | — |
| openlibrary.org (live) | unreliable | unsupported | — | — | — |
| quotes.toscrape.com (live) | unsupported | — | — | — | — |
| lamp-spec fixture | supported | — | — | — | — |
| wikipedia.org | — | — | — | — | — |

Statuses: `supported` / `unreliable` / `unsupported` / `—` (not yet evaluated).
Unsupported and unreliable rows must cite a concrete failing case id.

## Declared limitations (M9 cost/model ablation)

| Limitation | Evidence | Status |
|---|---|---|
| **D12** — there is still no cost/latency/correctness number for any model, including the default one this system runs on | `docs/analysis.md` §9 ships the ablation section with the tradeoff table **empty**, and the entire measured LLM spend of this project remains three deployed runs — $0.0029 (`09b21b3a`), $0.006474 (`cd7121fc`) and $0.005454 (`734d3d1f`), none of them a comparison. §2 of `docs/analysis.md` still names only the first two; it was written before the third and its refresh is M10's (spec-drift audit, M9). The mechanism that will fill it is merged here (`ABLATION_MODELS`, the allowlisted `model` field, and the driver at `evals/ablation.py`); see D14 for which model it will NOT measure, but the paid runs happen on the deployment and the deployment cannot vary the model until this merges and redeploys | `unsupported`, and **deliberately empty rather than estimated**. The emptiness is graded, not promised: `analysis-ablation-table-not-estimated` (invariant-tagged) fails if the section declares itself pending and contains any data row, and once it names a committed report every cell must equal what the driver's own formatter derives from that report. Watched red both ways — a fabricated row turns it red, and ablating either of the two branches that decide whether numbers may exist turns it red. Committed variants across four review rounds — the list lives in `evals/adversarial/analysis-ablation-table-not-estimated.json` and is deliberately not counted in prose, because three documents carried three different totals for it, all stale within one PR (PR #15, R19). Two rules, and only one of them is complete. **Structural (complete): the section contains exactly one table, the graded one** — so a results *table* cannot be smuggled in however its cells are spelled, and this is what forced §9's own "where the pieces live" table into a bullet list. **Figure-shaped (incomplete, and declared so): no currency amount, so-many-of-five score, bare cents/percent figure, or latency word beside a number** on any other line, in any markdown. That list has been widened in four review rounds — model ids it did not list (R2), markdown syntaxes it did not enumerate (R11), table respellings (R17), then prose respellings using R17's own spellings (R21) — which is the evidence that an enumeration over natural language never closes. **A determined author can still write a per-model estimate as an ordinary English sentence and this guard will not catch it.** That is stated here and in §9 rather than left as a "syntax-blind" claim the code cannot honour; what is closed is every shape an estimate has actually been smuggled in as, plus every table. §9 itself now publishes no figures at all, which is what makes the whole thing checkable. **The document-wide duplicate-table sweep was, for one round, never applied to the real file** — the live call omitted an optional `document` argument that silently fell back to the section, so it graded only synthetic inputs (PR #15, R12). That parameter is now required, and the committed file and every whole-document variant share one entry point so a variant can pin the scope the real check uses. Four bounds on what the table will license when it exists, all stated before the run so they cannot be chosen to fit it: **one run per (model, task) cell**, so a 3/5-vs-2/5 split is not evidence of a better model; **five tasks on one prompt**, so it measures this system with that model and not the model; **p95 over five runs is the slowest of five**, which is why the decision rule's latency bar is 2x and not something finer; and the correctness column compares with `verifier.answers_match`, which is exact outside numbers — a correct extraction with a wider span ("Price $39.00" for "$39.00") scores incorrect. That last one understates every model equally, including the default, so it cannot flatter a challenger. All five M9 cases are green in the committed runs `evals/report/20260820-162200-fast.json` (91/91) and `evals/report/20260820-162043-invariant.json` (27/27), taken after PR #15 review round 4. `specs/decisions/ADR-010-m9-model-ablation.md` Decisions 3, 4, 5 and 8 |
| **D15** — a `failure:env` run can mean "this model cannot plan", so the ablation's most likely finding used to abort the sweep instead of being recorded | `agent.py` returns `failure:env` for a planner exception (including `PlanError` from `parse_plan` — the model answered with prose instead of a JSON array), for a replan that raised, and for budget exhaustion, as well as for a missing key. Reproduced end to end on loopback: a planner raising `PlanError` yields `status: failure:env, reason: "planner failed: planner returned non-plan output: ... I'd be happy to help you with that!"`. The driver aborted the whole sweep on any `failure:env`, discarding every paid run so far and writing no report (PR #15, R1) | `unreliable`, and the taxonomy is the ceiling rather than the driver. **A run is published only if its terminal status is on an allowlist of statuses that measure the MODEL** — `success` and the four failure classes that require a plan to have run, plus `failure:env` with a model-fault reason and `failure:task` that is not our own URL refusal. Everything else aborts, including `failure:nav` (a page that will not load is reachability, not planning) and any class a later milestone adds. The allowlist replaced a denylist that was extended three times, each time by a reviewer: round 3 found that a PRE-PLAN nav failure — before any planner call, always $0.00 — was published as a model scoring 0/5, and task 5 of the set is live (PR #15, R14). Within `failure:env` the split is made in `agent.py` by catching `PlanError` — the type `parse_plan` raises, and the only thing meaning "the response arrived and was not a plan" — separately from every other exception: `planner rejected:` / `replanner rejected:` / `budget exhausted:` score an incorrect cell, and `planner failed:` (a call that failed at the provider or on the wire) aborts. Each row carries its `reason`. **The first repair of this row got it backwards** and is the reason the mechanism moved: it matched `planner failed:`, which `agent.py` writes for every planner exception, so an OpenRouter 402 would have been published as a model scoring 0/5 (PR #15, R9). Prefixes at all rather than a proper class because "the model cannot plan" has no entry in `docs/evals/failure-taxonomy.md`, and adding one changes what `specs/000-invariants.md` pins; that is the named upgrade path, not something a cost milestone does in passing. What makes the prefixes safe is that they are authored where the type is known and only named downstream. Graded by `ablation-env-failure-is-a-result`: a truth table, a real `run_one` whose planner emits prose (scored), and a real `run_one` whose planner raises HTTP 402 (aborts) — each watched red |
| **D17** — one ablation task failed on every model tested, with one signature | the M9 ablation ran four planners over five tasks (`evals/report/20260821-004617-ablation.json`); `tc3-shop-detail-nav` came back `failure:locate` for **all four**, every time, at the same resolver error on the same anchor — `no tier resolved` for a `near:` target whose anchor is a field label on a product detail page. It reproduced across every aborted sweep as well, so it is not a one-run artifact. Four independently-trained planners emitting one failure signature on one page is not four weak models | `unreliable`, and **a finding the ablation surfaced about the system, not about the models** — it is excluded from any model-quality reading and counted against none of them. The boundary is in `near:` (ADR-006), which resolves an anchor to the nearest element in document order and has no tier that reaches a value sitting in a labelled field on that page's shape. Not fixed here: M9 is a cost milestone, and a resolver change belongs behind its own eval case and its own red-first loop. Named as the upgrade path rather than folded into a cost decision |
| **D18** — five ablation sweeps aborted on transport, and the cause is not established | **Measured.** Five sweeps aborted before one completed, every abort on the client's transport or a run outliving its wall-clock budget, never on a model: connection timeouts mid-poll, a refused connect on the next submit, and twice a run past its completion budget. `/healthz` answered in about a fifth of a second either side of every one. Per-run wall clock climbed across a sweep — one model took seconds on an early task and minutes on a later one, against tens of seconds for that task on every other model. From the Zeabur dashboard over the same window: **peak memory ~175 MB against 8 GB, falling back to a ~55 MB baseline between runs; peak CPU ~18-20% of 2 vCPU, usually under 10%; no container restart** — one deployment, dating from the PR #15 merge, spans all five sweeps. Known configuration, not inference: one uvicorn worker, `asyncio.Semaphore(1)`, one Chromium launched and closed per run, teardown in a `finally` inside the Playwright context that all 12 returns in the run body pass through. **Unknown: the causal mechanism.** Nothing above reaches a cause, and this row has now asserted two that the evidence did not support. The first was a browser-teardown window, refuted by the code — `server.py` assigns the terminal record *inside* `async with SEM`, after `run_task` has returned and therefore after the browser is closed, so the window did not exist. The second was this row's own title: *the deployment cannot sustain a twenty-run sweep* implies capacity, and the dashboard refutes it — the sweep ran with roughly 98% of memory and 80% of CPU idle and no restart. Both are recorded rather than quietly edited, because being wrong twice in the same direction is the finding: a plausible mechanism is the thing this repository treats as a defect when the evidence does not reach it, and no eval case could have caught either, since nothing in the suite asserts anything about the deployment's runtime state. **No third mechanism is offered here.** What the evidence supports is a negative — the observed workload left large CPU and memory headroom — and one caveat on that negative: Zeabur samples coarsely, so a sub-minute peak could be missed. The *shape* (return to baseline, no upward drift) survives coarse sampling; the absolute peak does not. Remaining candidate areas, unranked and untested: request-path or event-loop blocking inside the single worker, Zeabur ingress behaviour, and the ablation client's own connection and timeout handling. | `unreliable`, **worked around without being diagnosed**, and the workaround is bounded and says so. Three driver constants moved, each from a measurement: socket budget, settle gap between runs, per-run completion budget. A retry was added and narrowed to **connection-phase failures only** — `urllib` wraps a failed connect in `URLError`, nothing that never reached the server was billed, so a resend cannot double-spend; a read timeout after the request landed raises a bare `TimeoutError` and is deliberately **not** retried, because that run may already be executing and resubmitting would spend twice and record once. ADR-010 Decision 13's rule is untouched: an exhausted retry still aborts the sweep and still writes no report. The spend from the five aborted sweeps is real and lives only on stderr, per CLAUDE.md rule 4 — a partial sweep is not a result. ADR-010 Decision 19 |
| **D20** — demo readiness is demonstrated for interview-shaped workloads, and only those | `evals/soak.py`, two back-to-back sequences of five sequential tasks against the live deployment (`evals/report/20260821-020840-soak.json`, plus a single-sequence run at `...-020709-soak.json`): **10/10 completed with zero infrastructure failures** — no transport error in any phase, no stall, no lost result. Client wall clock 4.68-13.53s per task with **no upward drift across the ten runs** (5.02, 7.14, 13.53, 4.68, 7.18, 5.06, 6.82, 11.27, 7.14, 7.15), against the rising per-run clock D18 records for a twenty-run sweep. Task set covers the paths the system has: fixture extract, fill+click+extract, form submission with postcondition verification, a multi-step sort, and a live site nobody here authored. Spend $0.0622 | `supported` **for this workload shape, and demonstrated rather than argued** — which is the narrow claim and deliberately so. What the soak shows is that the difference between it and D18's five aborted sweeps is **workload shape, not capacity**: same container, same 2 vCPU / 8 GB, ten clean runs. It does **not** retro-diagnose D18, and no mechanism is inferred from the contrast. Three caveats stated rather than buried. (1) These runs were served by the **deployed** build, whose default is still `anthropic/claude-sonnet-4.5` — PR #19's move to `openai/gpt-5.6-luna` is not merged, so the latency and correctness figures belong to the old default; the infrastructure conclusion is about the deployment either way. (2) `/readyz` returned **404 on every probe** because it is not deployed yet, so the readiness-transition half of this evidence is pending a redeploy — the soak records the 404 rather than omitting the column. (3) 8/10 correct, and **both incorrect runs are the same `near:` capability boundary as D17** (`no tier resolved` on a `near:` anchor), not an infrastructure event — correctness is context here, not the criterion. ADR-011 |
| **D19** — the section-9 estimate guard is complete only inside section 9 | inside §9 the structural rule holds — the section contains exactly one table, so any other table row is a results row however its cells are spelled. Outside §9 only an **exact copy** of the graded table is refused, document-wide. Measured, not assumed: an exact copy pasted into §8 or appended at the end of `docs/analysis.md` turns `analysis-ablation-table-not-estimated` red; a per-model table with a different header, in another section, does not | `unsupported`, declared rather than claimed shut. Same shape and same reason as the prose residual in D12: this guard has been widened four times across five review rounds, each widening prompted by a reviewer walking around the last one, and an enumeration over natural language never closes. A stated boundary beats a net that claims more than it catches. ADR-010 Decision 20 |
| **D16** — a planning call the provider billed for and that returned prose used to be recorded at zero cost | `live_planner` built its `usage` dict *after* `parse_plan`, so a completion charged by OpenRouter that failed to parse discarded its own cost, and `agent.py` accumulated tokens only on the success path. Measured against a stubbed provider response reporting 1,873 tokens / $0.0012: the committed row read `llm_tokens: 0, llm_usd: 0.0` (PR #15, R10). Four documents claimed "with their real cost and tokens still counted" | `supported` — `PlanError` carries the usage of the call that produced it, `live_planner` builds usage before parsing, and `agent.py` charges it to the model that emitted the prose. **Extended at round 3 (R15):** `content` was extracted one line above the guarded block, so `content: null` — what a reasoning model returns on `finish_reason: length`, and the ceiling model defaults to high reasoning effort — raised AttributeError, was classed as a transport failure, aborted the sweep and discarded $0.0064 the provider had billed. The whole of the response handling is inside the guard now. **Narrowed again at round 4 (R18):** that guard was a bare `except Exception -> PlanError`, so an envelope the planner could not read — no `choices` key, a string `error` field, `choices: null`, a gateway page — was classed as the model's fault and would have published an outage as a 0/5 cell, the R9 defect with its polarity flipped. Default-deny now applies at the response boundary too: only a response positively recognised as "the model answered, and the answer is not a plan" is scored, and seven envelope shapes plus three no-plan shapes are pinned as probes of the real `live_planner`. `choices: []` moved from the scored side to the aborting side, reversing a round-3 pin, because an envelope with no completion carries nothing to judge. Pinned at the production path, not at a hand-built exception: `ablation-env-failure-is-a-result` drives `live_planner` itself against a stubbed provider response and fails if the raised error drops the billed usage. That probe exists because the discrimination pass found the first version of this fix unpinned — reverting `live_planner` left the case green, since the end-to-end half constructs its own `PlanError`. Consequence had it shipped: a cheap model emitting prose on 3 of 5 tasks publishes ~40% of its true spend, into the column ADR-010's decision rule compares across models |
| **D13** — the public submit endpoint now takes a caller-chosen planner model | `POST /tasks` accepts an optional `model` field so the ablation can vary the planner from outside the deployment. The endpoint is public and unauthenticated, and the run budgets (30 actions, 100k tokens) count tokens, not price, so they cannot see a caller choosing an expensive model. The endpoint gates on `ALLOWED_MODELS` (the four ablated ids **plus** the incumbent default, which is priced out of the comparison but must stay reachable by name); the driver gates on the narrower `ABLATION_MODELS`. Three cases: `gateway-model-not-allowlisted` (five refusals, including an allowlisted id with a trailing space and the empty string), `gateway-model-reaches-planner` (the no-model default, the default by explicit name, and the four ablation ids, checked at the planner factory rather than at the HTTP response, which cannot tell an honoured parameter from a dropped one, pinned against the frozen OpenRouter snapshot, and grading the owner's price ceiling in both directions), `ablation-preflight-refuses-old-build` (the driver must refuse to spend against a deployment still serving a build that drops the field, and the probe run's freeness is measured — `unsupported`, $0.00, zero planner calls — not proxied), and `ablation-env-failure-is-a-result` (which also pins that a run attributed to a different model than the one submitted aborts: `RunResult` now echoes its planner model, so per-row attribution no longer rests on a single preflight at the start of a 20-run sweep) | `unreliable`, bounded in the safe direction — anything not on `ALLOWED_MODELS` is refused 422 before a run exists, and **every other model on that list is cheaper per token than the default**, so the field cannot point the key at a model more expensive per token than the one this deployment already runs. Deliberately the narrow claim: per-token cheaper does not bound per-*run* cost, since a model that plans worse replans more and two extra planning calls at a third of the price is a wash (M9 cold review corrected the loose version, which claimed a per-run bound). The real exposure is unchanged and older: there is still no per-IP rate limit (declared at M5, backlog), so a caller who loops can still spend repeatedly whichever model they pick. ADR-010 Decision 7 |
| **D14** — the model the system actually runs on is not in the ablation, and no committed run measures it | the owner set a price ceiling on 2026-08-20 — "the most expensive one I can accept is `deepseek/deepseek-v4-pro`" — and ruled that **the ceiling is the model, not the number**: that model stays in the set at whatever it lists for. `anthropic/claude-sonnet-4.5`, the incumbent `DEFAULT_MODEL`, is over it by a wide margin on both prompt and completion. Prices are quoted nowhere in prose or code; the only copy is `evals/labels/openrouter-models-20260820.json`, and the effective ceiling is derived from that file's entry for the ceiling model. That is not tidiness: the ceiling model's own price moved ~11% **inside one working session** (PR #15, R16), which the frozen snapshot caught and four hand-copied quotations did not. The four ablated ids are all at or under the ceiling and all OpenRouter-leaderboard top-10 for the week ending 2026-08-19 | `unsupported`, and a **reframing rather than a gap** — if the owner will not pay above the ceiling then the incumbent cannot stay the default on cost grounds alone, with or without a measurement, so M9's question becomes "which affordable popular model replaces it, and what does the cheapest one cost in correctness?" (ADR-010 Decisions 1, 2b, 5, 6). The consequence is stated rather than papered over: **the table will have four rows and none of them is the model in production today**, so no claim of the form "the cheap model is as good as what we had" is available from this data in either direction. **Resolved 2026-08-21 by the ablation itself** (ADR-010 Decision 18): the run happened, every candidate tied on correctness, Decision 5's pre-committed rule fell to its cost tie-breaker, and `openai/gpt-5.6-luna` replaced `anthropic/claude-sonnet-4.5` as `DEFAULT_MODEL`. The row stays because its *claim* stays true of the data: no cell measured the superseded incumbent, so nothing here supports "the cheap model is as good as what we had" in either direction — the comparison is among the four affordable candidates only. What changed is the surrounding arrangement. The endpoint **no longer** accepts the superseded incumbent by name: keeping it reachable was justified while it was still the default and merely priced out of the comparison, and once a replacement was chosen it would only have left a public unauthenticated endpoint able to spend on the model this system had just stopped paying for. Its id stays in the frozen snapshot as the evidence for the exclusion. The guard inverted with the decision: it used to require the incumbent to be **over** the ceiling and now requires the default to be a model the ablation **measured** — under the ceiling by construction — while still requiring the superseded incumbent to be present in the snapshot and still above the bar. Watched red four ways. Selection note: the leaderboard states of itself that it *"measure[s] adoption, not quality"* and does *"not rank models by accuracy, reasoning ability, or benchmark performance"* — popularity is the owner's criterion for what to test, never evidence about what is good |

## Declared limitations (M8 mutation & hostility)

| Limitation | Evidence | Status |
|---|---|---|
| **D5** — positional targeting (`index`) is order-fragile *and* silent: a re-ordered page returns a different entity, reported as success | `l4-shop-element-reordered` — the element-reordered mutation reverses the catalogue rows, `{role: listitem, index: 2}` moves from the Meridian row to the Aurora Pro row, and the run answers "Aurora Desk Lamp Pro $59.00" to a question about the Meridian Wall Clock. `grounded`, `not_a_dump` and BOTH identity-anchor checks pass, because the anchor string is on the page whichever row was read. Committed with the wrong answer as its expectation, so the case is a pin, not a pass | `unreliable` — declared since M5 for live table extraction (`live-books-travel-price` index 5, `live-hn-item1-title` index 11) and now executable rather than predicted. The mitigation exists and is not enforced: the same task written with `near` survives the same mutation (`l4-shop-element-reordered-near`, tier `structural`), and nothing makes a planner prefer it. `relocation_candidates` deliberately returns no rungs for a target that names no string, so there is nothing to recover with either |
| **D6** — content that is merely *late* is indistinguishable from content that is absent | fixture: `l4-shop-render-delayed` (the render-delayed mutation removes the catalogue at DOMContentLoaded and restores it 10s later) → `failure:locate`. Live twin: `live-quotes-js-delayed-empty` — quotes.toscrape.com/js-delayed renders ~10s late, and after `navigate()`'s bounded 2s wait for `load` the body is 78 characters against 1,499 on the immediate variant (measured 2026-08-19) | `unsupported`, in the safe direction — `resolve()` counts matches exactly once with no wait of any kind, and the relocation ladder re-observes into the same empty page, so the run stops loudly instead of answering. **Deliberately not fixed**: a bounded wait inside `resolve()` would make every legitimately-absent element pay it, and this suite is full of cases whose point is that a tier finds nothing (every first rung of every L4 case). The honest upgrade is a wait on the FIRST tier only, with its own case and its own latency measurement |
| **D7** — a page that carries no ARIA roles on its content is invisible to the planner while remaining fully visible to the verifier | `live-quotes-js-role-tier-blind`: on quotes.toscrape.com/js, `page.inner_text('body')` is 1,499 characters containing all ten quotes, while `observe()` returns 11 elements of which zero are content — WebArea, heading, 5 chrome links, navigation, list, the pager listitem, contentinfo; `get_by_role('link')` counts 5, all chrome. The run picks the only plausible target the observation offers, `{role: listitem, index: 0}`, and reports success with "Next →" | `unsupported` — this is the M8 hostile-domain result, published as it ran. The fixture twin is the a11y-stripped mutation, which the agent *survives* (`l4-shop-a11y-stripped`) because there the plan is written against the visible text; the live case is harder precisely because a planner cannot write that plan from an observation that omits the text. Two named upgrades, neither done here: an observation that falls back to text nodes when a page exposes no content roles, and an L3 evidence-only LLM check that asks whether the answer answers the question |
| **D8, closed 2026-08-21** — the `fast` suite exceeded its own wall-clock budget | was 66.6-68.3s at M8 and **71.3-76.5s at M9** with five more cases (a reviewer measured 68.6s on another machine — observations, not a bound), against the 60s ceiling in `specs/decisions/ADR-002-performance-thresholds.md` Decision 4, of which 10.6s is `l4-shop-overlay-modal` spending a full Playwright click timeout. The declaration held for two milestones on the assumption that the rest was irreducible trend. Measured per call at M12 (`specs/decisions/ADR-013-fast-suite-wall-clock.md`), on the 67.0s pre-M9 suite: 42.2s is deliberate waiting at bounds the suite exists to exercise, 13.5s is real work, and **11.3s is per-case Playwright driver start + `chromium.launch` + `browser.close`** — 58 cold Chromiums for one suite. Merged with M9 and M9 stage 2: 60.51s at 97 cases (`evals/report/20260821-170854-fast.json`) | `supported` — the 11.3s of scaffolding is gone (one browser for the suite, a fresh BrowserContext per run, so cookie/storage isolation is unchanged; process isolation was not, which review caught — PR #20 R2, now `shared-browser-relaunches-when-dead`), the 42.2s of deliberate waiting is untouched, no production timeout moved and no case left the suite. The ceiling is now applied by `evals/run.py` to the run it just measured, and exits non-zero, instead of living in prose — a first attempt that graded the newest committed report was falsified in review because it could not go red in CI (PR #20 R1); `fast-wall-clock-budget` grades the ruling — that is the actual defect this row records, since the drift from 13s to 68s took six milestones and never turned one run red. The local ceiling was re-measured to 70s when the readiness case's deliberate 3.0s slot hold put the suite at 59.35-60.16s across seven runs — the acceptance criterion's second branch, taken only after a per-call sweep showed no waste left to remove (ADR-013 Decision 4). The ceiling is per-environment since its first CI contact: `main` ran `fast` in 89.62s on the ubuntu-latest runner against the same 60s nothing had ever checked there, so CI carries its own measured 80s (`EVAL_WALL_BUDGET_S`, four runs at 64.3-69.0s on the merged tree) and both are enforced; the parallel eval runner remains backlog and remains the only lever on the 42.2s |
| **D9** — the mutation-survival metric counted "matched its expectation", so a case expecting a loud failure counted as a survival | latent since M2 and unobservable until M8 wrote the first mutation case that expects a failure. measured directly over the 10 mutation cases: the old rule scores 10/10, the new one 8/10 (the committed suite reads 9/11) on a catalogue the agent survives 8 of | `supported` — `expect.mutation_survived: false` (eval_adapter) excludes a case from the numerator while keeping it in the denominator. Same family as the M3 rule that a mutation nobody had to relocate around is not a recovery: the denominator stays honest, the numerator stops flattering. The fix itself was unguarded until PR #12 R2 said so — reverting it left the suite at 84/84 — and is now pinned by `mutation-metrics-honesty`. **Corrected again in round 2 (R8)**: the survival term was still "matched its expectation", so a case that expected AND got a loud failure counted as a survival unless its author opted out by hand. Surviving now requires `status == "success"`, and `l4-shop-render-delayed` no longer carries the opt-out key at all — one case still needs it, the wrong-answer pin. No published figure moved: both non-survivors were already excluded |
| **D10** — "N by relocating" counted every rescue, including one that relocated nothing | reviewer finding (PR #12, R1). `l4-shop-overlay-modal` is rescued by the act-family replan: its four resolved tiers are all `role`, `budgets_spent.replans == 1`, and no tier ever changes — yet it was inside the published "6 by relocating", the count `specs/decisions/ADR-002-performance-thresholds.md` introduced (0/3 at M2) precisely to keep the flattering reading out. Both ladders write the same `retry_or_recovery: "recovery"` label, so the label cannot tell them apart | `supported` — the family is read from the failure class of the attempt a rescue supersedes (`locate` = relocation ladder, `act` = replan ladder), and the runner now prints `N recovered (K by relocating)`. Published figures moved from "6 by relocating" to **6 recovered, 5 by relocating**. Guarded by `mutation-metrics-honesty`, whose row 2 is exactly this shape. **Corrected again in round 2 (R7)**: every relocation rung wears the `recovery` label and supersedes the attempt before it, including the rungs that LOSE, so a run whose rungs all failed and whose replan saved it was still counted as relocated (measured: 1 before, 0 after, on shop.html?mut=overlay-modal with a role-less first target). A rescue is now a labelled attempt that succeeded |
| **D11** — `mutation_relocated` reads the recovery family from the immediately-superseded attempt only, so a real relocation is undercounted after a laundered rung | reviewer finding (PR #12, R15). `agent.py`'s relocation loop points each rung's `superseded_by` at the next one, so if rung 1 retargets at a new tier and then fails `act` — the class-laundering shape `live-ol-search-a11y-invisible` records — and rung 2 wins, the rescue's predecessor reads `act` and the run is not counted as relocated even though the tier demonstrably changed. No case in the suite produces the shape today (relocated 5 / recovered 6 reproduce exactly) | `unreliable`, **declared not fixed**, and in the non-flattering direction — it can only undercount relocation, never inflate it. Named at the call site with a `ponytail:` comment and its upgrade path (walk the supersede chain back to the failure that started the ladder, with a `mutation-metrics-honesty` row pinning the intended answer first). Fixing it in the round meant to close the PR would be speculative counter surgery on a shape nothing exercises |

## Declared limitations (M7 verifier accuracy)

| Limitation | Evidence | Status |
|---|---|---|
| **D1** — `not_a_dump` closes only the single-extraction dump shape; chunking the same dump across several extractions evades it entirely | record id *chunked-dump-cheapest* in `evals/labels/verifier-sample.jsonl`: same task and fixture as record *probe5-shop-listing-dump* ("which product is the cheapest, and what is its price?" on `shop.html`), but the plan chunks the sorted catalogue into four separate `{role: listitem, index: 0..3}` extractions instead of one whole-container extraction. Each extraction's own ratio is ~12%, far under `DUMP_RATIO=0.35`, so all four pass and the runtime verifier returns PASS on a page dump | `unreliable` — this is the largest bound on M7's gain. `verify()` never receives the task text, so no L1 check, present or plausible, can tell "list every product" from "which is cheapest" given structurally identical per-extraction evidence; closing it needs the task text at the `verify()` call site (a contract change: the verifier takes raw evidence only, by design) or ground-truth L2, absent at runtime (`specs/decisions/ADR-008-m7-verifier-accuracy.md` Decision 5) |
| **D2** — `not_a_dump`'s threshold is chrome-sensitive and thinly calibrated | the ratio is computed against the size of the real page the value was read from (`body_len`, since the M7.1 fix — case `verifier-dump-ratio-anchor-flip`; see D4 below for what it was computed against before), so the same dump on a page with more surrounding boilerplate (sidebar, breadcrumbs, pagination, footer) dilutes toward and under 0.35. Calibrated on exactly two positive examples, at ratios 0.4541 and 0.5231 — only ~0.10 of headroom above the 0.35 threshold (`specs/decisions/ADR-008-m7-verifier-accuracy.md` Decision 2); both are unsaturated (their evidence window is the whole real page, not a truncation of it), so the fix does not move either number | `unreliable`, declared — no fixture in the repo carries enough surrounding chrome to demonstrate the dilution, and a two-point calibration sample is thin; a heavier-chrome fixture would sharpen or move the threshold |
| **D3, closed 2026-08-19** — a correct answer can false-FAIL `not_a_dump` on a sparse page | was declared rather than guarded, on the grounds that no fixture in the repo was sparse enough to demonstrate it. Falsified inside the same PR that declared it: main added `src/browser/fixtures/slow-asset.html` (37 clean chars of body text) for its own navigation cases, and its correct answer — the page's `<h1>`, 23 clean chars — is 62% of the page. `not_a_dump` FAILed it, taking `nav-load-event-never-fires` and `nav-action-load-event-never-fires` (main's cases, same extraction) down with it, alongside a case written for exactly this shape, `verifier-sparse-page-not-a-dump`. All three watched red on the merged tree: `checks.not_a_dump: false` | `supported` — `MIN_PAGE_CHARS = 100` (`verifier.py`) restores the floor `MIN_EVIDENCE` used to guard (removed at M7, ADR-008 Decision 3, for lack of exactly this evidence) below which `not_a_dump` does not apply at all. 100 sits between the sparse fixture (37/39 chars) and the smallest confirmed real dump, record *probe5-shop-listing-dump* (195 chars) — every extraction the `fast` suite produces below 100 chars was checked directly, and only the sparse fixture has a ratio anywhere near `DUMP_RATIO` (next highest 0.17); no confirmed dump is smaller than 195 (`specs/decisions/ADR-008-m7-verifier-accuracy.md` Decision 7). Kept as a row rather than deleted — the declaration was correct on its date, and the lesson is the same as the M6 `near:` rows below: a "no case demonstrates this" limitation is a claim about the eval set's current coverage, not a proof the shape cannot occur |
| **D4** — on a page longer than `PAGE_TEXT_KEEP`, a record with no `body_len` reads `not_a_dump`'s denominator from the *stored evidence window* instead of the real page — the window is capped at `PAGE_TEXT_KEEP` and doubles when a distant identity anchor forces a second window onto it, so the ratio stops scaling with the real page: a value over ~700 clean characters reads as "a dump" regardless of true page size, and the SAME value on the SAME page can flip PASS/FAIL depending only on whether the plan happened to carry a distant anchor (case `verifier-dump-ratio-anchor-flip`, red before the M7.1 fix: a 776-char value on a 4,388-char page — window 2000, ratio 0.388, FAIL; the identical value/page with a distant anchor — window 3615, ratio 0.2147, PASS) | closed going forward: `agent.py` now records `body_len` (the real page length) on every extraction, and `verify()` prefers it. Still open for the frozen M7 hand-labeled sample (`evals/labels/verifier-sample.jsonl`), which predates the field and always takes the fallback: **6 of its 28 extractions have a saturated window (window length ≥ `PAGE_TEXT_KEEP`, max 3,560 characters)**, so those six ratios are judged against a truncated-or-doubled window rather than their page. None of the six cross `DUMP_RATIO` either way (all far under 0.35), so the pinned confusion matrix in `evals/adversarial/verifier-precision-recall.json` is unaffected — but that is this sample's luck, not a property of the fallback | `unreliable`, permanently — the fallback exists because the 25 frozen records must keep replaying byte-for-byte (`specs/decisions/ADR-008-m7-verifier-accuracy.md`), so this shape cannot be closed for them without re-capturing (and re-hand-labeling) the sample |
| Ten focused, wrong answers pass the runtime verifier untouched | `verifier-precision-recall`'s 10 pre-existing false positives: wrong field, wrong table row, unsorted "cheapest", a search filled but never executed, a form filled but never submitted, a near-miss "Pro" variant — 4 of the 10 on live sites (`evals/labels/verifier-sample.jsonl`) | `unreliable` — none of these are page dumps, so `not_a_dump` was never going to reach them; closing this needs ground truth (L2, absent at runtime) or an evidence-only LLM check (L3, absent by design) |

## Declared limitations (M6 live breadth)

| Limitation | Evidence | Status |
|---|---|---|
| Live TC1 on news.ycombinator.com and openlibrary.org is stub-verified only (still true, and now the *only* live qualification) | `live-hn-item1-title` and `live-ol-edition-title` pass with hand-written plans; the live planner has never been run against either domain | `unreliable` — the same stub-vs-planner asymmetry declared for books.toscrape.com. The resolver/executor/verifier path handles both real DOMs; planning quality on them is unmeasured until a `full` run |
| **Closed 2026-08-18** — openlibrary.org was unreachable for the last day of M6, and half of what looked like the outage was ours | the host recovered; `live-ol-search-a11y-invisible` went green on the first attempt (its first green ever), and `live-ol-edition-title` kept failing `nav` until the wait condition was fixed — the page loads in 4.4s under `domcontentloaded` with the expected heading present, and times out at 25s under `load` (ADR-007) | `supported` — the live suite is 6/6 and all three live domains now rest on runs against current code. Kept as a row rather than deleted: the declaration was correct on its date, and the lesson is that a case declared unreachable should be re-run rather than allowed to harden into a conclusion |
| **Closed 2026-08-18** — a page whose `load` event never fires is unreadable, though its content is complete | `nav-load-event-never-fires` (pre-plan hop) and `nav-action-load-event-never-fires` (the `navigate` step), both watched red; the second re-watched red *after* the first was fixed, since `run_task` has two `page.goto` calls and fixing one would have left the other green-but-broken | `supported` — both route through one `navigate()` helper: `domcontentloaded`, then a **bounded** 2s wait for `load`, swallowed on timeout. A healthy page pays nothing; a hanging one is read instead of blamed. `networkidle` was tried first and rejected on measurement: +34s on the fast suite, over the ADR-002 budget (ADR-007) |
| An observe case pointed at an incompatible fixture reads as a broken observer, not a bad subject | no case — `_run_observe_case` on `slow-asset.html` raises `TimeoutError` after 30.4s (measured in the #9 review). Marked `ponytail:` at the call site | `unreliable`, eval-harness only — **declared, not fixed**. The strict `load` wait is correct there and stays: production asks "is this page usable enough to act on?", observe ground truth asks "what does a fully settled page expose?", and collapsing the two would leak the production contract into the harness. What is wrong is the *budget*: it inherits Playwright's 30s default rather than one anyone chose, and a timeout surfaces as a traceback instead of a result. Upgrade path: keep strict `load`, give it an explicit 5–10s eval budget, and return a structured `eval_env` failure. No production path touches it and no existing case reaches it |
| **Closed 2026-08-18** — a best-effort screenshot could block a run for 30s per step | same two cases: 32s and 64s before the bound, 4.3s and 8.4s after | `supported` — `page.screenshot()` waits for fonts, which on a page whose `load` never fires runs to its 30s default, inside a `try/except` whose comment says failing there is acceptable. Best-effort and unbounded cannot both be true; it gets the same 2s budget as a postcondition. No case asserts it — it is a latency property and there is no timing-assertion mechanism here — so the evidence is the committed report, named in ADR-007 rather than implied |
| A control invisible to both the a11y tree and the light DOM cannot be located | `live-ol-search-a11y-invisible`: Open Library's homepage search field exposes zero searchbox/combobox/textbox roles and no visible `<input>` — it is mounted by JS behind a reveal interaction (hand probe, 2026-08-17) | `unsupported` — no locator tier in the contract can reach it, a hostility class no fixture represents. The **diagnosis** is now proven live: after two runs that failed for other reasons (`failure:act` before the M6 fix, `failure:nav` during the outage), the case went green on 2026-08-18 — `failure:locate`, diagnosis 1/1, on a real site nobody here authored. The control remains unreachable; what is supported is saying so correctly instead of fabricating |
| **Closed at M6** — relocation onto a non-actionable element launders the failure class | was: report 20260817-024235-live, diagnosis 0/1 — the unresolvable searchbox relocated to a text-tier match on the literal string "Search" and the `Locator.fill` error reported `failure:act` for a root cause of `locate`. Now: `relocate-fill-non-editable` (fixture twin, watched red at `failure:act`) | `supported` — the guard sits in the executor's fill path, not in the relocate rung, so a first-attempt plan that targets a non-fillable element is classified the same way. An element that cannot hold a value is the wrong element, which is `locate`; a readonly or disabled input is the right element in the wrong state and stays `act` |
| **Closed at M6** — unknown target-schema keys are silently dropped by the resolver | was: `live-hn-item1-submitter`'s `near:` resolving as bare `{role: link}` → `ResolveError: 39 matches`, ignored rather than rejected. Now: `resolver-unknown-target-key` | `supported` — the five keys in `specs/001-browser-contract.md` are the whole schema and anything else stops the run as `failure:task`, naming the key |
| **Closed at M6** — `near:` was advertised in the contract and never implemented | `live-hn-item1-submitter` (39 links, the submitter picked by proximity to "points by"), `live-books-detail-upc` (a spec-table value picked by its label), `tc1-shop-price-near`, `near-excludes-its-own-anchor` | `supported` — document-order proximity, with the anchor's own element excluded and the refusal rules in the row below. It is the first mechanism to emit the `structural` tier in any run. Not a relocation rung: the ladder still climbs between `role` and `text` only |
| **Closed at M6, after a cold review** — `near:` returned confident wrong answers on three page shapes `shop.html` does not have | `near-anchor-substring` (anchor "Total" bound to "Subtotal" → the subtotal returned as the order total), `near-prefers-the-container` ("which row costs $24.50" → a different product at a different price), `near-equidistant-is-ambiguous` ("which product costs $24.50" → "Add to cart") | `supported` — anchors match exactly before loosely and keep only the deepest match; a candidate wrapping the anchor outranks every neighbour; and both kinds of ambiguity (two disjoint anchor matches, two equidistant candidates) are loud `locate` failures instead of tie-breaks. All three had reported `success` with a `PASS` verdict and nothing in the trace to suggest doubt |
| **Closed at M6** — a relocation rung dropped the `near` constraint it was recovering | `relocation-preserves-near`: a failed `{role, text, near}` relocated to `{role, name}` with proximity deleted, resolved a different element and reported success | `supported` — rungs carry `index` and `near` forward as intent. Found by the M6 spec-drift audit, not by the suite: `resolver-unknown-target-key` surviving one layer down |
| `near:` degenerates to first-candidate inside shadow DOM | no case — no fixture in the repo uses shadow DOM (M6 cold review) | `unsupported`, declared: Playwright's locators pierce open shadow roots and `document.querySelectorAll('*')` does not, so an anchor or candidate inside one scores `indexOf === -1`, every candidate ties, and the first wins — reported as `structural`, `success`. Not fixed because a fix with no fixture behind it is how the three defects above shipped |
| A fill into a contenteditable field is failed `act` after it worked | no case — no fixture has a contenteditable (M6 cold review) | `unreliable`, in the safe direction: `FILLABLE_JS` admits `isContentEditable`, then the readback calls `input_value()`, which throws on anything that is not a form control. Every modern rich-text composer is this shape |
| **Closed at M6** — a correct answer can be graded FAIL because the stored evidence is narrower than the page | `evidence-window-keeps-the-anchor`, found by `live-books-detail-upc`: right value, anchor present on the page, runtime check passed, eval verdict FAIL — the identity anchor was 2000+ characters from the value on a product page with a long description | `supported` — the evidence window now keeps a slice around the anchor as well as the value. A false FAIL is the safe direction and still a defect: in a report it is indistinguishable from a run that really did read the answer off the wrong entity's page. **M7 measured verifier precision on a 25-record hand-labeled sample** (`verifier-precision-recall`): precision 0.476, recall 0.909 — a floor reading of a deliberately adversarial sample, not general accuracy. Not from this mechanism, though: `identity_anchors` is vacuously true on every one of the 25 records (the replay passes no `expect`, so `anchors=[]` always), so this fix contributed nothing to that matrix — the matrix came from the mechanical L1 checks (`grounded`, `actions_verified`, ...) plus `not_a_dump` (`specs/decisions/ADR-008-m7-verifier-accuracy.md`, `docs/analysis.md` §5) |

## Declared limitations (M5)

| Limitation | Evidence | Status |
|---|---|---|
| Live TC3 on books.toscrape.com is `unreliable`, not `supported` | `live-books-travel-price` passes with a **stubbed** plan; the same task with the **real** planner failed on the deployment (run `cd7121fc`) | `unreliable` — the two results disagree, and the disagreement is the finding. With a hand-written plan the resolver/executor/verifier path handles the live DOM; with the live planner the run failed loudly at `failure:locate`. Declaring this `supported` on the strength of the stubbed case would be exactly the flattery this table exists to prevent |
| The live planner emits target roles that are not ARIA roles | deployed run `cd7121fc`: `{"role": "text", "name": null, "text": null}` → `ResolveError: no tier resolved` → `failure:locate` | `unreliable` — the planner reaches for a `text` "role" when it wants to read text. The agent fails loudly with the right class and no fabricated answer, and relocation correctly finds nothing to climb to (a target with neither name nor text has no rungs), so the cost is a failed run rather than a wrong one |
| Live extraction from a table is positional | `live-books-travel-price` targets `{role: cell, index: 5}` | `unreliable`, and **superseded at M6**: ARIA makes `rowheader` a subclass of `cell`, so label and value cells interleave and the price is reached by counting. The semantic form, `near:`, was advertised in the `specs/001-browser-contract.md` target schema and never implemented — the spec-drift declared at the freeze. It is implemented now and `live-books-detail-upc` reaches a table value by naming its label. This row stays because the *old* case still counts: a plan may still target a cell positionally, and nothing forces the semantic form |
| "The deployed URL was verified" has a shelf life, and the claim rarely carries one | deploy-smoke runs on push and on a 02:17 UTC cron. On 2026-08-18 the deployment served the M6 build and passed deploy-smoke at 14:02 UTC (run `32037391876`), then returned 404 on every path — edge with nothing bound behind it — by 18:04, with no push in between. The build was fine; the platform stopped serving it | `unreliable` — an outage starting between pushes is invisible for up to 24h, and today's cron will catch this one hours after it mattered. Not an argument for more polling: an argument that every claim about the deployed URL should carry the timestamp it was true at, which is why the run ids in this table do |
| The paid submit endpoint has no per-IP rate limit | M5 operator error: a polling loop submitted ~30 runs before anyone noticed; see `docs/analysis.md` §8a | `unsupported` by omission — per-IP limiting is backlog. Spend is bounded per *run* (30 actions, 100k tokens) but not per *caller*, so a careless loop (or an unfriendly one) can spend repeatedly. The concurrency semaphore serialises the damage but does not cap it |
| A redeploy destroys the run history | same incident: the runs that mattered most were unrecoverable because the process restarted | `unsupported` by design — `RUNS`/`STREAMS` are in-memory (analysis §4). Acceptable for a reviewer demo, and the reason the cost of that incident can only be given as an upper bound |
| Capability is about one hop deep | T9 held-out probe: 2 correct answers in 8 answer-seeking tasks. Second hops, aggregates and comparisons all failed. **Confirmed on the deployed M6 build**, run `734d3d1f` (2026-08-18): "find the cheapest book and tell me its exact price" was planned as `extract {"role": "article", "index": 0}` and returned the FIRST product, £45.17 instead of £23.21 — `success`, `PASS` | `unreliable` — the two successes were single-hop extractions whose answer string was already in the pre-plan observation. There is no compare/rank/filter step in the plan vocabulary, so "which is cheapest" is planned as "extract the whole list" |
| Values that exist only in an HTML attribute cannot be read | T9 probe #1 and #6: listing titles are CSS-truncated to `A Light in the ...`, with the full string only in `title=`/`alt=` | `unsupported` — extraction reads element text, so the run fails loudly (`failure:semantic` on the identity anchor, or an empty extraction) rather than returning the truncated string |
| With no start URL, the planner plans blind | T9 probe #7: step 1 came back `note: null`, `postcondition_ok: null` — no observation happened; the plan emitted `{"role": "article"}` and failed `locate` | `unreliable` — commit `ed1f774` ("the planner never plans blind") covers only the URL-supplied path, where there is a page to observe. Spec drift found by the probe, declared rather than fixed at the freeze |
| The keyword scope screen over-refuses on non-instrumental phrasing | `l5-refuse-login-contracted` (the row deliberately removed from it): "What is the sign in the shop window?" is refused | `unreliable`, in the safe direction — the words in "sign in the shop window" and "sign in to my account" are identical and only intent differs, which no keyword screen can separate. First eval evidence that the deferred LLM screening has a real job |
| An answer is never checked for being *responsive* to the question | T9 probe #5: a 20-book page dump was returned as the answer and rejected only by `grounded`, on a whitespace mismatch. Deployed run `734d3d1f` is the sharper case: the answer was a whole product tile, grounded and anchored, responsive to "which product" but not to "which is cheapest" | **Partially closed at M7** — `not_a_dump` (L1) now rejects an extracted value that reproduces most of its own evidence window (`verifier-catches-listing-dump`); a genuine multi-row list still passes, judged per-extraction rather than on the assembled answer (`verifier-list-rows-not-a-dump`). `unreliable` for everything else: a short, plausible, WRONG answer (wrong field, wrong row, near-miss variant) — including `734d3d1f`'s single product tile, which is far below `DUMP_RATIO` and was never in reach of this check — still sails through untouched, because nothing checks whether the answer answers the question, only whether it looks like the page was dumped. Only ground-truth L2 catches that, and a live run has none (`specs/decisions/ADR-008-m7-verifier-accuracy.md`, `docs/analysis.md` §5) |
| Planning quality unmeasured | every `fast` case stubs the planner at the module boundary | by design (cost-discipline); the `full` suite is the only measurement |
| Values in ARIA name-prohibited elements (`<dd>`, `<dt>`, `code`, `time`…) cannot be targeted **by role+name** | `observe-name-prohibited-roles`; the M6 counter-example is `near-excludes-its-own-anchor`, whose target `{role: cell, near: "UPC"}` carries no name at all | `unreliable`, corrected at M6 (was `unsupported`): the observation still blanks the unusable name, so role+name still cannot reach them and the agent still fails loudly rather than planning an unresolvable target. But `near:` reaches them by role + proximity, which is what a `<dt>`/`<dd>` pair is for. The limitation is narrower than it was declared |
| A locator broken at **both** reachable tiers is not recovered | `l4-shop-button-text-renamed` recovers text → role+name, `l4-recover-name-to-text` the reverse | `unreliable` — relocation has exactly two rungs to climb between: `role` and `text`. M6 made `structural` reachable (`near:`), but only when a *plan* asks for it — it is not a relocation rung, because a rung must be derivable from the failed target's own strings and a proximity anchor is a different element's text. So a mutation that kills both reachable tiers still has nowhere to go. `attrs` exists in the taxonomy and in no code path |
| An identity anchor can be satisfied by evidence from a discarded attempt | `verifier-superseded-not-a-loophole` pins the trace half only | `unreliable` — supersede hides a failed attempt from trace grading, but `evidence_text` is still built from every extraction, including the superseded one (ADR-005) |
| A replan is refused when the failed action changed nothing on the page | `replan-cannot-launder-noop-action` vs its benign twin `recovery-replan-postcondition` | `supported`, with a ceiling: the test is whole-body text equality, so an action whose only effect is visual or off-page reads as no change and its recovery is refused — loud, and in the safe direction |
| Relocation rung 1 ignores the target's role, and a rung is never guaranteed to be a different *element* | no case yet; found by cold review (ADR-005), sharpened by the M6 cold review | `unreliable` — a target `{role: link, text: X}` can relocate onto a same-named heading, a common listing-page shape. The M6 half: a failed `{text: "Search"}` relocates to `{role: button, name: "Search"}` — provably the same button — and still counts in `recovery_rungs`. It cannot create a false green, but every "N rungs tried" figure published here is an upper bound, not a count of distinct attempts |
| The progress stream is graded on the executor's hook, not on the SSE endpoint | `stream-shows-every-step` installs its own `on_step` and compares step ids only | `unreliable` — the gateway's own emitter and its copy semantics are untested, and a stream that stripped step *contents* would still pass (ADR-005) |
| Recovery is reported as a floor, not a rate | **M8:** 7 injected cases assert recovery; 13 rungs were tried to produce them (3 cases / 6 rungs at M5, when this row was written) | by design (ADR-003) — three cases is not a population. It stays `x/y` with the denominator printed until a live suite gives it one |
| Near-miss entity whose name contains the target's name | `trap-near-miss-entity` | caught only with external ground truth; a live run's runtime anchor passes it |
| Identity anchors on aggregate pages (listings, search results) | `trap-search-not-executed`, and now a live instance: deployed run `734d3d1f` anchored on `"Travel"` — the category, which every product on the listing satisfies — and the wrong product passed the check | `unreliable` — every candidate entity is in the page text, so the anchor certifies the wrong answer too. The larger of the two anchor holes, and it sits exactly where TC2/TC4 live. |
| The `fast` suite never runs observe → plan → resolve | all fixture cases inject a stub plan at the planner boundary | by design (cost-discipline), but it means the L4 self-maintenance passes are measured on plans hand-authored against the mutated DOM. Only the `full` suite closes this. |
| A click whose effect cannot be asserted | `postcondition-unverified-click` | `unsupported` by choice: the run fails loudly rather than reporting an unverified state change as success |
| ZH support is character-level, not planning-level | 6 ZH cases, all with stubbed plans | `unreliable` until a ZH case runs in the `full` suite |
