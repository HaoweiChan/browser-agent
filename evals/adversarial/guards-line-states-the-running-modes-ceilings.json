{
  "id": "guards-line-states-the-running-modes-ceilings",
  "task": "browser",
  "suites": [
    "fast",
    "invariant"
  ],
  "input": {
    "kind": "invariant",
    "check": "guards-line-matches-the-mode"
  },
  "expect": {},
  "provenance": "User report, 2026-08-30. The task form exposed implementation details instead of helping a visitor provide a usable URL: action and replan ceilings, estimated token cost, concurrency, and a deployment-level mode that cannot be changed here. The inline note now keeps only the actionable boundary: use a public webpage that does not require sign-in. Operational ceilings remain enforced by the executor and visible in run evidence; they do not need to occupy the input form.",
  "triage": {
    "class": "ui-exposes-internal-details",
    "red_first": "Watched red against the pre-fix page: missing_user_guidance reported the concise public-page instruction, while internal_details_in_task_form found actions, replans, one run at a time, deployment, and the guardrails label.",
    "note": "The check pins the product boundary and the absence of internal implementation language, not styling."
  }
}
