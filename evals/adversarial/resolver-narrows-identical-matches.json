{
  "id": "resolver-narrows-identical-matches",
  "task": "browser",
  "suites": [
    "fast"
  ],
  "level": "L2",
  "tc": "TC1",
  "input": {
    "task": "Who wrote the quote about thinking?",
    "fixture": "forum-thread.html",
    "stub_plan": [
      {
        "action": "extract",
        "target": {
          "text": "Marisol Vance"
        },
        "anchor": "Marisol Vance"
      }
    ]
  },
  "expect": {
    "status": "success",
    "answer": "Marisol Vance",
    "anchors": [
      "Marisol Vance"
    ],
    "trace_note_contains": "narrowed: document-order"
  },
  "provenance": "M38, rung 2. Deployment run e985e048 (quotes.toscrape.com): `extract {text: \"Albert Einstein\"}` resolved to 3 matches and the run died `ResolveError: 3 matches at tier text` — three elements that say the same thing, on a page whose answer was the string being matched. Rung 1 cannot help: the identity anchor of such a task IS the author name, so it matches the candidates themselves and identifies no single place (NEAREST_JS returns AMBIGUOUS, which the anchor rung swallows rather than raising — only an explicit `near` is loud about it). Watched red first: `status failure:locate`, reason `ambiguous-match: 3 matches at tier text for {'text': 'Marisol Vance'}` (no relocation rung: the text tier is already used and <small> carries no accessible name for the role+name rung).",
  "triage": {
    "class": "capability-gap",
    "what_this_grades": "the narrow case where picking the first match in document order cannot change the answer: every match has the same role AND the same reading, the plan carried no `index`, the action reads rather than acts, and the task asks for one thing. Take any conjunct away and this rung is a silent-wrong-answer generator, which is why each has its OWN case, red when and only when that conjunct is removed (PR #42 R2/R3 — this list used to name cases that pass for unrelated reasons): `resolver-refuses-different-readings` (same role, different readings, no anchor, first-in-document-order is the wrong user), `resolver-refuses-mixed-roles` (same reading, different roles), `resolver-refuses-plural-wording` and `resolver-refuses-plural-with-anchor` (a task asking for more than one, one case per rung), `resolver-refuses-narrowing-a-click` (a CLICK on two buttons with one accessible name and the same reading).",
    "not_covered": "matches that read the same today and diverge tomorrow — the check is over the rendered text at resolve time, not a promise about the page."
  }
}
