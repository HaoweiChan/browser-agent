{
  "id": "docs-numbers-are-derived",
  "task": "browser",
  "suites": [
    "fast",
    "invariant"
  ],
  "input": {
    "kind": "doc-counts",
    "readme_quotes": [
      "offline gate: {fast} cases, zero paid calls",
      "--suite live        # {live} cases",
      "it is {all} cases ({fast} of them in the offline gate)",
      "`live {live}/{live}` covers {live_sites} real sites."
    ],
    "where_it_stands": {
      "headline": "fast",
      "reports": {
        "fast": "20260826-083201-fast.json",
        "invariant": "20260826-083047-invariant.json",
        "live": "20260826-080559-live.json"
      }
    },
    "d8_retired_2026_08_21": "The d8 half graded support-matrix D8 while D8 was an OPEN wall-clock breach: it recomputed the published range from the reports the row cited and required the row to cite them. M12 (PR #20, ADR-013) then CLOSED D8 — the row no longer publishes a breach range, so there is nothing left for this half to recompute, and it was failing on a row that had legitimately changed rather than on a defect. Retired rather than re-pointed because the number it protected is now gated directly and better: `fast-wall-clock-budget` asserts the suite's own wall clock against the ceiling (60s local, 80s CI) and `wall-clock-probe-history-isolated` covers the history side, so a regression reddens the gate itself instead of a prose range. The reports it cited (20260821-215759/215923-fast.json) were measured pre-M12 and are now uncited; they are prune candidates under ADR-012. The README-counts half stays — nothing else derives those, and it reddens the moment a case is added.",
    "analysis_coverage": {
      "doc": "docs/analysis.md",
      "split_quote": "{total} distinct cases ({golden} golden + {adversarial} adversarial)",
      "section_start": "## 6. Coverage",
      "section_end": "## 7. What is not measured"
    },
    "criterion5": {
      "forbidden": [
        "criterion 5 is now green",
        "criterion 5 green offline",
        "A-freeze is achieved",
        "the inviolable property holds on the deployed"
      ],
      "required_in": {
        "specs/decisions/ADR-015-a-freeze.md": [
          "RED on the deployed build",
          "§8a-3"
        ],
        "specs/decisions/INDEX.md": [
          "RED on the deployed build"
        ]
      }
    },
    "headline_red_excludes_this_case_2026_08_22": "The headline-report-is-red check (PR #34 R4) ignores THIS case's own row in the report it inspects. Not a loophole: a superseded report showing this case red says only that the block was stale when it was taken, which is what fixing the block cures — and without the exclusion the guard has no fixed point, because once it goes red every subsequent report contains it failing and no green report can ever be produced to cite. Found by merging origin/main (M36) into task/M32, where every number had to move at once. Any OTHER failing case still disqualifies the report, which is the whole of what R4 asked for.",
    "describes_a_deleted_rule": [
      "nothing uncommitted in the tree",
      "the ceiling that maximum derives in the ADR's prose must be the ceiling",
      "must multiply the published maximum and land on the ceiling"
    ],
    "commands_publish_the_committed_ceiling": true,
    "ceiling_sweep_rows": [
      {
        "line": "python3 -m evals.run --suite fast   # wall clock <= 75s",
        "flags": true,
        "note": "the drift this exists for: a withdrawn ceiling re-typed into a gate command"
      },
      {
        "line": "python3 -m evals.run --suite invariant   # 100%, ceiling 15s",
        "flags": true,
        "note": "'ceiling' anchors it as surely as '<='"
      },
      {
        "line": "python3 -m evals.run --suite fast   # wall clock <= 90s",
        "flags": false,
        "note": "the committed ceiling, stated"
      },
      {
        "line": "python3 -m evals.run --suite fast   # wall clock <= 90 seconds",
        "flags": false,
        "note": "spelled out; the unit alternation is not decoration"
      },
      {
        "line": "python3 -m evals.run --suite fast   # ~71s on an M-series laptop",
        "flags": false,
        "note": "PR #40 R1: a TRUE timing note. The first version flagged it at 71s against 90 and blocked the commit while asserting the opposite of the truth"
      },
      {
        "line": "python3 -m evals.run --suite fast   # p95 2.2s per case",
        "flags": false,
        "note": "PR #40 R1, second shape: a per-case duration is not a suite ceiling"
      },
      {
        "line": "python3 -m evals.run --suite live   # 999s and still $0.00",
        "flags": false,
        "note": "a suite with no committed ceiling cannot publish a wrong one"
      },
      {
        "line": "~~python3 -m evals.run --suite fast   # wall clock <= 75s~~",
        "flags": false,
        "note": "struck: prompts/ is append-only and records the gate block as it stood on its date"
      },
      {
        "line": "python3 -m evals.run --suite fast   # wall clock <= the ceiling evals/run.py enforces",
        "flags": false,
        "note": "the dropped-literal form this PR moved CLAUDE.md to; it cannot go stale, so it cannot be wrong"
      }
    ],
    "analysis_section1": {
      "doc": "docs/analysis.md",
      "quotes": [
        "{actions} browser actions in a `fast` run; **{with_browser} of the {total}** `fast` cases drive a real Chromium end to end",
        "The\nremaining {remaining} are those refusals plus pure-code probes of a single"
      ],
      "why": "T-M39-5 / PR #44 R4: this pair sat three lines under a preamble saying every count in the section is current, read out of a report two trees old, while the split quote beside it was being kept honest. Recomputed from the headline report the same way, so it cannot drift again without going red."
    },
    "doc_quotes": [
      {
        "doc": "docs/analysis.md",
        "quotes": [
          "`live` is {live}/{live} across {live_sites} real sites"
        ]
      },
      {
        "doc": "specs/decisions/ADR-030-m41-sec10k-inspector-probe.md",
        "quotes": [
          "enforces\n{gt_conjuncts} conjuncts, each watched red before it was green"
        ]
      },
      {
        "doc": "docs/support-matrix.md",
        "quotes": [
          "`sec10k-ground-truth-endpoint-eval-only`, {gt_conjuncts} conjuncts each watched red"
        ]
      }
    ]
  },
  "expect": {
    "note": "every count in README equals what the runner loads, README's \"Where it stands\" block equals the reports it cites, and no tracked markdown file (prompts/ included, tasks/ excluded and why is in provenance) asserts criterion 5 green — outside a struck-through span or across bold/italic/link markdown, whitespace-normalized — while docs/analysis.md §8a-3's post-merge evidence says red, and no `--suite` gate command in ANY markdown file in the tree — `tasks/` and dot-directories included, `.git`/`.venv` excluded as machinery, a wider set than the criterion-5 scan in this same sentence — publishes a wall-clock ceiling, anchored to `<=`/`ceiling`/`budget`/`wall clock`, that is not the one `evals/run.py` commits for that suite, with `ceiling_sweep_rows` pinning both directions including the true timing notes that must stay green"
  },
  "m40_note": "M40 repointed the `fast` report: adding `ui-terminal-state-on-every-ending` moved the suite to 138 cases, and this check exists to refuse a Where-it-stands block recomputed out of a smaller tree. Worth recording because resizing a suite has a fixed point here: the first run of the resized suite cannot cite a green report of that suite, because none exists yet — the run that would produce one is red on this check. The branch went through that state (report 20260823-220001-fast.json, 137/138, cited while red) and out of it with `--suite fast --report` once the count claims agreed; the block now reproduces a green 138-case run exactly.",
  "provenance": "PR #21 review, R6 (MEDIUM) and R5 (MEDIUM) — the same class twice: a number in a document of record that this branch's own committed artifacts contradict. README said 91 fast / 101 total while the runner said 93 fast / 103 files (the edit added +5 for the new cases and carried a pre-existing -1 forward). D8 said `71.3-76.5s at M9 with five more cases` while the four fast reports committed in the same PR measure 79.01-79.75s with six more cases — understating a declared budget breach by ~3.4s, on the row M12 is keyed to. Neither number is re-typed here: the suite sizes come from `evals.run.load_cases`, the runner's own discovery, and the D8 range is recomputed from the `wall_seconds` of the reports the row cites. Watched red against the pre-fix documents: `readme_does_not_say: 'offline gate: 95 cases, zero paid calls'` and `d8_range: {the_cited_reports_show: '79.0-79.8s'}`. PR #23 R4 added the \"Where it stands\" half: the same commit that fixed the three graded count lines to 102/112 left the block beside them publishing `20260821-224446-fast.json` — `fast 98/98  invariant 30/30  59.4s`, a pre-merge run at a main-only sha — while the branch committed 102-result and 34-result reports at its own head. Ungraded prose next to graded prose drifts on the first merge. Watched red against that README: `readme_does_not_cite: 20260822-010408-fast.json` plus `readme_does_not_say: fast  102/102` / `invariant  34/34` / `59.6s`. Extended at M29 (`docs/analysis.md` §8a-3, `specs/decisions/ADR-015-a-freeze.md`): ADR-015's own header and criterion 5 asserted 'criterion 5 is now green offline, with live re-confirmation ... pending' and INDEX.md's one-liner said 'criterion 5 green offline with live re-confirmation pending redeploy' — both true when written, both falsified by the post-merge probe this milestone committed in the same document tree. Watched red against the pre-amendment files (git stash of this PR's ADR-015/INDEX.md edits): `asserts_criterion5_green: 'criterion 5 is now green'` (from both `specs/decisions/ADR-015-a-freeze.md` and `specs/decisions/INDEX.md`) and `asserts_criterion5_green: 'criterion 5 green offline'` (INDEX.md). The forbidden-phrase scan strips `~~struck~~` spans first, on purpose — ADR-015's own amendment convention (this ADR's citation of the plan's criterion-7 precedent) keeps the falsified original wording struck rather than deleted, and a naive substring scan would flag that preserved history forever; only prose asserting green OUTSIDE a strikethrough trips it. PR #28 R2 (MEDIUM): the criterion5 check was a hardcoded 5-file allowlist over 4 literal phrases, so a new document, or the SAME claim wrapped across a line, was invisible — demonstrated live by the orchestrator: `'criterion 5 green offline' in live_text` was False while `'criterion 5\\ngreen offline' in live_text` (the actual text of prompts/014-a-freeze.md:9-10) was True, and `required_in` read raw `text` rather than `live_text`, so a struck 'RED on the deployed build' would have satisfied the very requirement meant to force it live. Widened to every tracked-looking markdown file (`RUN_ROOT.rglob('*.md')`, dot-directories excluded) rather than documented as a ceiling, since the widen was a few lines (`pathlib.rglob`, stdlib); both texts are now whitespace-normalized before matching, and `required_in` reads the same normalized, struck-stripped text as `forbidden`. Watched red on the widened+normalized version against the pre-fix documents (git stash of the PR #29 repair commit): `asserts_criterion5_green: 'criterion 5 is now green'` from both `specs/decisions/ADR-015-a-freeze.md` and `specs/decisions/INDEX.md`, PLUS a new row the old substring matcher could not have produced — `asserts_criterion5_green: 'criterion 5 green offline'` from `prompts/014-a-freeze.md` (the line-wrapped instance), which the pre-widen 5-file allowlist never scanned at all. The remaining, declared ceiling: this is still a LITERAL (normalized) substring match, not semantic — a paraphrase (\"the mandatory gate is satisfied\", \"the deployment is clean\") is not caught, the same enumeration-over-natural-language ceiling D12/D19 (`docs/support-matrix.md`) already name for this repo's other literal-phrase guards. PR #28 R4/R5 (both MEDIUM, round 2): R4 — the widen+normalize fix still missed the SAME literal phrase split by markdown emphasis or a link (`**bold**`, `[text](url)`), which the declared 'paraphrase' ceiling did not name; live repro, `docs/_scratch_review_check.md` containing 'the deploy note says criterion 5 **green** offline now' scored `passed: True` under the whitespace-only normalizer. Fixed by unwrapping `[text](url)` -> `text` and `**bold**`/`*italic*` -> their text before matching. Deliberately NOT stripping underscore emphasis (`_word_`, `__word__`): this repo's own prose is full of backtick-spanned snake_case identifiers (`_run_doc_counts_case`, `RUN_ROOT`), and an underscore-strip would mangle those — a real underscore-emphasis evasion is the named residual ceiling now, not silently implied by a vague 'paraphrase' note. R5 — the `prompts/` exclusion put the exact file that demonstrated R2's line-wrap hole (`prompts/014-a-freeze.md`) permanently out of scan scope, so widening 'to every tracked markdown file' was cosmetic for that directory. Resolved by removing the exclusion and striking `prompts/014-a-freeze.md`'s two falsified spans (lines ~9-13 and ~87-90) in place, with a dated M29 pointer to ADR-015's amendment — the same struck-not-deleted convention this ADR itself uses, now stated as the general `prompts/` rule in `prompts/README.md`'s Append-only section rather than a rule that happened to fit this one case. `tasks/` stays excluded: tried narrowing to a backtick/quote strip first, per the reviewer's suggested shape, but it does not help — the false positive is unquoted prose (`tasks/TODO.md`'s own M29 spec block: '...carry no surviving claim that A-freeze is achieved', the forbidden phrase quoted as the thing that must NOT be true, not inside backticks). Residual risk stated rather than hidden: a real live claim written into `tasks/*.md` would not be caught by this guard; bounded because `tasks/` is a milestone tracker (CLAUDE.md layout), not one of the M29 spec's named documents of record, and both files are short enough for direct reviewer reading each milestone. Watched red, isolated from the tracked case file: OLD (whitespace-only) code against the bold-split scratch file -> `passed: True, wrong: {}`; NEW code, same file -> `passed: False, wrong: {'docs': [{'asserts_criterion5_green': 'criterion 5 green offline', 'doc': 'docs/_scratch_review_check.md'}]}`. Separately, `prompts/` back in scope before striking 014 -> `passed: False, wrong: {'docs': [{'asserts_criterion5_green': 'criterion 5 green offline', 'doc': 'prompts/014-a-freeze.md'}]}`; after striking -> green. T-R33 (tasks/TODO.md) is removed, not left open — its acceptance criterion (a cross-reference from 014 to the amendment) is exactly what the strikethrough edit added. M36 refresh: fast/invariant/live moved to 116/38/9 (eight new judge-boundary cases, all `fast`-tagged) and the \"Where it stands\" block now cites evals/report/20260822-175656-fast.json / -175708-invariant.json / -175548-live.json, all green, judge cost $0.0000 in every suite. PR #33 R1/R2 repair refresh: fast/all moved to 118/129 (two new cases -- judge-parse-response-strict-boolean, judge-injection-marker-forge-cannot-escape-fence, both fast-tagged), \"Where it stands\" now cites evals/report/20260822-182833-fast.json / -182724-invariant.json / -182855-live.json, all green, judge cost $0.0000 in every suite. M31 re-pointed the block at this milestone's own three runs: the suite grew by two cases and the replan count moved (the plan lint spends from the same counter the act ladder does), so the pre-M31 block would have published a stale total and a stale replan number side by side. Merged into M31: the two trees combined, so the block is re-pointed once more at a run of the merged tree. PR #35 R6: the `where_it_stands` half graded the block against whatever report the case named, and a report from a smaller tree still parses -- so `fast 132/132 invariant 51/51` recomputed correctly out of pre-M36 reports in a commit whose README said 133 cases four sections earlier, and no check could see the suite had grown. The cited report must now hold as many results as the suite has cases. PR #35 R14: the size check applied to every cited suite including `live`, so growing a NETWORK suite would have reddened the offline $0 gate with a red only a network run could clear. It now applies to the suites this case is tagged with. PR #35 R15/R16 added the `describes_a_deleted_rule` sweep. Three rounds running, the repair was correct and its own description was left behind: §2 still published the clean-tree requirement round 3 deleted, and §6, README and a code comment still said a derivation landing below the committed ceiling was red when it is green. Each rule this PR deleted or weakened leaves its old wording behind as a phrase no document may carry again. It closes re-assertion of THOSE rules, not the general class -- the general defence is one description in one place, which is what ADR-019 §6 now is. T-M32-9 (PR #34 R19/R27): four publications had drifted off `WALL_BUDGET_S = {\"fast\": 90, \"invariant\": 20}` at once — CLAUDE.md's Gate block (\"wall clock <= 15s\" / \"<= 75s\") and Commands block (\"ceiling 15s\"), this repo's stated working contract, so every committed `fast` run read as a breach against the contract while passing the gate it actually has; `fast-wall-clock-budget`'s own `expect.note` (\"falls back to the committed 80\" beside `env_override` rows all expecting 90); `evals/run.py`'s OVER BUDGET message crediting \"ADR-002 Decision 4\"; and ADR-002's Ruling (\"`fast` 80s local ... `invariant` has had its own 15s ceiling since ADR-019\", both wrong in the same sentence). Three of the four predate the branch that noticed, and the ceiling had moved twice in two days, so the literals are DROPPED rather than corrected wherever the prose allows — CLAUDE.md's Gate block now says \"wall clock <= the ceiling evals/run.py enforces\", which cannot go stale, and ADR-002's Ruling defers to ADR-019 §2/§3/§5 the way INDEX.md's line already did. What is graded is the ONE form that can only mean a live ceiling: a runnable `--suite X` command publishing a ceiling in its own trailing comment. A general number sweep was considered and rejected as unpricable: ~93 lines of tracked markdown pair a seconds literal with a ceiling word and nearly all of them are the record — ADR-002's own Decision 4 (60s), ADR-013's derivation prose, README's history section — and ADR-002's Ruling had a live number and a historical one in a single sentence, so no cheap pattern separates them and a grader that cannot would be switched off within a week (the lesson PR #34's round 5 had to preserve). Watched red on CLAUDE.md before the fix, against the unchanged file: {'publishes_a_ceiling_nothing_enforces': '--suite invariant ... 15s', 'committed': 20, 'doc': 'CLAUDE.md'}, {'... --suite fast ... 75s', 'committed': 90}, and the third from the Commands block — each row twice, because AGENTS.md is a symlink to CLAUDE.md and the walk sees both names. Exercised in all four directions against a scratch doc: a re-introduced \"--suite fast # wall clock <= 75s\" is red, the same line preserved inside a `~~struck~~` span is green (`prompts/` is append-only and records the gate block as it stood on its date — a guard that reddens on preserved history is one someone turns off, which is why struck spans come out first here exactly as they do for the criterion-5 sweep), and \"<= 90 seconds\" spelled out is green. PR #40 round 1 falsified this half four ways and every one is now pinned. R1: `_CEILING_LITERAL` matched any duration in a `--suite` comment, not a ceiling, so \"# ~71s on an M-series laptop\" and \"# p95 2.2s per case\" — both TRUE — reddened the gate while the message asserted the opposite. Watched red before the fix: {'publishes_a_ceiling_nothing_enforces': '--suite fast ... 71s', 'committed': 90} and the same at 2.2s. The literal is now anchored to `<=`/`≤`/`ceiling`/`budget`/`wall clock` within 24 characters. The deeper finding was the exercise set, not the regex: all four directions tried earlier were ceiling-shaped, so the over-fire had no way to appear — an exercise set that only tries what the code was written for proves nothing. Hence `ceiling_sweep_rows`, value-level payloads no file has to carry, graded through the SAME `_ceiling_drift` the file sweep uses so a row probes the sweep and not a copy of it; removing the anchor reddens two rows immediately. R5: the sweep reused `md_files`, which excludes `tasks/` and every dot-directory — 16 tracked files unscanned, including `.claude/skills/finish-task/SKILL.md`, a SECOND publication of the gate commands, where a ceiling would have been green forever. Watched: with a ceiling planted there the pre-fix sweep returned passed=True; after widening to every markdown file in the tree (`.git`/`.venv` excluded as machinery) the same plant gives {'publishes_a_ceiling_nothing_enforces': '--suite invariant ... 15s', 'committed': 20, 'doc': '.claude/skills/finish-task/SKILL.md'}, and a plant in `tasks/` reddens too. The criterion-5 scan keeps its `tasks/` carve-out — that exists because a tracker quotes forbidden phrases as the thing that must NOT be true, reasoning that does not transfer to a ceiling in a gate command. R2/R3: two more live publications, ADR-002's Status line and ADR-013's Ruling, both fixed by dropping the literal; neither is a `--suite` command, so neither is graded here and T-M32-16 says so.",
  "triage": {
    "class": "doc-drift",
    "note": "tagged invariant — a hand-maintained count is wrong again by the next milestone, and this repo has already burned a review round on three documents carrying three different totals for the same thing (PR #15, R19). The failure mode this closes is not the wrong number, it is the number that nothing recomputes. Adding a case to the suite now turns this red until README is updated, which is the intended cost. D8's cited reports are immutable artifacts, so its assertion is stable; moving the range means citing the runs it was measured from. The block cites report files rather than recomputing 'the latest run' on purpose: a report is immutable, so the assertion is stable, and moving the block means citing the runs it was measured from — the same contract D8's retired half had. The M29 half is the same shape one level up: a milestone's own ADR asserted its gate criterion was green-pending, and the milestone after it is the one that knows whether pending resolved true or false — nothing forced that resolution back into the ADR until this case checked for it. PR #28 R3 (LOW, routed to debt, tasks/TODO.md T-R33): prompts/ is append-only — a curated file records what was known and decided on its date and is never rewritten to look right in hindsight (the same principle ADR-015's own struck-not-deleted amendments follow, stated explicitly for prompts/ in prompts/README.md). prompts/014-a-freeze.md keeping its original 'green offline, pending' wording is correct by that convention, not a defect — a live reader who wants the current status reads specs/decisions/ADR-015-a-freeze.md, which this M29/M28 work keeps current, not a dated prompt record. PR #28 R4/R5: the markdown-strip ceiling is now literal and stated precisely (bold/italic/link unwrapped, underscore emphasis deliberately not — a real gap, named rather than implied), and the `prompts/` carve-out from R3 turned out to be the same mistake as the original 5-file allowlist one level down — excluding a directory is just a coarser allowlist. The general lesson, stated once rather than per-finding: for a guard whose job is 'does prose currently assert X', the safe move is to strike/fix the false claim, not to stop looking at the file that contains it. The T-M32-9 half grades a form, not a number and not a file: prose narrating a past ceiling is indistinguishable from prose publishing a live one by any pattern cheap enough to keep, so this reaches only the gate-command form and says so. Residual, named rather than implied: a live ceiling published in any other shape — an ADR Ruling, an INDEX one-liner, a support-matrix row — is invisible here. ADR-019's Ruling is covered separately by `published-band-matches-the-ledger` item 6 (ruling); ADR-002's is now covered by having no literal left to drift.",
    "watched_red": "PR #35 R6, red against the tree as this PR shipped it, no mutation needed: {cites_a_report_of_a_different_tree: 20260823-000232-fast.json, report_cases: 132, suite_now: 133} and {cites_a_report_of_a_different_tree: 20260823-000031-invariant.json, report_cases: 51, suite_now: 52}. Growing a suite without touching the docs is now exactly this red. R14 watched red against the pre-fix scoping, with `live` grown by one case: {cites_a_report_of_a_different_tree: 20260823-000337-live.json, report_cases: 9, suite_now: 10} -- unreachable offline. With the scoping in place the same mutation leaves only {readme_does_not_say: \"--suite live        # 10 cases\"}, a text edit. R15/R16 watched red against the tree as round 3 shipped it, no mutation needed: {describes_a_deleted_rule: \"the ceiling that maximum derives in the ADR's prose must be the ceiling\", doc: README.md}, {describes_a_deleted_rule: \"nothing uncommitted in the tree\", doc: specs/decisions/ADR-019-wall-clock-ceilings-per-suite.md} and {describes_a_deleted_rule: \"must multiply the published maximum and land on the ceiling\", doc: same}. The README one was not in the review's evidence -- it came out of the class sweep."
  }
}
